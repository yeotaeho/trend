# 검증 파이프라인 v2 구현서

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

버전 v0.1 · 2026-09-06 · 설계 원문: `검증파이프라인-v2-설계서.md` (커밋 `8f6ef1f`)

**Goal:** 키워드 목록 관문을 임베딩 기반 중복 판정, LLM 선별 배치, 피드백 되먹임으로 구성된 정책 관문으로 바꾼다.

**Architecture:** 항목마다 임베딩을 적재 시점에 한 번 저장하고(`items.embedding`), 72시간 창 정확 검색으로 중복·관련을 판정한다. 파이프라인 잡은 `1국면(항목별 관문) → 2국면(25건 선별 배치) → 3국면(항목별 점수·판정)` 으로 재구성되고, 모든 LLM 호출은 `llm_calls` 예약 행을 먼저 커밋한다. 피드백은 항목당 1건이며 최근접 사례로 프롬프트에 주입되고 소스 신뢰도를 보정한다.

**Tech Stack:** Python 3.12 · SQLAlchemy 2 async + asyncpg · Alembic · Neon Postgres(pgvector) · Anthropic SDK `messages.parse` · Voyage/OpenAI 임베딩 API(httpx) · pytest + respx

## Global Constraints

- 임베딩 차원은 **1024 고정**. 응답 길이가 다르면 예외(`EmbeddingDimError`)이지 `None` 이 아니다.
- 벡터 인덱스 없음. 모든 벡터 검색은 `published_at >= now() - 72h` 창 안의 정확 검색.
- 임베딩 텍스트는 `제목 + "\n" + 본문 앞 300자`. 보강 뒤 재계산하지 않는다.
- 상태 값 추가 없음. 관문 구분은 `decisions.stage` (`rule · triage · score · llm`).
- 단일 프로세스 전제. 배치 잠금(`FOR UPDATE SKIP LOCKED`)은 외부 호출 동안 유지.
- 일일 상한: 선별 60회, 판정 300회(탐색 3회 포함), 탐색 발송 1건. "오늘" 은 `Asia/Seoul` 달력일, 매 검사 시점의 현재 시각.
- `rules.yaml` 은 `extra="forbid"`. 삭제된 키가 남아 있으면 기동 실패.
- 한국어 문장 종결은 `.` `?` `!`. 새 소스 파일 첫 줄은 한 줄 한국어 역할 주석.
- 각 단계 끝: `uv run ruff check . && uv run ruff format . && uv run mypy app && uv run pytest` 통과 → 커밋 → Codex 리뷰 → 옵시디언 기록.
- 외부 API 호출 형식(Voyage·OpenAI·Anthropic 캐시 제어)은 구현 직전에 공식 문서로 대조한다. 이 문서의 요청 본문은 2026-09 기준 초안이다.

---

## 파일 지도

| 파일 | 단계 | 책임 |
|---|---|---|
| `app/db/alembic/versions/20260906_0003_embeddings.py` | 1 | 스키마 변경 전부 (vector 확장, items 컬럼, trigram 인덱스 제거, sources.trust_adjusted, feedback 유니크, llm_calls) |
| `app/db/types.py` (신규) | 1 | `Vector` 컬럼 타입. 쓰기만 지원, 읽기는 SQL 에서만 |
| `app/db/models.py` | 1 | `Item.embedding`·`embedding_model`, `Source.trust_adjusted`, `Feedback` 유니크, `LlmCall` |
| `app/api/discord.py`, `app/api/telegram.py` | 1 | 피드백 upsert |
| `app/pipeline/embedding.py` (신규) | 1 | 임베딩 어댑터 `embed`, 텍스트 조립, `embed_pending` |
| `app/pipeline/dedupe.py` | 1 | 벡터 쿼리 A·B, `classify`, `version_tokens`. trigram 코드 삭제 |
| `app/jobs/collect.py`, `app/jobs/pipeline.py` | 1 | 적재 직후 임베딩, 새 중복 판정 연결 |
| `scripts/backfill_embeddings.py`, `scripts/calibrate_dedupe.py` (신규) | 1 | 백필, 임계값 보정 |
| `app/config.py`, `config/rules.yaml` | 2 | 정책 설정 재구조화 |
| `app/pipeline/rules.py` | 2 | exclude 만 |
| `app/pipeline/scoring.py` | 2 | `rel` 항, `is_stale` |
| `app/db/budget.py` (신규) | 2 | `reserve_call` |
| `app/pipeline/triage.py` (신규) | 2 | 선별 배치 프롬프트·호출·파싱 |
| `app/pipeline/llm.py` | 2 | `client()`, `render_policy`, 판정 프롬프트 갱신 |
| `app/jobs/pipeline.py` | 2 | 3국면 재구성 |
| `app/pipeline/feedback.py` (신규) | 3 | 최근접 피드백 사례 |
| `app/jobs/notify.py`, `app/notify/discord.py`, `app/schemas.py` | 3 | 탐색 슬롯, `Level.EXPLORE` |
| `app/pipeline/trust.py` (신규), `scripts/weekly_report.py` (신규) | 4 | 신뢰도 보정, 주간 리포트 |

---

# 1단계 — 임베딩 저장과 벡터 중복 판정

끝나면: 새 항목마다 임베딩이 저장되고, 한·영 교차 중복이 잡히며, trigram 코드가 사라진다.

### Task 1.1: 마이그레이션 0003 과 모델

**Files:**
- Create: `app/db/types.py`
- Create: `app/db/alembic/versions/20260906_0003_embeddings.py`
- Modify: `app/db/models.py`

**Interfaces:**
- Produces: `app.db.types.Vector(dim)` (SQLAlchemy 타입), `Item.embedding`, `Item.embedding_model`, `Source.trust_adjusted`, `LlmCall(kind, batch_id, called_at)`, `Feedback` 의 `uq_feedback_item_id`

- [ ] **Step 1: `Vector` 타입 작성**

```python
# app/db/types.py
# 벡터 컬럼 타입 — pgvector 를 파이썬 의존성 없이 쓴다. 쓰기만 지원하고 비교·읽기는 SQL 에서 한다

from __future__ import annotations

from collections.abc import Callable, Sequence
from typing import Any

from sqlalchemy import Text, cast
from sqlalchemy.engine import Dialect
from sqlalchemy.sql.elements import ColumnElement
from sqlalchemy.types import UserDefinedType


class Vector(UserDefinedType[Sequence[float]]):
    """`vector(dim)` 컬럼.

    값은 `[0.1,0.2,…]` 문자열로 만들어 `CAST(CAST(:v AS text) AS vector(dim))` 로 넣는다.
    asyncpg 는 vector 타입 코덱이 없어서 파라미터가 vector 로 추론되면 실패한다.
    text 를 거치면 서버가 text→vector 캐스트를 해 준다. 파이썬에서 벡터를 읽는 경로는 없다.
    """

    cache_ok = True

    def __init__(self, dim: int) -> None:
        self.dim = dim

    def get_col_spec(self, **kw: Any) -> str:
        return f"vector({self.dim})"

    def bind_processor(self, dialect: Dialect) -> Callable[[Any], str | None]:
        def process(value: Sequence[float] | None) -> str | None:
            if value is None:
                return None
            return "[" + ",".join(f"{x:.7g}" for x in value) + "]"

        return process

    def bind_expression(self, bindvalue: ColumnElement[Any]) -> ColumnElement[Any]:
        return cast(cast(bindvalue, Text), self)
```

- [ ] **Step 2: 모델 수정**

`app/db/models.py` 의 import 에 `UniqueConstraint` 는 이미 있다. `from app.db.types import Vector` 를 추가하고 아래를 반영한다.

```python
class Source(Base):
    # … 기존 컬럼 뒤에
    # sources.yaml 의 trust_score 는 그대로 두고, 피드백으로 보정한 값은 여기. NULL 이면 미보정.
    trust_adjusted: Mapped[float | None] = mapped_column(Float)


class Item(Base):
    # … raw 뒤에
    # 적재 시점의 `제목 + 본문 300자` 임베딩. NULL 은 미계산 또는 실패. 파이썬에서 읽지 않는다
    # (deferred). 비교는 전부 SQL. 모델을 바꾸면 embedding_model 이 달라진 행을 백필이 재계산한다.
    embedding: Mapped[list[float] | None] = mapped_column(Vector(1024), deferred=True)
    embedding_model: Mapped[str | None] = mapped_column(Text)


class Feedback(Base):
    __tablename__ = "feedback"
    # 항목당 판정은 하나. 다시 누르면 덮어쓴다 (웹훅 upsert).
    __table_args__ = (UniqueConstraint("item_id", name="uq_feedback_item_id"),)

    id: Mapped[int] = mapped_column(primary_key=True)
    item_id: Mapped[int] = mapped_column(ForeignKey("items.id", ondelete="CASCADE"), index=True)
    verdict: Mapped[str] = mapped_column(String(10))
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())


class LlmCall(Base):
    """LLM 호출 직전에 독립 트랜잭션으로 커밋하는 예약 행. 일일 상한은 이 표로 센다."""

    __tablename__ = "llm_calls"

    id: Mapped[int] = mapped_column(primary_key=True)
    kind: Mapped[str] = mapped_column(String(10), index=True)  # triage | judge | explore
    batch_id: Mapped[str] = mapped_column(String(36))
    called_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), index=True)
```

- [ ] **Step 3: 마이그레이션 작성**

```python
# app/db/alembic/versions/20260906_0003_embeddings.py
"""임베딩 컬럼·llm_calls·feedback 유니크·trust_adjusted — 검증 파이프라인 v2

Revision ID: 0003
Revises: 0002
Create Date: 2026-09-06
"""

from __future__ import annotations

from collections.abc import Sequence

import sqlalchemy as sa
from alembic import op

from app.db.types import Vector

revision: str = "0003"
down_revision: str | None = "0002"
branch_labels: str | Sequence[str] | None = None
depends_on: str | Sequence[str] | None = None


def upgrade() -> None:
    op.execute("CREATE EXTENSION IF NOT EXISTS vector")
    op.add_column("items", sa.Column("embedding", Vector(1024), nullable=True))
    op.add_column("items", sa.Column("embedding_model", sa.Text(), nullable=True))
    # 제목 trigram 검색은 벡터 검색으로 대체된다. 확장은 다른 용도가 없어도 해가 없으니 둔다.
    op.execute("DROP INDEX IF EXISTS ix_items_title_trgm")

    op.add_column("sources", sa.Column("trust_adjusted", sa.Float(), nullable=True))

    # 피드백은 항목당 1건. 기존 중복은 최신(created_at DESC, id DESC) 1건만 남긴다.
    op.execute(
        """
        DELETE FROM feedback f
        USING (
            SELECT id, row_number() OVER (
                PARTITION BY item_id ORDER BY created_at DESC, id DESC
            ) AS rn
            FROM feedback
        ) ranked
        WHERE f.id = ranked.id AND ranked.rn > 1
        """
    )
    op.create_unique_constraint("uq_feedback_item_id", "feedback", ["item_id"])

    op.create_table(
        "llm_calls",
        sa.Column("id", sa.Integer(), primary_key=True),
        sa.Column("kind", sa.String(10), nullable=False),
        sa.Column("batch_id", sa.String(36), nullable=False),
        sa.Column("called_at", sa.DateTime(timezone=True), nullable=False),
    )
    op.create_index("ix_llm_calls_kind", "llm_calls", ["kind"])
    op.create_index("ix_llm_calls_called_at", "llm_calls", ["called_at"])


def downgrade() -> None:
    op.drop_table("llm_calls")
    op.drop_constraint("uq_feedback_item_id", "feedback", type_="unique")
    op.drop_column("sources", "trust_adjusted")
    op.execute("CREATE INDEX ix_items_title_trgm ON items USING gin (title gin_trgm_ops)")
    op.drop_column("items", "embedding_model")
    op.drop_column("items", "embedding")
```

- [ ] **Step 4: 마이그레이션 적용**

Run: `uv run alembic upgrade head`
Expected: `Running upgrade 0002 -> 0003` 로 끝남. 실패하면 Neon 콘솔에서 `vector` 확장 지원 여부부터 본다.

- [ ] **Step 5: 린트·타입·기존 테스트**

Run: `uv run ruff check . && uv run mypy app && uv run pytest -q`
Expected: 73 passed (아직 동작 변경 없음)

- [ ] **Step 6: 커밋**

```bash
git add app/db/types.py app/db/models.py app/db/alembic/versions/20260906_0003_embeddings.py
git commit -m "feat: 마이그레이션 0003 — items.embedding, llm_calls, feedback 유니크, sources.trust_adjusted"
```

### Task 1.2: 피드백 upsert

유니크 제약이 들어갔으므로 같은 버튼을 두 번 누르면 지금 코드는 IntegrityError 다. 두 웹훅을 upsert 로 바꾼다.

**Files:**
- Modify: `app/api/discord.py:74-75`, `app/api/telegram.py:41-42`
- Create: `app/db/feedback.py`
- Test: `tests/test_feedback_upsert.py`

**Interfaces:**
- Produces: `app.db.feedback.upsert_feedback(session, item_id: int, verdict: str) -> None`

- [ ] **Step 1: 실패하는 테스트**

```python
# tests/test_feedback_upsert.py
# 피드백 upsert 문장 테스트 — ON CONFLICT 로 verdict·created_at 을 덮어쓴다

from sqlalchemy.dialects import postgresql

from app.db.feedback import feedback_upsert_stmt


def test_upsert_overwrites_verdict_and_time():
    sql = str(feedback_upsert_stmt(7, "useless").compile(dialect=postgresql.dialect()))
    assert "ON CONFLICT (item_id) DO UPDATE" in sql
    assert "verdict = excluded.verdict" in sql
    assert "created_at = now()" in sql
```

- [ ] **Step 2: 실패 확인**

Run: `uv run pytest tests/test_feedback_upsert.py -v`
Expected: FAIL, `ModuleNotFoundError: app.db.feedback`

- [ ] **Step 3: 구현**

```python
# app/db/feedback.py
# 피드백 저장 — 항목당 1건, 다시 누르면 판정과 시각을 덮어쓴다

from __future__ import annotations

from sqlalchemy import func
from sqlalchemy.dialects.postgresql import Insert, insert
from sqlalchemy.ext.asyncio import AsyncSession

from app.db.models import Feedback


def feedback_upsert_stmt(item_id: int, verdict: str) -> Insert:
    stmt = insert(Feedback).values(item_id=item_id, verdict=verdict)
    return stmt.on_conflict_do_update(
        constraint="uq_feedback_item_id",
        set_={"verdict": stmt.excluded.verdict, "created_at": func.now()},
    )


async def upsert_feedback(session: AsyncSession, item_id: int, verdict: str) -> None:
    await session.execute(feedback_upsert_stmt(item_id, verdict))
```

두 웹훅에서 `session.add(Feedback(item_id=item_id, verdict=verdict))` 를 `await upsert_feedback(session, item_id, verdict)` 로 바꾸고, `from app.db.models import Feedback` 를 `from app.db.feedback import upsert_feedback` 로 바꾼다.

- [ ] **Step 4: 통과 확인**

Run: `uv run pytest tests/test_feedback_upsert.py tests/test_telegram_api.py tests/test_discord.py -v`
Expected: 모두 PASS

- [ ] **Step 5: 커밋**

```bash
git add app/db/feedback.py app/api/discord.py app/api/telegram.py tests/test_feedback_upsert.py
git commit -m "feat: 피드백 upsert — 항목당 1건, 재클릭은 덮어쓰기"
```

### Task 1.3: 임베딩 어댑터

**Files:**
- Create: `app/pipeline/embedding.py`
- Modify: `app/config.py` (`Settings`), `.env.example`, `tests/conftest.py`
- Test: `tests/test_embedding.py`

**Interfaces:**
- Produces: `embedding_text(title: str, summary_raw: str | None) -> str`, `async embed(texts: list[str]) -> list[list[float]] | None`, `async embed_pending(session, item_ids: list[int] | None = None) -> int`, `EmbeddingDimError`, `DIM = 1024`, `MAX_BATCH = 128`

- [ ] **Step 1: 설정 추가**

`app/config.py` `Settings` 에 추가한다.

```python
    # 임베딩. provider 는 voyage | openai. 모델을 바꾸면 scripts/backfill_embeddings.py 로 전량 재계산.
    embedding_provider: str = "voyage"
    embedding_model: str = "voyage-3.5-lite"
    voyage_api_key: str = ""
    openai_api_key: str = ""
```

`.env.example` 에 추가한다.

```
# 임베딩 — 중복·관련 판정과 피드백 사례 검색에 쓴다. 한·영 교차가 필요해 다국어 모델이어야 한다.
#   Voyage: https://dash.voyageai.com → API keys. 기본값.
#   OpenAI 키가 이미 있으면 EMBEDDING_PROVIDER=openai, EMBEDDING_MODEL=text-embedding-3-small.
#   모델을 바꾸면 uv run python scripts/backfill_embeddings.py 로 전량 재계산해야 한다.
EMBEDDING_PROVIDER=voyage
EMBEDDING_MODEL=voyage-3.5-lite
VOYAGE_API_KEY=
OPENAI_API_KEY=
```

`tests/conftest.py` 에 `os.environ.setdefault("VOYAGE_API_KEY", "test-voyage-key")` 를 추가한다.

- [ ] **Step 2: 실패하는 테스트**

```python
# tests/test_embedding.py
# 임베딩 어댑터 테스트 — 텍스트 조립, 배치 분할, 차원 검증, 재시도와 None 폴백

import httpx
import pytest
import respx

from app.pipeline.embedding import DIM, EmbeddingDimError, embed, embedding_text

VOYAGE = "https://api.voyageai.com/v1/embeddings"


def vec(seed: float) -> list[float]:
    return [seed] * DIM


def test_embedding_text_joins_title_and_300_chars():
    text = embedding_text("제목", "본" * 500)
    assert text.startswith("제목\n")
    assert len(text) == len("제목\n") + 300


def test_embedding_text_without_body():
    assert embedding_text("제목", None) == "제목"


@pytest.mark.asyncio
@respx.mock
async def test_embed_returns_vectors_in_order():
    route = respx.post(VOYAGE).mock(
        return_value=httpx.Response(
            200, json={"data": [{"index": 0, "embedding": vec(0.1)}, {"index": 1, "embedding": vec(0.2)}]}
        )
    )
    result = await embed(["a", "b"])
    assert result == [vec(0.1), vec(0.2)]
    sent = route.calls[0].request
    assert sent.headers["authorization"] == "Bearer test-voyage-key"
    body = sent.read().decode()
    assert '"output_dimension": 1024' in body or '"output_dimension":1024' in body


@pytest.mark.asyncio
@respx.mock
async def test_wrong_dimension_raises_not_none():
    respx.post(VOYAGE).mock(
        return_value=httpx.Response(200, json={"data": [{"index": 0, "embedding": [0.1] * 512}]})
    )
    with pytest.raises(EmbeddingDimError):
        await embed(["a"])


@pytest.mark.asyncio
@respx.mock
async def test_server_error_retries_then_none(monkeypatch):
    monkeypatch.setattr("app.pipeline.embedding.RETRY_WAIT", 0)
    route = respx.post(VOYAGE).mock(return_value=httpx.Response(503))
    assert await embed(["a"]) is None
    assert route.call_count == 3


@pytest.mark.asyncio
@respx.mock
async def test_client_error_fails_immediately(monkeypatch):
    monkeypatch.setattr("app.pipeline.embedding.RETRY_WAIT", 0)
    route = respx.post(VOYAGE).mock(return_value=httpx.Response(401))
    assert await embed(["a"]) is None
    assert route.call_count == 1


@pytest.mark.asyncio
async def test_more_than_max_batch_is_rejected():
    with pytest.raises(ValueError):
        await embed(["x"] * 129)
```

- [ ] **Step 3: 실패 확인**

Run: `uv run pytest tests/test_embedding.py -v`
Expected: FAIL, `ModuleNotFoundError: app.pipeline.embedding`

- [ ] **Step 4: 구현**

```python
# app/pipeline/embedding.py
# 임베딩 어댑터 — Voyage/OpenAI 호출, 항목 텍스트 조립, 미계산 항목 채우기

from __future__ import annotations

import asyncio
from typing import Any

import httpx
from sqlalchemy import select, update
from sqlalchemy.ext.asyncio import AsyncSession

from app.config import get_settings
from app.db.models import Item
from app.log import get_logger

DIM = 1024
MAX_BATCH = 128
SNIPPET_CHARS = 300
TIMEOUT = httpx.Timeout(30.0)
RETRY_ATTEMPTS = 3
RETRY_WAIT = 1.0  # 초. 시도 번호를 곱한다
log = get_logger(__name__)


class EmbeddingDimError(RuntimeError):
    """응답 차원이 DIM 과 다르다. 장애가 아니라 설정 오류라 None 으로 숨기지 않는다."""


def embedding_text(title: str, summary_raw: str | None) -> str:
    """적재 시점에 한 번만 만든다. 보강 뒤 다시 만들지 않는다 — 모든 벡터가 같은 재료여야 비교가 맞다."""
    snippet = (summary_raw or "")[:SNIPPET_CHARS]
    return f"{title}\n{snippet}" if snippet else title


def _request(texts: list[str]) -> tuple[str, dict[str, str], dict[str, Any]]:
    """제공자별 (url, headers, json). 요청 형식은 구현 직전 공식 문서로 대조한다."""
    s = get_settings()
    if s.embedding_provider == "openai":
        return (
            "https://api.openai.com/v1/embeddings",
            {"Authorization": f"Bearer {s.openai_api_key}"},
            {"input": texts, "model": s.embedding_model, "dimensions": DIM},
        )
    return (
        "https://api.voyageai.com/v1/embeddings",
        {"Authorization": f"Bearer {s.voyage_api_key}"},
        {
            "input": texts,
            "model": s.embedding_model,
            "output_dimension": DIM,
            "input_type": "document",
        },
    )


async def embed(texts: list[str]) -> list[list[float]] | None:
    """텍스트 순서대로 벡터를 돌려준다. 네트워크·5xx·429 만 재시도하고 최종 실패는 None 이다."""
    if len(texts) > MAX_BATCH:
        raise ValueError(f"한 호출 최대 {MAX_BATCH}건")
    if not texts:
        return []
    url, headers, payload = _request(texts)
    for attempt in range(1, RETRY_ATTEMPTS + 1):
        try:
            async with httpx.AsyncClient(timeout=TIMEOUT) as client:
                response = await client.post(url, json=payload, headers=headers)
        except httpx.TransportError as exc:
            log.warning("embedding.transport_error", attempt=attempt, error=str(exc))
            if attempt == RETRY_ATTEMPTS:
                return None
            await asyncio.sleep(RETRY_WAIT * attempt)
            continue

        if response.status_code < 400:
            rows = sorted(response.json()["data"], key=lambda r: int(r["index"]))
            vectors = [[float(x) for x in r["embedding"]] for r in rows]
            bad = [len(v) for v in vectors if len(v) != DIM]
            if bad or len(vectors) != len(texts):
                raise EmbeddingDimError(
                    f"차원 {bad[:1] or '?'} 또는 개수 {len(vectors)}/{len(texts)} 가 맞지 않음"
                )
            return vectors

        retry = response.status_code == 429 or response.status_code >= 500
        log.warning("embedding.http_error", status=response.status_code, attempt=attempt)
        if not retry or attempt == RETRY_ATTEMPTS:
            return None
        await asyncio.sleep(RETRY_WAIT * attempt)
    return None


async def embed_pending(session: AsyncSession, item_ids: list[int] | None = None) -> int:
    """embedding 이 NULL 인 항목(주어지면 그 id 안에서)을 최대 MAX_BATCH 건 계산해 저장한다.

    실패하면 NULL 로 남기고 0 을 돌려준다. 다음 잡이 다시 시도한다.
    """
    stmt = (
        select(Item.id, Item.title, Item.summary_raw)
        .where(Item.embedding.is_(None))
        .order_by(Item.id)
        .limit(MAX_BATCH)
    )
    if item_ids is not None:
        if not item_ids:
            return 0
        stmt = stmt.where(Item.id.in_(item_ids))
    rows = (await session.execute(stmt)).all()
    if not rows:
        return 0

    vectors = await embed([embedding_text(title, body) for _, title, body in rows])
    if vectors is None:
        log.warning("embedding.batch_failed", count=len(rows))
        return 0

    model = get_settings().embedding_model
    for (item_id, _, _), vector in zip(rows, vectors, strict=True):
        await session.execute(
            update(Item)
            .where(Item.id == item_id)
            .values(embedding=vector, embedding_model=model)
        )
    return len(rows)
```

- [ ] **Step 5: 통과 확인**

Run: `uv run pytest tests/test_embedding.py -v`
Expected: 7 passed

- [ ] **Step 6: 커밋**

```bash
git add app/pipeline/embedding.py app/config.py .env.example tests/conftest.py tests/test_embedding.py
git commit -m "feat: 임베딩 어댑터 — Voyage/OpenAI, 1024 차원 계약, 재시도 후 None 폴백"
```

### Task 1.4: 적재 직후 임베딩과 백필 스크립트

**Files:**
- Modify: `app/jobs/collect.py:36-43`
- Create: `scripts/backfill_embeddings.py`

**Interfaces:**
- Consumes: `embed_pending(session, item_ids)`

- [ ] **Step 1: 수집 잡 연결**

`run_source` 에서 세이브포인트 커밋 직후, `except Exception as exc:` 블록 **밖**(정상 경로)에 넣는다. 임베딩 실패는 수집 실패가 아니다.

```python
            await savepoint.commit()
        except Exception as exc:
            # … 기존 실패 처리 그대로 …
            return 0

        if inserted:
            # 임베딩 실패는 수집 실패가 아니다. NULL 로 남기면 파이프라인 잡이 다시 시도한다.
            try:
                await embed_pending(session, inserted)
            except Exception as exc:
                log.warning("collect.embed_failed", source=source.name, error=str(exc))

        source.last_polled_at = datetime.now(UTC)
```

import 에 `from app.pipeline.embedding import embed_pending` 를 추가한다. `EmbeddingDimError` 도 여기서 잡혀 로그로 남는데, 이건 설정 오류라 `send_ops_alert` 로도 보낸다.

```python
            except EmbeddingDimError as exc:
                log.error("collect.embed_dim_error", error=str(exc))
                try:
                    await send_ops_alert(f"임베딩 차원 오류: {exc}")
                except Exception as alert_exc:
                    log.warning("collect.alert_failed", error=str(alert_exc))
            except Exception as exc:
                log.warning("collect.embed_failed", source=source.name, error=str(exc))
```

- [ ] **Step 2: 백필 스크립트**

```python
# scripts/backfill_embeddings.py
# 임베딩 백필 — NULL 이거나 모델이 바뀐 행을 128건씩 계산한다. 멱등이라 중단 후 재실행 가능

from __future__ import annotations

import asyncio

from sqlalchemy import func, select, update

from app.config import get_settings
from app.db.models import Item
from app.db.session import engine, session_scope
from app.log import configure_logging
from app.pipeline.embedding import MAX_BATCH, embed_pending


async def main() -> None:
    configure_logging()
    model = get_settings().embedding_model
    try:
        async with session_scope() as session:
            # 모델이 바뀐 행은 NULL 로 되돌려 같은 경로로 재계산한다.
            stale = await session.execute(
                update(Item)
                .where(Item.embedding_model.is_not(None), Item.embedding_model != model)
                .values(embedding=None, embedding_model=None)
            )
            print(f"모델 불일치로 초기화: {stale.rowcount}")
            await session.commit()

        total = 0
        while True:
            async with session_scope() as session:
                done = await embed_pending(session)
            if done == 0:
                break
            total += done
            print(f"진행: {total}")

        async with session_scope() as session:
            left = (
                await session.execute(
                    select(func.count()).select_from(Item).where(Item.embedding.is_(None))
                )
            ).scalar_one()
        print(f"완료: {total}건 계산, 남은 NULL {left}건 (0 이 아니면 재실행)")
    finally:
        await engine.dispose()


if __name__ == "__main__":
    asyncio.run(main())
```

`embed_pending` 이 0 을 돌려주는 경우는 두 가지다. 더 없거나 이번 배치가 실패했거나. 실패면 로그에 `embedding.batch_failed` 가 남고 스크립트는 멈추므로 재실행하면 된다. 4,600건이면 37회 호출이다.

- [ ] **Step 3: 백필 실행**

Run: `PYTHONIOENCODING=utf-8 uv run python scripts/backfill_embeddings.py`
Expected: 마지막 줄 `완료: 4xxx건 계산, 남은 NULL 0건`. `MAX_BATCH` 는 import 만 하고 안 쓰면 ruff 가 잡으니 `print` 문구에 넣거나 import 를 뺀다.

- [ ] **Step 4: 검증·커밋**

Run: `uv run ruff check . && uv run mypy app && uv run pytest -q`
Expected: 전부 통과

```bash
git add app/jobs/collect.py scripts/backfill_embeddings.py
git commit -m "feat: 적재 직후 임베딩 계산, 백필 스크립트"
```

### Task 1.5: 벡터 중복·관련 판정

**Files:**
- Modify: `app/pipeline/dedupe.py` (전면 교체), `app/config.py` (`DedupeConfig`, `Rules.dedupe`), `config/rules.yaml`
- Test: `tests/test_dedupe.py`

**Interfaces:**
- Produces:
  - `DedupeConfig(dup_threshold=0.92, related_threshold=0.80, window_hours=72)`
  - `Candidate(id, source_id, cluster_id, title, sim)`
  - `Verdict(kind: Literal["dup","related","independent"], cluster_id: int, mention_count: int)`
  - `version_tokens(title: str) -> frozenset[str]`
  - `classify(*, item_id, own_source_id, own_title, dup, related, cfg) -> Verdict`
  - `async find_candidates(session, item_id, cfg) -> tuple[Candidate | None, list[Candidate]]`
  - `async dedupe_skipped(session, item_id) -> bool` 는 두지 않는다. 임베딩 NULL 판단은 호출자가 한다

- [ ] **Step 1: 설정**

`app/config.py` 에 `ScoringConfig` 앞에 추가하고 `Rules` 에 `dedupe: DedupeConfig = Field(default_factory=DedupeConfig)` 를 넣는다.

```python
class DedupeConfig(BaseModel):
    # 출발값. scripts/calibrate_dedupe.py 의 표본을 보고 정한다.
    dup_threshold: float = 0.92
    related_threshold: float = 0.80
    window_hours: int = 72
```

`config/rules.yaml` 의 `scoring:` 앞에 추가한다.

```yaml
# 벡터 유사도 임계값. 중복(≥dup)은 생존 항목만 기준, 관련(≥related)은 상태 무관.
dedupe:
  dup_threshold: 0.92
  related_threshold: 0.80
  window_hours: 72
```

- [ ] **Step 2: 실패하는 테스트**

```python
# tests/test_dedupe.py
# 중복·관련 분류 테스트 — 임계값, 생존자 기준, 버전 예외, 같은 소스 미집계, NULL 클러스터

from app.config import DedupeConfig
from app.pipeline.dedupe import Candidate, classify, version_tokens

CFG = DedupeConfig()


def cand(id: int, sim: float, source_id: int = 2, cluster_id: int | None = None, title: str = "t"):
    return Candidate(id=id, source_id=source_id, cluster_id=cluster_id, title=title, sim=sim)


def run(dup=None, related=(), title="Claude Fable 5.1 released", source_id=1):
    return classify(
        item_id=100, own_source_id=source_id, own_title=title, dup=dup, related=list(related), cfg=CFG
    )


def test_version_tokens():
    assert version_tokens("Next.js v16.4.0-canary.14 released") == {"v16.4.0-canary.14"}
    assert version_tokens("uv 0.12.9 and 0.12.8 compared") == {"0.12.9", "0.12.8"}
    assert version_tokens("no version here") == frozenset()


def test_dup_inherits_cluster_of_survivor():
    v = run(dup=cand(5, 0.95, cluster_id=3))
    assert v.kind == "dup" and v.cluster_id == 3


def test_dup_with_null_cluster_uses_candidate_id():
    v = run(dup=cand(5, 0.95, cluster_id=None))
    assert v.kind == "dup" and v.cluster_id == 5


def test_below_dup_threshold_is_not_dup():
    v = run(dup=cand(5, 0.90), related=[cand(5, 0.90)])
    assert v.kind == "related" and v.cluster_id == 5


def test_different_version_demotes_dup_to_related():
    v = run(
        dup=cand(5, 0.97, title="Next.js v16.4.0-canary.13"),
        related=[cand(5, 0.97, title="Next.js v16.4.0-canary.13")],
        title="Next.js v16.4.0-canary.14",
    )
    assert v.kind == "related"


def test_missing_version_on_one_side_keeps_dup():
    v = run(dup=cand(5, 0.97, title="Next.js canary release"), title="Next.js v16.4.0-canary.14")
    assert v.kind == "dup"


def test_independent_gets_own_id():
    v = run()
    assert v.kind == "independent" and v.cluster_id == 100 and v.mention_count == 1


def test_mention_count_excludes_own_source_but_clusters_still_inherit():
    v = run(related=[cand(5, 0.85, source_id=1, cluster_id=9)])
    assert v.kind == "related" and v.cluster_id == 9 and v.mention_count == 1


def test_mention_count_distinct_other_sources():
    v = run(related=[cand(5, 0.9, source_id=2), cand(6, 0.85, source_id=3), cand(7, 0.81, source_id=2)])
    assert v.mention_count == 3
```

- [ ] **Step 3: 실패 확인**

Run: `uv run pytest tests/test_dedupe.py -v`
Expected: FAIL, `ImportError: cannot import name 'Candidate'`

- [ ] **Step 4: 구현 (파일 전체 교체)**

```python
# app/pipeline/dedupe.py
# 중복·관련 판정 — 72시간 창 안 임베딩 코사인 유사도. 중복은 생존 항목 기준, 관련은 상태 무관

from __future__ import annotations

import re
from dataclasses import dataclass
from datetime import UTC, datetime, timedelta
from typing import Literal

from sqlalchemy import text
from sqlalchemy.ext.asyncio import AsyncSession

from app.config import DedupeConfig
from app.schemas import ItemStatus

_SURVIVED = (ItemStatus.SCORED.value, ItemStatus.QUEUED.value, ItemStatus.SENT.value)
# 버전 토큰 통째로: 16.4.0-canary.14, v1.2.3, 0.12.9. 앞뒤 경계는 공백·괄호·문장 끝.
_VERSION = re.compile(r"(?<![\w.])(v?\d+(?:\.\d+)+(?:[-.][0-9A-Za-z.]+)?)(?![\w.])")

_DUP_SQL = text(
    """
    WITH q AS (SELECT embedding FROM items WHERE id = :item_id)
    SELECT i.id, i.source_id, i.cluster_id, i.title,
           1 - (i.embedding <=> q.embedding) AS sim
    FROM items i, q
    WHERE i.id <> :item_id
      AND i.embedding IS NOT NULL
      AND i.published_at >= :since
      AND i.status IN :statuses
    ORDER BY sim DESC
    LIMIT 1
    """
).bindparams(bindparam("statuses", expanding=True))

_RELATED_SQL = text(
    """
    WITH q AS (SELECT embedding FROM items WHERE id = :item_id)
    SELECT i.id, i.source_id, i.cluster_id, i.title,
           1 - (i.embedding <=> q.embedding) AS sim
    FROM items i, q
    WHERE i.id <> :item_id
      AND i.embedding IS NOT NULL
      AND i.published_at >= :since
      AND 1 - (i.embedding <=> q.embedding) >= :related
    ORDER BY sim DESC
    """
)


@dataclass(slots=True, frozen=True)
class Candidate:
    id: int
    source_id: int
    cluster_id: int | None
    title: str
    sim: float


@dataclass(slots=True, frozen=True)
class Verdict:
    kind: Literal["dup", "related", "independent"]
    cluster_id: int
    mention_count: int


def version_tokens(title: str) -> frozenset[str]:
    return frozenset(_VERSION.findall(title))


def _inherit(c: Candidate) -> int:
    # 아직 처리되지 않은 NEW 후보는 cluster_id 가 NULL 이다. 그 항목 자신이 뿌리가 된다.
    return c.cluster_id if c.cluster_id is not None else c.id


def classify(
    *,
    item_id: int,
    own_source_id: int,
    own_title: str,
    dup: Candidate | None,
    related: list[Candidate],
    cfg: DedupeConfig,
) -> Verdict:
    """쿼리 결과만 받아 판정한다. DB 없이 테스트하기 위해 순수 함수다."""
    others = {c.source_id for c in related} - {own_source_id}
    mentions = len(others) + 1

    if dup is not None and dup.sim >= cfg.dup_threshold:
        mine, theirs = version_tokens(own_title), version_tokens(dup.title)
        if not (mine and theirs and mine != theirs):
            return Verdict("dup", _inherit(dup), mentions)
        # 버전이 다르면 같은 릴리즈가 아니다. 관련로 내린다.

    if related:
        best = max(related, key=lambda c: c.sim)
        return Verdict("related", _inherit(best), mentions)
    return Verdict("independent", item_id, mentions)


async def find_candidates(
    session: AsyncSession, item_id: int, cfg: DedupeConfig
) -> tuple[Candidate | None, list[Candidate]]:
    """쿼리 A(생존자 중 최댓값 1건)와 B(관련 임계값 이상 전부). 항목의 embedding 이 NULL 이면 (None, [])."""
    since = datetime.now(UTC) - timedelta(hours=cfg.window_hours)
    dup_row = (
        await session.execute(
            _DUP_SQL, {"item_id": item_id, "since": since, "statuses": list(_SURVIVED)}
        )
    ).first()
    related_rows = (
        await session.execute(
            _RELATED_SQL, {"item_id": item_id, "since": since, "related": cfg.related_threshold}
        )
    ).all()
    to_cand = lambda r: Candidate(r.id, r.source_id, r.cluster_id, r.title, float(r.sim))  # noqa: E731
    return (to_cand(dup_row) if dup_row else None, [to_cand(r) for r in related_rows])
```

`from sqlalchemy import bindparam, text` 로 import 를 맞춘다. 항목의 `embedding` 이 NULL 이면 `q` 가 NULL 이라 `<=>` 결과도 NULL 이고 두 쿼리 모두 빈 결과다. 호출자는 그 경우를 `dedupe_skipped` 로 기록한다.

- [ ] **Step 5: 통과 확인**

Run: `uv run pytest tests/test_dedupe.py -v`
Expected: 10 passed

- [ ] **Step 6: 커밋**

```bash
git add app/pipeline/dedupe.py app/config.py config/rules.yaml tests/test_dedupe.py
git commit -m "feat: 벡터 중복·관련 판정 — 쿼리 A/B 분리, 버전 토큰 예외, 순수 함수 classify"
```

### Task 1.6: 파이프라인에 새 중복 판정 연결

2단계에서 3국면으로 재구성하기 전까지는 `_process` 의 앞부분만 바꾼다.

**Files:**
- Modify: `app/jobs/pipeline.py:48-58` (`_process` 첫 블록), `app/jobs/pipeline.py:162-170` (`run_pipeline` 배치 직후), `:100-105` (mention_count 호출)

- [ ] **Step 1: `_process` 중복 블록 교체**

```python
async def _process(session: AsyncSession, item: Item, source: Source) -> bool:
    rules = get_rules()

    dup, related = await find_candidates(session, item.id, rules.dedupe)
    embedded = await session.scalar(
        select(Item.embedding.is_not(None)).where(Item.id == item.id)
    )
    if not embedded:
        # 임베딩 실패 항목. 중복·다중소스 판정 없이 진행한다 — 장애가 항목을 죽이면 안 된다.
        session.add(_record(item, Stage.RULE, True, {"reason": "dedupe_skipped"}))
        verdict = Verdict("independent", item.id, 1)
    else:
        verdict = classify(
            item_id=item.id,
            own_source_id=item.source_id,
            own_title=item.title,
            dup=dup,
            related=related,
            cfg=rules.dedupe,
        )
    item.cluster_id = verdict.cluster_id
    if verdict.kind == "dup":
        item.status = ItemStatus.FILTERED_OUT.value
        session.add(
            _record(item, Stage.RULE, False, {"reason": "dup", "cluster_id": verdict.cluster_id})
        )
        return False
```

이어지는 점수 블록에서 `mentions = await mention_count(...)` 를 지우고 `mention_count=verdict.mention_count` 를 쓴다. import 는 `from app.pipeline.dedupe import Verdict, classify, find_candidates` 로 바꾼다.

- [ ] **Step 2: 배치 시작 시 임베딩 보충**

`run_pipeline` 에서 `for item, source in await _claim_batch(session):` 앞에 넣는다.

```python
        batch = await _claim_batch(session)
        # 수집 때 실패한 임베딩을 다시 시도한다. 여기서도 실패하면 dedupe_skipped 로 진행한다.
        await embed_pending(session, [item.id for item, _ in batch])
        for item, source in batch:
```

import 에 `from app.pipeline.embedding import embed_pending` 을 추가한다.

- [ ] **Step 3: 실행 확인**

Run: `PYTHONIOENCODING=utf-8 uv run python scripts/run_job.py pipeline`
Expected: 예외 없이 `pipeline.done` 로그. `decisions` 에 `reason: dup` 또는 `dedupe_skipped` 가 새 형식으로 쌓인다.

- [ ] **Step 4: 검증·커밋**

Run: `uv run ruff check . && uv run mypy app && uv run pytest -q`

```bash
git add app/jobs/pipeline.py
git commit -m "feat: 파이프라인이 벡터 중복 판정을 쓴다, 배치 시작 시 임베딩 보충"
```

### Task 1.7: 임계값 보정 스크립트

**Files:**
- Create: `scripts/calibrate_dedupe.py`

- [ ] **Step 1: 스크립트**

```python
# scripts/calibrate_dedupe.py
# 중복 임계값 보정 — 최근 7일 항목 쌍을 유사도 구간별로 10개씩 보여준다. 보고 rules.yaml 을 정한다

from __future__ import annotations

import asyncio

from sqlalchemy import text

from app.db.session import engine, session_scope

BANDS = [(0.95, 1.01), (0.90, 0.95), (0.85, 0.90), (0.80, 0.85), (0.75, 0.80)]
SQL = text(
    """
    SELECT a.title AS ta, b.title AS tb, sa.name AS sa, sb.name AS sb,
           1 - (a.embedding <=> b.embedding) AS sim
    FROM items a
    JOIN items b ON a.id < b.id
    JOIN sources sa ON sa.id = a.source_id
    JOIN sources sb ON sb.id = b.source_id
    WHERE a.published_at >= now() - interval '7 days'
      AND b.published_at >= now() - interval '7 days'
      AND a.embedding IS NOT NULL AND b.embedding IS NOT NULL
      AND 1 - (a.embedding <=> b.embedding) >= :lo
      AND 1 - (a.embedding <=> b.embedding) < :hi
    ORDER BY random()
    LIMIT 10
    """
)


async def main() -> None:
    try:
        async with session_scope() as session:
            for lo, hi in BANDS:
                rows = (await session.execute(SQL, {"lo": lo, "hi": hi})).all()
                print(f"\n=== {lo:.2f} ~ {min(hi, 1.0):.2f}  ({len(rows)}쌍 표본) ===")
                for r in rows:
                    print(f"{r.sim:.3f} | {r.sa} | {r.ta[:70]}")
                    print(f"      | {r.sb} | {r.tb[:70]}")
    finally:
        await engine.dispose()


if __name__ == "__main__":
    asyncio.run(main())
```

- [ ] **Step 2: 실행하고 임계값 확정**

Run: `PYTHONIOENCODING=utf-8 uv run python scripts/calibrate_dedupe.py`
Expected: 구간별 쌍 목록. 사용자가 읽고 "같은 사건" 이 시작되는 구간을 `dup_threshold`, "같은 주제" 가 시작되는 구간을 `related_threshold` 로 `rules.yaml` 에 적는다. 이 판단은 사람이 한다.

- [ ] **Step 3: 커밋**

```bash
git add scripts/calibrate_dedupe.py config/rules.yaml
git commit -m "feat: 중복 임계값 보정 스크립트, 보정된 dedupe 임계값"
```

### Task 1.8: 1단계 마무리

- [ ] 전체 검사: `uv run ruff check . && uv run ruff format . && uv run mypy app && uv run pytest -q`
- [ ] `git grep -n "pg_trgm\|similarity(" app/` 결과가 비어 있는지 확인. 남았으면 삭제.
- [ ] Codex 리뷰: `codex exec --sandbox read-only "커밋 범위 <0003 첫 커밋>..HEAD 를 리뷰. 관점: 벡터 SQL 의 파라미터 캐스트, 세이브포인트와 update 문의 상호작용, 임베딩 실패 경로, 삭제되지 않은 trigram 잔재" </dev/null`
- [ ] 지적 반영 → 재커밋 → 재리뷰.
- [ ] 옵시디언: `spoke/검증 파이프라인 v2.md` 기능 노트 생성, 허브 표 갱신, 일일 노트 `기술파악 - 임베딩·벡터 중복 판정.md`.

---

# 2단계 — 정책 설정, LLM 선별, 점수 관문

끝나면: 키워드 없는 새 용어 항목이 통과하고, 탈락 이유가 `decisions` 에 문장으로 남고, 화이트리스트·키워드 관문 코드가 사라진다.

### Task 2.1: 설정 재구조화

**Files:**
- Modify: `app/config.py`, `config/rules.yaml`, `tests/test_rules.py` (임시로 `Rules(...)` 생성부만)

**Interfaces:**
- Produces:
  - `PolicyConfig(interests: str, not_interested: str, focus_repos: list[str], focus_stack: list[str])`
  - `ExcludeConfig(keywords: list[str], domains: list[str])`
  - `TriageConfig(batch_size=25, daily_cap_calls=60)`
  - `ScoringConfig(w_src, w_rel, w_hot, w_multi, w_fresh, threshold, max_age_hours=72)`
  - `NotifyConfig(..., explore_judge_cap=3)`
  - `Rules(policy, exclude, dedupe, triage, scoring, notify)` 전부 `extra="forbid"`

- [ ] **Step 1: 설정 모델**

`app/config.py` 에서 `ScoringConfig`·`NotifyConfig`·`Rules` 를 아래로 바꾸고 `PolicyConfig`·`ExcludeConfig`·`TriageConfig` 를 추가한다. `from pydantic import BaseModel, ConfigDict, Field`.

```python
class _Strict(BaseModel):
    # 삭제된 키(include_keywords, always_pass_sources …)가 남아 있으면 기동이 실패해야 한다.
    model_config = ConfigDict(extra="forbid")


class PolicyConfig(_Strict):
    """선별·판정 프롬프트가 읽는 정책. 문장으로 쓴다. 관문이 아니라 힌트다."""

    interests: str = ""
    not_interested: str = ""
    focus_repos: list[str] = Field(default_factory=list)
    focus_stack: list[str] = Field(default_factory=list)


class ExcludeConfig(_Strict):
    keywords: list[str] = Field(default_factory=list)
    domains: list[str] = Field(default_factory=list)


class DedupeConfig(_Strict):
    dup_threshold: float = 0.92
    related_threshold: float = 0.80
    window_hours: int = 72


class TriageConfig(_Strict):
    batch_size: int = 25
    daily_cap_calls: int = 60


class ScoringConfig(_Strict):
    w_src: float = 0.25
    w_rel: float = 0.25
    w_hot: float = 0.2
    w_multi: float = 0.2
    w_fresh: float = 0.1
    threshold: float = 0.45
    # 모든 소스 공통. 이보다 오래된 항목은 선별 호출 없이 stale 로 버린다.
    max_age_hours: int = 72


class NotifyConfig(_Strict):
    daily_push_cap: int = 15
    quiet_start_hour: int = 23
    quiet_end_hour: int = 8
    timezone: str = "Asia/Seoul"
    explore_judge_cap: int = 3


class Rules(_Strict):
    """config/rules.yaml — 정책·제외 규칙·임계값·발송 정책."""

    policy: PolicyConfig = Field(default_factory=PolicyConfig)
    exclude: ExcludeConfig = Field(default_factory=ExcludeConfig)
    dedupe: DedupeConfig = Field(default_factory=DedupeConfig)
    triage: TriageConfig = Field(default_factory=TriageConfig)
    scoring: ScoringConfig = Field(default_factory=ScoringConfig)
    notify: NotifyConfig = Field(default_factory=NotifyConfig)
```

- [ ] **Step 2: `rules.yaml` 전면 교체**

```yaml
# 검증 파이프라인 정책. 관문 순서: stale → 중복 → exclude → LLM 선별 → 점수 → LLM 판정 → 발송.
# 키워드 목록은 관문이 아니다. 선별·판정 프롬프트가 아래 policy 를 문장으로 읽는다.

policy:
  interests: |
    - AI 시스템 개발자. LLM 애플리케이션·에이전트·MCP 를 직접 만든다.
    - 주력 스택: Python(FastAPI, SQLAlchemy, Pydantic), Postgres, Docker, TypeScript/Next.js.
    - 관심: 새 모델 출시와 가격·컨텍스트 변화, 에이전트·RAG 기법, MCP 서버와 개발 도구,
      쓰는 라이브러리의 메이저 릴리즈, 개발 생태계의 정책·가격 변화.
  not_interested: |
    - 채용·홍보·입문자용 튜토리얼·이미 널리 알려진 일반론.
    - 특정 종목·투자 도구처럼 개발과 무관한 금융 콘텐츠.
  # "특히 주목" 힌트. 프롬프트에 그대로 들어간다.
  focus_repos:
    - anthropics/*
    - modelcontextprotocol/*
    - langchain-ai/*
    - astral-sh/*
    - vercel/next.js
    - fastapi/fastapi
    - pydantic/pydantic
  focus_stack: [claude, mcp, langgraph, fastapi, pydantic, sqlalchemy, postgres, next.js, uv, ruff]

# 싸고 정확한 제외 규칙만 남긴다. exclude 는 언제나 우선한다.
exclude:
  keywords: [hiring, "we're hiring", giveaway, webinar, sponsored, "discount code", 채용, 광고]
  domains: []

# 벡터 유사도 임계값. 중복(≥dup)은 생존 항목만 기준, 관련(≥related)은 상태 무관.
dedupe:
  dup_threshold: 0.92
  related_threshold: 0.80
  window_hours: 72

# LLM 선별 배치. 25건을 한 호출에 넣고 관련도 0~1 을 받는다.
triage:
  batch_size: 25
  daily_cap_calls: 60

scoring:
  w_src: 0.25
  w_rel: 0.25
  w_hot: 0.2
  w_multi: 0.2
  w_fresh: 0.1
  threshold: 0.45
  max_age_hours: 72

notify:
  daily_push_cap: 15
  quiet_start_hour: 23
  quiet_end_hour: 8
  timezone: Asia/Seoul
  explore_judge_cap: 3
```

1단계에서 보정한 `dedupe` 값을 그대로 옮긴다.

- [ ] **Step 3: 기동 실패 확인**

옛 키가 남아 있을 때 실패하는지 본다.

Run: `uv run python -c "from app.config import Rules; Rules.model_validate({'include_keywords': ['x']})"`
Expected: `ValidationError … Extra inputs are not permitted`

- [ ] **Step 4: 커밋** (이 시점엔 `rules.py`·`scoring.py`·`llm.py` 가 옛 필드를 참조해 mypy 가 깨진다. Task 2.2~2.4 와 함께 한 커밋으로 묶어도 되지만, 커밋을 나눌 거면 여기서 `uv run pytest tests/test_dedupe.py` 만 확인하고 넘어간다.)

### Task 2.2: 규칙 필터를 exclude 전용으로

**Files:**
- Modify: `app/pipeline/rules.py`, `tests/test_rules.py`

**Interfaces:**
- Produces: `apply_rules(rules: Rules, *, title: str, body: str | None, url: str) -> RuleResult(passed, reason, matched_keywords)`

- [ ] **Step 1: 테스트 교체**

```python
# tests/test_rules.py
# 규칙 필터 테스트 — exclude 키워드·도메인만 판정한다. 통과는 기본값이다

from app.config import ExcludeConfig, Rules
from app.pipeline.rules import apply_rules

RULES = Rules(
    exclude=ExcludeConfig(keywords=["hiring", "giveaway"], domains=["spam.example", "medium.com/@bot"])
)


def call(**kwargs):
    base = {"title": "", "body": None, "url": "https://ok.example/a"}
    return apply_rules(RULES, **{**base, **kwargs})


def test_plain_item_passes():
    result = call(title="주말 잡담")
    assert result.passed and result.reason == "ok"


def test_exclude_keyword():
    result = call(title="Claude team is hiring")
    assert not result.passed and result.reason == "exclude_keyword"
    assert result.matched_keywords == ["hiring"]


def test_excluded_domain():
    assert call(title="Claude X", url="https://spam.example/a").reason == "exclude_domain"


def test_excluded_domain_path_prefix():
    assert call(title="x", url="https://medium.com/@bot/some-post").reason == "exclude_domain"


def test_word_boundary_avoids_substring_match():
    assert call(title="hiringham 마을 소식").passed


def test_body_is_searched():
    assert call(title="새 소식", body="본문에 giveaway 안내").reason == "exclude_keyword"
```

- [ ] **Step 2: 구현**

`apply_rules` 를 교체하고 `_matches_any` 와 `fnmatch` import 중 `_excluded_domain` 이 안 쓰는 것은 지운다 (`fnmatch` 는 `_excluded_domain` 이 쓴다).

```python
def apply_rules(rules: Rules, *, title: str, body: str | None, url: str) -> RuleResult:
    """제목 + 본문 앞부분으로 제외만 판정한다. 포함 판단은 LLM 선별이 한다."""
    text = f"{title}\n{(body or '')[:MATCH_BODY_CHARS]}"
    excluded = find_keywords(text, rules.exclude.keywords)
    if excluded:
        return RuleResult(False, "exclude_keyword", excluded)
    if _excluded_domain(url, rules.exclude.domains):
        return RuleResult(False, "exclude_domain")
    return RuleResult(True, "ok")
```

파일 첫 줄 주석을 `# 규칙 필터 — exclude 키워드·도메인 판정. 포함 판단은 LLM 선별이 맡는다` 로 바꾼다.

- [ ] **Step 3: 확인**

Run: `uv run pytest tests/test_rules.py -v`
Expected: 6 passed

### Task 2.3: 점수 관문 — `rel` 항과 `is_stale`

**Files:**
- Modify: `app/pipeline/scoring.py`, `tests/test_scoring.py`

**Interfaces:**
- Produces: `score_item(cfg, *, trust, relevance, metrics, mention_count, published_at, now=None) -> ScoreResult`, `is_stale(published_at, max_age_hours, now=None) -> bool`

- [ ] **Step 1: 테스트**

`test_scoring.py` 의 `keyword_hits=` 를 전부 `relevance=` 로 바꾸고 아래를 추가·수정한다.

```python
def test_trusted_fresh_relevant_item_passes():
    result = score_item(
        CFG, trust=1.0, relevance=1.0, metrics={}, mention_count=1,
        published_at=NOW - timedelta(hours=1), now=NOW,
    )
    assert result.passed
    assert result.score == CFG.w_src + CFG.w_rel + CFG.w_fresh


@pytest.mark.parametrize(
    ("trust", "needed"),
    [(1.0, 0.4), (0.9, 0.5), (0.8, 0.6), (0.7, 0.7), (0.6, 0.8), (0.5, 0.9)],
)
def test_relevance_needed_to_pass_alone_within_a_day(trust, needed):
    def score(rel):
        return score_item(
            CFG, trust=trust, relevance=rel, metrics={}, mention_count=1, published_at=NOW, now=NOW
        )

    assert score(needed).passed
    assert not score(needed - 0.05).passed


def test_stale_boundary():
    assert not is_stale(NOW - timedelta(hours=71), 72, now=NOW)
    assert is_stale(NOW - timedelta(hours=73), 72, now=NOW)
```

`import pytest` 와 `from app.pipeline.scoring import freshness, hotness, is_stale, score_item` 로 맞춘다.

- [ ] **Step 2: 구현**

```python
def is_stale(published_at: datetime, max_age_hours: int, *, now: datetime | None = None) -> bool:
    """전역 신선도 가드. 선별 호출 앞에 둬서 백필·오래된 항목이 예산을 쓰지 않게 한다."""
    return (now or datetime.now(UTC)) - published_at > timedelta(hours=max_age_hours)


def score_item(
    cfg: ScoringConfig,
    *,
    trust: float,
    relevance: float,
    metrics: dict[str, float],
    mention_count: int,
    published_at: datetime,
    now: datetime | None = None,
) -> ScoreResult:
    breakdown = {
        "src": cfg.w_src * trust,
        "rel": cfg.w_rel * relevance,
        "hot": cfg.w_hot * hotness(metrics),
        "multi": cfg.w_multi * min(max(mention_count - 1, 0), 2) / 2,
        "fresh": cfg.w_fresh * freshness(published_at, now=now),
    }
    total = sum(breakdown.values())
    # 부동소수 합이 0.4499999 로 떨어져 경계 케이스가 어긋나지 않게 소수 6자리에서 비교한다.
    return ScoreResult(total, round(total, 6) >= cfg.threshold, breakdown)
```

`from datetime import UTC, datetime, timedelta`. 첫 줄 주석은 `# 점수화 — trust·relevance·hotness·multi·freshness 가중합, 전역 신선도 가드`.

- [ ] **Step 3: 확인**

Run: `uv run pytest tests/test_scoring.py -v`
Expected: 전부 PASS (parametrize 6개 포함)

- [ ] **Step 4: 커밋 (2.1~2.3 묶음)**

```bash
git add app/config.py config/rules.yaml app/pipeline/rules.py app/pipeline/scoring.py tests/test_rules.py tests/test_scoring.py
git commit -m "feat: 정책 설정 재구조화 — exclude 전용 규칙, rel 점수 항, 전역 stale 가드, extra=forbid"
```

이 커밋 시점에 `app/pipeline/llm.py`·`app/jobs/pipeline.py` 는 옛 필드를 참조해 mypy 가 실패한다. Task 2.6 까지 연속으로 진행한 뒤 전체 검사를 한다.

### Task 2.4: 호출 예약 `reserve_call`

**Files:**
- Create: `app/db/budget.py`
- Test: `tests/test_budget.py` (순수 부분), `tests/integration/test_budget_db.py`

**Interfaces:**
- Produces: `async reserve_call(kind: Literal["triage","judge","explore"]) -> str | None` (batch_id 또는 None), `today_start(tz: str, now: datetime) -> datetime`, `BUDGETS`, `LOCK_KEY`

- [ ] **Step 1: 순수 테스트**

```python
# tests/test_budget.py
# 호출 예산 테스트 — 서울 달력일 시작 시각과 예산·kind 대응

from datetime import UTC, datetime

from app.db.budget import BUDGETS, budget_for, today_start


def test_today_start_is_seoul_midnight_in_utc():
    # KST 2026-09-06 00:30 = UTC 09-05 15:30. 그날의 시작은 UTC 09-05 15:00.
    now = datetime(2026, 9, 5, 15, 30, tzinfo=UTC)
    assert today_start("Asia/Seoul", now) == datetime(2026, 9, 5, 15, 0, tzinfo=UTC)


def test_before_seoul_midnight_belongs_to_previous_day():
    now = datetime(2026, 9, 5, 14, 30, tzinfo=UTC)  # KST 09-05 23:30
    assert today_start("Asia/Seoul", now) == datetime(2026, 9, 4, 15, 0, tzinfo=UTC)


def test_explore_shares_judge_budget():
    assert budget_for("explore") == ("judge", ("judge", "explore"))
    assert budget_for("triage") == ("triage", ("triage",))
    assert set(BUDGETS) == {"triage", "judge"}
```

- [ ] **Step 2: 실패 확인**

Run: `uv run pytest tests/test_budget.py -v`
Expected: FAIL, `ModuleNotFoundError: app.db.budget`

- [ ] **Step 3: 구현**

```python
# app/db/budget.py
# LLM 호출 예산 — 호출 직전에 독립 트랜잭션으로 예약 행을 커밋해 실패한 호출까지 상한에 센다

from __future__ import annotations

import uuid
from datetime import datetime
from typing import Literal
from zoneinfo import ZoneInfo

from sqlalchemy import func, select, text

from app.config import get_rules, get_settings
from app.db.models import LlmCall
from app.db.session import SessionLocal

Kind = Literal["triage", "judge", "explore"]
# 예산 이름 → 그 예산을 쓰는 kind. 탐색 판정은 판정 예산을 같이 쓴다.
BUDGETS: dict[str, tuple[str, ...]] = {"triage": ("triage",), "judge": ("judge", "explore")}
# 예산이 달라도 잠금은 하나. kind 별 키면 judge 와 explore 가 서로를 못 막아 301회가 난다.
LOCK_KEY = 74_110_003


def today_start(tz: str, now: datetime) -> datetime:
    local = now.astimezone(ZoneInfo(tz))
    return local.replace(hour=0, minute=0, second=0, microsecond=0)


def budget_for(kind: str) -> tuple[str, tuple[str, ...]]:
    for name, kinds in BUDGETS.items():
        if kind in kinds:
            return name, kinds
    raise ValueError(f"알 수 없는 kind {kind!r}")


def _caps() -> dict[str, int]:
    rules = get_rules()
    return {
        "triage": rules.triage.daily_cap_calls,
        "judge": get_settings().llm_daily_cap,
        "explore": rules.notify.explore_judge_cap,
    }


async def reserve_call(kind: Kind) -> str | None:
    """상한 미만이면 예약 행을 커밋하고 batch_id 를 돌려준다. 상한이면 None.

    배치 트랜잭션과 별개의 세션이다. 잠금 → 집계 → 삽입 → 커밋이 한 트랜잭션이라
    같은 프로세스의 두 잡이 교차해도 상한을 넘지 않는다. 시각은 한 번만 읽어
    "오늘" 범위와 called_at 에 같이 쓴다.
    """
    budget, kinds = budget_for(kind)
    caps = _caps()
    tz = get_rules().notify.timezone
    async with SessionLocal() as session, session.begin():
        await session.execute(text("SELECT pg_advisory_xact_lock(:k)"), {"k": LOCK_KEY})
        now = (await session.execute(select(func.now()))).scalar_one()
        start = today_start(tz, now)
        used = (
            await session.execute(
                select(func.count())
                .select_from(LlmCall)
                .where(LlmCall.kind.in_(kinds), LlmCall.called_at >= start)
            )
        ).scalar_one()
        if used >= caps[budget]:
            return None
        if kind != budget:
            own = (
                await session.execute(
                    select(func.count())
                    .select_from(LlmCall)
                    .where(LlmCall.kind == kind, LlmCall.called_at >= start)
                )
            ).scalar_one()
            if own >= caps[kind]:
                return None
        batch_id = str(uuid.uuid4())
        session.add(LlmCall(kind=kind, batch_id=batch_id, called_at=now))
        return batch_id
```

`now` 를 DB 의 `now()` 로 읽는 이유는 앱 서버 시계와 `called_at` 비교 기준을 한 곳으로 맞추기 위해서다.

- [ ] **Step 4: 통합 테스트**

```python
# tests/integration/__init__.py  (빈 파일)

# tests/integration/conftest.py
# 통합 테스트 공통 — TEST_DATABASE_URL 이 없으면 전부 건너뛴다. Neon dev 브랜치를 가리켜야 한다

import os

import pytest

if not os.environ.get("TEST_DATABASE_URL"):
    pytest.skip("TEST_DATABASE_URL 없음", allow_module_level=True)
```

```python
# tests/integration/test_budget_db.py
# 예약 원자성 테스트 — 299건 상태에서 judge·explore 를 동시에 예약하면 하나만 성공한다

import asyncio

import pytest
from sqlalchemy import delete, func, select, text

from app.db.budget import reserve_call
from app.db.models import LlmCall
from app.db.session import SessionLocal


@pytest.fixture(autouse=True)
async def clean_llm_calls():
    async with SessionLocal() as s, s.begin():
        await s.execute(delete(LlmCall))
    yield
    async with SessionLocal() as s, s.begin():
        await s.execute(delete(LlmCall))


@pytest.mark.asyncio
async def test_concurrent_reservations_respect_shared_cap(monkeypatch):
    monkeypatch.setattr("app.db.budget._caps", lambda: {"triage": 60, "judge": 300, "explore": 3})
    async with SessionLocal() as s, s.begin():
        now = (await s.execute(select(func.now()))).scalar_one()
        s.add_all(LlmCall(kind="judge", batch_id=f"seed-{i}", called_at=now) for i in range(299))

    results = await asyncio.gather(reserve_call("judge"), reserve_call("explore"))
    assert sum(r is not None for r in results) == 1

    async with SessionLocal() as s:
        total = (await s.execute(select(func.count()).select_from(LlmCall))).scalar_one()
    assert total == 300


@pytest.mark.asyncio
async def test_explore_own_cap(monkeypatch):
    monkeypatch.setattr("app.db.budget._caps", lambda: {"triage": 60, "judge": 300, "explore": 1})
    assert await reserve_call("explore") is not None
    assert await reserve_call("explore") is None
    assert await reserve_call("judge") is not None
```

루트 `tests/conftest.py` 의 `DATABASE_URL` 기본값을 `os.environ.get("TEST_DATABASE_URL", "postgresql://test:test@localhost:5432/test")` 로 바꿔 통합 테스트가 같은 엔진을 쓰게 한다. `pyproject.toml` 의 `[tool.pytest.ini_options]` 에 `asyncio_mode = "auto"` 가 없으면 추가한다.

- [ ] **Step 5: 확인**

Run: `uv run pytest tests/test_budget.py -v`
Expected: 3 passed

Run (Neon dev 브랜치 URL 로): `TEST_DATABASE_URL=<dev> uv run pytest tests/integration -v`
Expected: 2 passed. dev 브랜치가 없으면 Neon 콘솔에서 `main` 에서 분기해 만들고 `DATABASE_URL=<dev> uv run alembic upgrade head` 를 먼저 돌린다.

- [ ] **Step 6: 커밋**

```bash
git add app/db/budget.py tests/test_budget.py tests/integration tests/conftest.py pyproject.toml
git commit -m "feat: LLM 호출 예약 reserve_call — 자문 잠금으로 원자적 일일 상한"
```

### Task 2.5: LLM 선별 배치

**Files:**
- Create: `app/pipeline/triage.py`
- Modify: `app/pipeline/llm.py` (`client()`, `render_policy`, 판정 프롬프트), `app/schemas.py` (`Stage.TRIAGE`)
- Test: `tests/test_triage.py`

**Interfaces:**
- Produces:
  - `TriageEntry(idx: int, source: str, title: str, snippet: str, examples: str = "")`
  - `TriageItem(idx: int, relevance: float, reason: str)` (pydantic), `TriageBatch(items: list[TriageItem])`
  - `TriageBatchError(RuntimeError)`
  - `build_user_content(entries) -> str`
  - `parse_triage(batch: TriageBatch, expected: list[int]) -> tuple[dict[int, TriageItem], list[int]]` — (유효 결과, 항목 실패 idx). 개수 불일치·미지 idx 는 `TriageBatchError`
  - `async call_triage(rules, entries) -> TriageBatch`
  - `app.pipeline.llm.client() -> AsyncAnthropic`, `render_policy(policy: PolicyConfig) -> str`

- [ ] **Step 1: `Stage.TRIAGE`**

`app/schemas.py` 의 `Stage` 에 `TRIAGE = "triage"` 를 `RULE` 다음에 추가한다.

- [ ] **Step 2: 테스트**

```python
# tests/test_triage.py
# 선별 배치 테스트 — 사용자 블록 조립, 배치 실패와 항목 실패의 구분

import pytest

from app.pipeline.triage import (
    TriageBatch,
    TriageBatchError,
    TriageEntry,
    TriageItem,
    build_user_content,
    parse_triage,
)


def entries():
    return [
        TriageEntry(1, "rss:arxiv-cs-cl", "Over-Refusal and Subspaces", "abstract…"),
        TriageEntry(2, "youtube:jocoding", "클로드 가격 논란", "", examples='👎 "[영상] 가격 논란"'),
    ]


def test_user_content_numbers_items_and_appends_examples():
    text = build_user_content(entries())
    assert "[1] rss:arxiv-cs-cl | Over-Refusal and Subspaces\nabstract…" in text
    assert "[2] youtube:jocoding | 클로드 가격 논란" in text
    assert '유사 피드백: 👎 "[영상] 가격 논란"' in text


def test_parse_valid_batch():
    batch = TriageBatch(items=[TriageItem(idx=1, relevance=0.2, reason="r"), TriageItem(idx=2, relevance=0.7, reason="r")])
    ok, failed = parse_triage(batch, [1, 2])
    assert set(ok) == {1, 2} and failed == []


def test_count_mismatch_is_batch_error():
    batch = TriageBatch(items=[TriageItem(idx=1, relevance=0.2, reason="r")])
    with pytest.raises(TriageBatchError):
        parse_triage(batch, [1, 2])


def test_unknown_idx_is_batch_error():
    batch = TriageBatch(items=[TriageItem(idx=1, relevance=0.2, reason="r"), TriageItem(idx=9, relevance=0.2, reason="r")])
    with pytest.raises(TriageBatchError):
        parse_triage(batch, [1, 2])


def test_duplicate_idx_marks_missing_item_failed():
    batch = TriageBatch(items=[TriageItem(idx=1, relevance=0.2, reason="r"), TriageItem(idx=1, relevance=0.3, reason="r")])
    ok, failed = parse_triage(batch, [1, 2])
    assert set(ok) == {1} and failed == [2]


def test_out_of_range_relevance_is_item_failure():
    batch = TriageBatch(items=[TriageItem(idx=1, relevance=1.4, reason="r"), TriageItem(idx=2, relevance=0.3, reason="r")])
    ok, failed = parse_triage(batch, [1, 2])
    assert set(ok) == {2} and failed == [1]


def test_reason_is_truncated_to_200_chars():
    batch = TriageBatch(items=[TriageItem(idx=1, relevance=0.5, reason="x" * 500)])
    ok, _ = parse_triage(batch, [1])
    assert len(ok[1].reason) == 200
```

- [ ] **Step 3: 실패 확인**

Run: `uv run pytest tests/test_triage.py -v`
Expected: FAIL, `ModuleNotFoundError: app.pipeline.triage`

- [ ] **Step 4: `llm.py` 정리**

`_client` 를 `client` 로 바꾸고(호출부 갱신), `render_policy` 를 추가하고, 시스템 프롬프트가 정책을 읽게 한다.

```python
def client() -> AsyncAnthropic:
    return AsyncAnthropic(api_key=get_settings().anthropic_api_key)


def render_policy(policy: PolicyConfig) -> str:
    """선별·판정 프롬프트가 같은 문장을 읽는다."""
    parts = ["관심:", policy.interests.strip(), "관심 없음:", policy.not_interested.strip()]
    if policy.focus_repos:
        parts.append("특히 주목하는 저장소: " + ", ".join(policy.focus_repos))
    if policy.focus_stack:
        parts.append("특히 주목하는 스택·용어: " + ", ".join(policy.focus_stack))
    return "\n".join(parts)
```

`SYSTEM_PROMPT` 의 `사용자 관심 스택:\n{interests}` 를 `사용자 정책:\n{policy}` 로 바꾸고, `judge` 안의 `system=SYSTEM_PROMPT.format(interests=rules.interests)` 를 `system=SYSTEM_PROMPT.format(policy=render_policy(rules.policy))` 로 바꾼다. `from app.config import PolicyConfig, Rules, get_settings`.

- [ ] **Step 5: `triage.py` 구현**

```python
# app/pipeline/triage.py
# LLM 선별 배치 — 25건을 한 호출에 넣어 관련도 0~1 과 이유를 받는다. 정독은 판정이 한다

from __future__ import annotations

from dataclasses import dataclass

from pydantic import BaseModel

from app.config import Rules, get_settings
from app.pipeline.llm import client, render_policy

REASON_CHARS = 200

SYSTEM_PROMPT = """\
너는 개인용 개발 트렌드 알림기의 선별 단계다. 항목마다 "이 사용자가 읽을 후보인가" 를 0~1 로 매긴다.
정독·요약은 다음 단계가 하니 여기서는 관련도만 빠르게 판단한다.

사용자 정책:
{policy}

relevance 눈금
- 0.9 이상: 새 모델·릴리즈·기법·도구의 직접적인 변화. 사용자가 알아야 할 것.
- 0.6: 관심 주제의 일반적인 글. 읽으면 도움이 되지만 변화는 아님.
- 0.3: 주변부. 관심 주제와 스치는 정도.
- 0: 무관, 홍보, 구인, 스폰서, 잡담.
목록에 없는 새 이름·용어라도 문맥이 정책과 맞으면 높게 준다. 키워드가 아니라 뜻으로 판단한다.
"유사 피드백" 이 붙은 항목은 사용자가 비슷한 글에 남긴 👍/👎 이니 강하게 참고한다.
reason 은 20단어 이내 한국어 한 문장.
모든 항목에 대해 idx 를 그대로 돌려준다.
"""


@dataclass(slots=True, frozen=True)
class TriageEntry:
    idx: int
    source: str
    title: str
    snippet: str
    examples: str = ""  # 3단계에서 채운다. 비어 있으면 줄이 붙지 않는다


class TriageItem(BaseModel):
    idx: int
    relevance: float
    reason: str


class TriageBatch(BaseModel):
    items: list[TriageItem]


class TriageBatchError(RuntimeError):
    """응답 전체가 프로토콜에 맞지 않는다. 25건 공통이라 항목 탓이 아니다."""


def build_user_content(entries: list[TriageEntry]) -> str:
    blocks = []
    for e in entries:
        block = f"[{e.idx}] {e.source} | {e.title}"
        if e.snippet:
            block += f"\n{e.snippet}"
        if e.examples:
            block += f"\n    유사 피드백: {e.examples}"
        blocks.append(block)
    return "\n\n".join(blocks)


def parse_triage(
    batch: TriageBatch, expected: list[int]
) -> tuple[dict[int, TriageItem], list[int]]:
    """(유효 결과, 항목 실패 idx). 개수·미지 idx 는 배치 실패다."""
    if len(batch.items) != len(expected):
        raise TriageBatchError(f"응답 {len(batch.items)}건, 기대 {len(expected)}건")
    known = set(expected)
    if any(it.idx not in known for it in batch.items):
        raise TriageBatchError("기대하지 않은 idx 가 있음")

    ok: dict[int, TriageItem] = {}
    for it in batch.items:
        if it.idx in ok or not 0.0 <= it.relevance <= 1.0:
            continue
        ok[it.idx] = TriageItem(idx=it.idx, relevance=it.relevance, reason=it.reason[:REASON_CHARS])
    failed = [idx for idx in expected if idx not in ok]
    return ok, failed


async def call_triage(rules: Rules, entries: list[TriageEntry]) -> TriageBatch:
    """한 번의 호출. 기반 실패(네트워크·5xx·429·인증)는 SDK 예외로 올라온다."""
    response = await client().messages.parse(
        model=get_settings().llm_model,
        max_tokens=2048,
        system=[
            {
                "type": "text",
                "text": SYSTEM_PROMPT.format(policy=render_policy(rules.policy)),
                "cache_control": {"type": "ephemeral"},
            }
        ],
        messages=[{"role": "user", "content": build_user_content(entries)}],
        output_format=TriageBatch,
    )
    parsed = response.parsed_output
    if parsed is None:
        raise TriageBatchError("구조화 출력이 비어 있음")
    return parsed
```

`system` 블록 배열과 `cache_control` 형식은 Anthropic 문서(프롬프트 캐싱)로 대조한다.

- [ ] **Step 6: 확인**

Run: `uv run pytest tests/test_triage.py -v`
Expected: 7 passed

- [ ] **Step 7: 커밋**

```bash
git add app/pipeline/triage.py app/pipeline/llm.py app/schemas.py tests/test_triage.py
git commit -m "feat: LLM 선별 배치 — 정책 프롬프트, 구조화 출력, 배치 실패와 항목 실패 구분"
```

### Task 2.6: 파이프라인 잡 3국면 재구성

**Files:**
- Modify: `app/jobs/pipeline.py` (전면 교체)

**Interfaces:**
- Consumes: `find_candidates`, `classify`, `embed_pending`, `apply_rules`, `is_stale`, `score_item`, `call_triage`, `parse_triage`, `reserve_call`, `llm.judge`, `llm.enrich_body`
- Produces: `run_pipeline() -> int` (SCORED 수)

- [ ] **Step 1: 파일 전체 교체**

```python
# 파이프라인 잡 — NEW 배치를 1국면(항목 관문) → 2국면(선별 배치) → 3국면(점수·판정) 으로 통과시킨다

from __future__ import annotations

import uuid
from datetime import UTC, datetime

from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from app.config import Rules, get_rules
from app.db.budget import reserve_call
from app.db.models import Decision, Item, Source, Summary
from app.db.session import session_scope
from app.log import get_logger
from app.notify.discord import send_ops_alert
from app.pipeline import llm
from app.pipeline.dedupe import Verdict, classify, find_candidates
from app.pipeline.embedding import embed_pending
from app.pipeline.rules import apply_rules
from app.pipeline.scoring import is_stale, score_item
from app.pipeline.triage import (
    TriageBatchError,
    TriageEntry,
    TriageItem,
    call_triage,
    parse_triage,
)
from app.schemas import ItemStatus, Stage

BATCH_SIZE = 50
SNIPPET_CHARS = 300
TRIAGE_ERROR_LIMIT = 2  # 같은 항목의 항목 실패가 이만큼 쌓이면 폐기
INFRA_FAIL_LIMIT = 2  # 한 잡에서 기반·배치 실패가 연속 이만큼이면 운영 알림 후 종료
log = get_logger(__name__)


def _record(item: Item, stage: Stage, passed: bool, details: dict[str, object]) -> Decision:
    return Decision(
        item_id=item.id, stage=stage.value, passed=passed, score=item.score, details=details
    )


async def _claim_batch(session: AsyncSession) -> list[tuple[Item, Source]]:
    """최신순으로 집어 잠근다. 잠금은 세 국면과 외부 호출 동안 유지된다 (단일 프로세스 전제)."""
    stmt = (
        select(Item, Source)
        .join(Source, Source.id == Item.source_id)
        .where(Item.status == ItemStatus.NEW.value)
        .order_by(Item.published_at.desc())
        .limit(BATCH_SIZE)
        .with_for_update(of=Item, skip_locked=True)
    )
    return [(item, source) for item, source in (await session.execute(stmt)).all()]


# ---------- 1국면 ----------


async def _gate(session: AsyncSession, rules: Rules, item: Item, source: Source) -> Verdict | None:
    """stale → 중복 → exclude. 살아남으면 중복 판정 결과를, 탈락하면 None 을 돌려준다."""
    if is_stale(item.published_at, rules.scoring.max_age_hours):
        item.status = ItemStatus.DROPPED.value
        age = (datetime.now(UTC) - item.published_at).total_seconds() / 3600
        session.add(_record(item, Stage.SCORE, False, {"reason": "stale", "age_hours": round(age)}))
        return None

    dup, related = await find_candidates(session, item.id, rules.dedupe)
    embedded = await session.scalar(select(Item.embedding.is_not(None)).where(Item.id == item.id))
    if not embedded:
        session.add(_record(item, Stage.RULE, True, {"reason": "dedupe_skipped"}))
        verdict = Verdict("independent", item.id, 1)
    else:
        verdict = classify(
            item_id=item.id,
            own_source_id=item.source_id,
            own_title=item.title,
            dup=dup,
            related=related,
            cfg=rules.dedupe,
        )
    item.cluster_id = verdict.cluster_id
    if verdict.kind == "dup":
        item.status = ItemStatus.FILTERED_OUT.value
        session.add(
            _record(item, Stage.RULE, False, {"reason": "dup", "cluster_id": verdict.cluster_id})
        )
        return None

    rule = apply_rules(rules, title=item.title, body=item.summary_raw, url=item.url)
    session.add(
        _record(item, Stage.RULE, rule.passed, {"reason": rule.reason, "matched": rule.matched_keywords})
    )
    if not rule.passed:
        item.status = ItemStatus.FILTERED_OUT.value
        return None
    return verdict


# ---------- 2국면 ----------


async def _existing_relevance(session: AsyncSession, item_ids: list[int]) -> dict[int, TriageItem]:
    """판정 예산이 바닥나 NEW 로 남았던 항목은 이미 선별 결과가 있다. 다시 호출하지 않는다."""
    if not item_ids:
        return {}
    stmt = (
        select(Decision.item_id, Decision.details)
        .where(
            Decision.item_id.in_(item_ids),
            Decision.stage == Stage.TRIAGE.value,
            Decision.passed.is_(True),
        )
        .order_by(Decision.created_at.desc())
    )
    found: dict[int, TriageItem] = {}
    for item_id, details in (await session.execute(stmt)).all():
        if item_id not in found and "relevance" in details:
            found[item_id] = TriageItem(
                idx=item_id, relevance=float(details["relevance"]), reason=str(details["reason"])
            )
    return found


async def _triage_error_count(session: AsyncSession, item_id: int) -> int:
    stmt = select(Decision).where(
        Decision.item_id == item_id,
        Decision.stage == Stage.TRIAGE.value,
        Decision.passed.is_(False),
    )
    return len((await session.execute(stmt)).scalars().all())


async def _triage(
    session: AsyncSession, rules: Rules, survivors: list[tuple[Item, Source]]
) -> dict[int, TriageItem]:
    """배치별 선별. 돌려주는 값은 item_id → 결과. 없는 항목은 NEW 로 남는다."""
    results = await _existing_relevance(session, [item.id for item, _ in survivors])
    pending = [(i, s) for i, s in survivors if i.id not in results]
    infra_failures = 0
    size = rules.triage.batch_size

    for start in range(0, len(pending), size):
        chunk = pending[start : start + size]
        batch_id = await reserve_call("triage")
        if batch_id is None:
            log.info("pipeline.triage_cap_reached")
            break

        entries = [
            TriageEntry(item.id, source.name, item.title, (item.summary_raw or "")[:SNIPPET_CHARS])
            for item, source in chunk
        ]
        expected = [item.id for item, _ in chunk]
        try:
            try:
                batch = await call_triage(rules, entries)
                ok, failed = parse_triage(batch, expected)
            except TriageBatchError as exc:
                log.warning("pipeline.triage_batch_retry", error=str(exc))
                batch = await call_triage(rules, entries)
                ok, failed = parse_triage(batch, expected)
        except Exception as exc:
            # 기반 실패든 두 번째 배치 실패든 항목 탓이 아니다. 결정 행 없이 NEW 로 둔다.
            infra_failures += 1
            log.warning("pipeline.triage_failed", error=str(exc), consecutive=infra_failures)
            if infra_failures >= INFRA_FAIL_LIMIT:
                try:
                    await send_ops_alert(f"선별 호출이 연속 {infra_failures}배치 실패: {exc}")
                except Exception as alert_exc:
                    log.warning("pipeline.alert_failed", error=str(alert_exc))
                break
            continue
        infra_failures = 0

        for item, _ in chunk:
            if item.id in ok:
                res = ok[item.id]
                results[item.id] = res
                session.add(
                    _record(
                        item,
                        Stage.TRIAGE,
                        True,
                        {"relevance": res.relevance, "reason": res.reason, "batch_id": batch_id},
                    )
                )
            else:
                session.add(_record(item, Stage.TRIAGE, False, {"reason": "triage_error"}))
                if await _triage_error_count(session, item.id) + 1 >= TRIAGE_ERROR_LIMIT:
                    item.status = ItemStatus.DROPPED.value
        await session.flush()
    return results


# ---------- 3국면 ----------


async def _judge(
    session: AsyncSession, rules: Rules, item: Item, source: Source, verdict: Verdict, tri: TriageItem
) -> bool:
    """점수 → 보강 → 판정. 돌려주는 값은 판정 호출 여부. 예산이 없으면 False 이고 항목은 NEW 로 남는다."""
    trust = source.trust_adjusted if source.trust_adjusted is not None else source.trust_score
    metrics = item.raw.get("metrics", {}) if isinstance(item.raw, dict) else {}
    score = score_item(
        rules.scoring,
        trust=trust,
        relevance=tri.relevance,
        metrics=metrics if isinstance(metrics, dict) else {},
        mention_count=verdict.mention_count,
        published_at=item.published_at,
    )
    item.score = score.score
    session.add(
        _record(
            item,
            Stage.SCORE,
            score.passed,
            {"breakdown": score.breakdown, "triage_reason": tri.reason},
        )
    )
    if not score.passed:
        item.status = ItemStatus.DROPPED.value
        return False

    body = item.summary_raw
    enrich_failed = False
    if not body or len(body) < llm.ENRICH_MIN_CHARS:
        enriched = await llm.enrich_body(item.url)
        if enriched:
            body = enriched
            item.summary_raw = enriched[:3000]
        else:
            enrich_failed = True

    if await reserve_call("judge") is None:
        log.info("pipeline.judge_cap_reached", item_id=item.id)
        return False

    result = await llm.judge(rules, source=source.name, title=item.title, body=body)
    session.add(
        _record(
            item,
            Stage.LLM,
            result.verdict.worth_notifying,
            {
                "enrich_failed": enrich_failed,
                "importance": result.verdict.importance,
                "tags": result.verdict.tags,
            },
        )
    )
    session.add(
        Summary(
            item_id=item.id,
            title_ko=result.verdict.title_ko,
            summary_ko=result.verdict.summary_ko,
            tags=result.verdict.tags,
            importance=result.verdict.importance,
            worth_notifying=result.verdict.worth_notifying,
            model=result.model,
            tokens_in=result.tokens_in,
            tokens_out=result.tokens_out,
        )
    )
    item.category = result.verdict.category.value
    item.status = (
        ItemStatus.SCORED.value if result.verdict.worth_notifying else ItemStatus.DROPPED.value
    )
    return True


# ---------- 잡 ----------


async def run_pipeline() -> int:
    """한 배치를 처리하고 SCORED 수를 돌려준다."""
    rules = get_rules()
    passed = 0
    async with session_scope() as session:
        batch = await _claim_batch(session)
        if not batch:
            return 0
        await embed_pending(session, [item.id for item, _ in batch])

        survivors: list[tuple[Item, Source, Verdict]] = []
        for item, source in batch:
            item_id = item.id
            savepoint = await session.begin_nested()
            try:
                verdict = await _gate(session, rules, item, source)
            except Exception as exc:
                await savepoint.rollback()
                log.warning("pipeline.gate_failed", item_id=item_id, error=str(exc))
                continue
            await savepoint.commit()
            if verdict is not None:
                survivors.append((item, source, verdict))

        triaged = await _triage(session, rules, [(i, s) for i, s, _ in survivors])

        for item, source, verdict in survivors:
            tri = triaged.get(item.id)
            if tri is None or item.status != ItemStatus.NEW.value:
                continue
            item_id = item.id
            savepoint = await session.begin_nested()
            try:
                await _judge(session, rules, item, source, verdict, tri)
            except Exception as exc:
                await savepoint.rollback()
                log.warning("pipeline.judge_failed", item_id=item_id, error=str(exc))
                continue
            await savepoint.commit()
            if item.status == ItemStatus.SCORED.value:
                passed += 1

    log.info("pipeline.done", scored=passed)
    return passed
```

`uuid` import 가 안 쓰이면 뺀다. `TriageEntry.idx` 에 `item.id` 를 그대로 쓰므로 프롬프트의 `[idx]` 가 항목 id 다. 응답에 낯선 숫자가 오면 배치 실패로 잡힌다.

- [ ] **Step 2: 옛 코드 삭제 확인**

Run: `git grep -n "always_pass\|include_keywords\|include_repos\|whitelist\|w_kw\|keyword_hits\|mention_count(" app/ config/ tests/`
Expected: 결과 없음. 남으면 지운다.

- [ ] **Step 3: 전체 검사**

Run: `uv run ruff check . && uv run ruff format . && uv run mypy app && uv run pytest -q`
Expected: 전부 통과

- [ ] **Step 4: 실행 확인**

Run: `PYTHONIOENCODING=utf-8 uv run python scripts/run_job.py pipeline`
Expected: 로그에 `pipeline.done`. 남아 있던 NEW 4,000건은 `stale` 로 정리되어 선별 호출이 거의 없다. 이어서 새로 수집한 배치를 한 번 더 돌려 `decisions` 의 `triage` 행 25건의 `reason` 을 읽는다. 정책 문장을 손볼 근거다.

- [ ] **Step 5: 커밋**

```bash
git add app/jobs/pipeline.py
git commit -m "feat: 파이프라인 3국면 — 관문·선별 배치·점수/판정, 화이트리스트·키워드 관문 삭제"
```

### Task 2.7: 2단계 마무리

- [ ] `CLAUDE.md` 의 파이프라인 도식·`rules.yaml` 설명·시크릿 목록(임베딩 키)을 갱신한다. `.env.example` 은 Task 1.3 에서 이미 갱신했다.
- [ ] Codex 리뷰: `codex exec --sandbox read-only "커밋 범위 <2.1 첫 커밋>..HEAD 리뷰. 관점: 3국면 사이 세이브포인트·잠금 경계, 선별 실패 3층위가 코드에 그대로인지, reserve_call 이 모든 LLM 호출 앞에 있는지, 삭제된 설정 키 잔재" </dev/null`
- [ ] 지적 반영 → 재커밋 → 재리뷰.
- [ ] 옵시디언: 기능 노트 `검증 파이프라인 v2` 갱신, `결정 기록`(정책 관문 결정·기각 대안), `런타임 플로우`(3국면), `파일·함수 레퍼런스`, `폴더·파일 역할 지도`, 일일 노트 `기술파악 - LLM 선별 배치.md`.

---

# 3단계 — 피드백 사례 주입과 탐색 슬롯

끝나면: 👍/👎 가 다음 선별·판정 프롬프트에 사례로 들어가고, 하루 1건 🧪 카드가 온다.

### Task 3.1: 최근접 피드백 사례

**Files:**
- Create: `app/pipeline/feedback.py`
- Test: `tests/test_feedback_examples.py`, `tests/integration/test_feedback_db.py`

**Interfaces:**
- Produces: `FeedbackExample(verdict: str, title_ko: str)`, `format_examples(examples) -> str`, `async nearest_feedback(session, item_id, *, k, min_sim=0.75) -> list[FeedbackExample]`

- [ ] **Step 1: 순수 테스트**

```python
# tests/test_feedback_examples.py
# 피드백 사례 서식 테스트 — 👍/👎 와 제목을 한 줄로

from app.pipeline.feedback import FeedbackExample, format_examples


def test_format_examples():
    text = format_examples(
        [FeedbackExample("useful", "[릴리즈] uv 0.12.9"), FeedbackExample("useless", "[영상] 잡담")]
    )
    assert text == '👍 "[릴리즈] uv 0.12.9" · 👎 "[영상] 잡담"'


def test_empty_is_empty_string():
    assert format_examples([]) == ""
```

- [ ] **Step 2: 구현**

```python
# app/pipeline/feedback.py
# 최근접 피드백 사례 — 항목과 임베딩이 가까운 👍/👎 항목을 찾아 프롬프트에 붙일 한 줄을 만든다

from __future__ import annotations

from dataclasses import dataclass

from sqlalchemy import text
from sqlalchemy.ext.asyncio import AsyncSession

MARK = {"useful": "👍", "useless": "👎"}
_SQL = text(
    """
    WITH q AS (SELECT embedding FROM items WHERE id = :item_id)
    SELECT f.verdict, s.title_ko
    FROM feedback f
    JOIN items i ON i.id = f.item_id
    JOIN summaries s ON s.item_id = f.item_id, q
    WHERE f.item_id <> :item_id
      AND i.embedding IS NOT NULL
      AND 1 - (i.embedding <=> q.embedding) >= :min_sim
    ORDER BY i.embedding <=> q.embedding
    LIMIT :k
    """
)


@dataclass(slots=True, frozen=True)
class FeedbackExample:
    verdict: str
    title_ko: str


def format_examples(examples: list[FeedbackExample]) -> str:
    return " · ".join(f'{MARK[e.verdict]} "{e.title_ko}"' for e in examples)


async def nearest_feedback(
    session: AsyncSession, item_id: int, *, k: int, min_sim: float = 0.75
) -> list[FeedbackExample]:
    rows = (await session.execute(_SQL, {"item_id": item_id, "min_sim": min_sim, "k": k})).all()
    return [FeedbackExample(r.verdict, r.title_ko) for r in rows]
```

피드백은 항목당 1건이라 조인이 곧 최신 판정이다.

- [ ] **Step 3: 통합 테스트**

```python
# tests/integration/test_feedback_db.py
# 최근접 피드백 통합 테스트 — 같은 벡터의 항목에 남긴 피드백이 사례로 나온다

import pytest
from sqlalchemy import delete, text

from app.db.models import Feedback, Item, Source, Summary
from app.db.session import SessionLocal
from app.pipeline.feedback import nearest_feedback

VEC = "[" + ",".join(["0.03"] * 1024) + "]"


@pytest.mark.asyncio
async def test_nearest_feedback_finds_identical_vector():
    async with SessionLocal() as s, s.begin():
        src = Source(name="test:fb", type="rss", config={})
        s.add(src)
        await s.flush()
        a = Item(source_id=src.id, external_id="a", url="https://t/a", url_normalized="https://t/a",
                 url_hash="fb-a", title="A", published_at=text("now()"))
        b = Item(source_id=src.id, external_id="b", url="https://t/b", url_normalized="https://t/b",
                 url_hash="fb-b", title="B", published_at=text("now()"))
        s.add_all([a, b])
        await s.flush()
        await s.execute(
            text("UPDATE items SET embedding = CAST(:v AS vector) WHERE id IN (:a, :b)"),
            {"v": VEC, "a": a.id, "b": b.id},
        )
        s.add(Summary(item_id=b.id, title_ko="[릴리즈] B", summary_ko="", importance=3,
                      worth_notifying=True, model="m"))
        s.add(Feedback(item_id=b.id, verdict="useful"))
        ids = (a.id, b.id, src.id)

    try:
        async with SessionLocal() as s:
            examples = await nearest_feedback(s, ids[0], k=3)
        assert [(e.verdict, e.title_ko) for e in examples] == [("useful", "[릴리즈] B")]
    finally:
        async with SessionLocal() as s, s.begin():
            await s.execute(delete(Item).where(Item.id.in_(ids[:2])))
            await s.execute(delete(Source).where(Source.id == ids[2]))
```

`published_at=text("now()")` 가 ORM 에서 안 먹으면 `datetime.now(UTC)` 로 바꾼다. `CAST(:v AS vector)` 는 `:v` 가 text 파라미터로 추론되지 않을 수 있어 실패하면 `CAST(CAST(:v AS text) AS vector)` 로 쓴다.

- [ ] **Step 4: 확인·커밋**

Run: `uv run pytest tests/test_feedback_examples.py -v` → 2 passed. `TEST_DATABASE_URL=<dev> uv run pytest tests/integration -v` → 통과.

```bash
git add app/pipeline/feedback.py tests/test_feedback_examples.py tests/integration/test_feedback_db.py
git commit -m "feat: 최근접 피드백 사례 조회"
```

### Task 3.2: 선별·판정 프롬프트에 사례 주입

**Files:**
- Modify: `app/jobs/pipeline.py` (`_triage` 의 entries 조립, `_judge` 의 judge 호출), `app/pipeline/llm.py` (`judge` 시그니처·`USER_TEMPLATE`)

**Interfaces:**
- Produces: `llm.judge(rules, *, source, title, body, examples: str = "") -> LLMResult`

- [ ] **Step 1: `llm.py`**

```python
USER_TEMPLATE = """\
소스: {source}
제목: {title}
{examples}본문:
{body}
"""
```

`judge` 에 `examples: str = ""` 인자를 추가하고 `USER_TEMPLATE.format(..., examples=f"유사 피드백: {examples}\n" if examples else "")` 로 채운다. `SYSTEM_PROMPT` 판단 기준 끝에 한 줄 추가한다. `- "유사 피드백" 은 사용자가 비슷한 글에 남긴 👍/👎 다. importance 와 worth_notifying 에 강하게 반영한다.`

- [ ] **Step 2: `pipeline.py`**

`_triage` 의 entries 조립을 바꾼다.

```python
        entries = []
        for item, source in chunk:
            examples = await nearest_feedback(session, item.id, k=2)
            entries.append(
                TriageEntry(
                    item.id,
                    source.name,
                    item.title,
                    (item.summary_raw or "")[:SNIPPET_CHARS],
                    format_examples(examples),
                )
            )
```

`_judge` 의 판정 호출을 바꾼다.

```python
    examples = format_examples(await nearest_feedback(session, item.id, k=3))
    result = await llm.judge(
        rules, source=source.name, title=item.title, body=body, examples=examples
    )
```

import: `from app.pipeline.feedback import format_examples, nearest_feedback`.

- [ ] **Step 3: 검사·커밋**

Run: `uv run ruff check . && uv run mypy app && uv run pytest -q`

```bash
git add app/jobs/pipeline.py app/pipeline/llm.py
git commit -m "feat: 선별·판정 프롬프트에 최근접 피드백 사례 주입"
```

### Task 3.3: 탐색 슬롯

**Files:**
- Modify: `app/schemas.py` (`Level.EXPLORE`), `app/notify/discord.py` (`build_payload`), `app/jobs/notify.py` (`_explore`, `run_notify` 끝)
- Test: `tests/test_discord.py` (머리말), `tests/test_policy.py` (explore 는 push 집계 제외 — 이미 `level == PUSH` 만 세므로 확인만)

**Interfaces:**
- Produces: `Level.EXPLORE = "explore"`, `EXPLORE_PREFIX = "🧪 실험 · 경계 항목"`, `async _explore(session, rules, now) -> bool`

- [ ] **Step 1: 테스트**

`tests/test_discord.py` 에 추가한다 (기존 `build_payload` 테스트의 픽스처 이름을 그대로 쓴다).

```python
def test_explore_payload_has_prefix_and_is_silent(item, summary):
    payload = build_payload(item, summary, Level.EXPLORE, "src")
    assert payload["content"].startswith("🧪 실험 · 경계 항목\n\n")
    assert payload["flags"] == FLAG_SUPPRESS_NOTIFICATIONS
```

- [ ] **Step 2: `Level`·디스코드**

`Level` 에 `EXPLORE = "explore"` 추가. `discord.py` 에 `EXPLORE_PREFIX = "🧪 실험 · 경계 항목"` 을 두고 `build_payload` 의 `content` 를 바꾼다.

```python
    content = render(item, summary, source_name)
    if level is Level.EXPLORE:
        content = f"{EXPLORE_PREFIX}\n\n{content}"[:MAX_CONTENT]
    payload: dict[str, Any] = {"content": content, ...}
```

- [ ] **Step 3: `_explore`**

`app/jobs/notify.py` 에 추가하고 `run_notify` 의 `log.info("notify.done", …)` 앞에서 호출한다.

```python
EXPLORE_BAND = 0.10


async def _explore_sent_today(session: AsyncSession, cfg: NotifyConfig, now: datetime) -> bool:
    start = today_start(cfg.timezone, now)
    stmt = select(func.count()).select_from(Notification).where(
        Notification.level == Level.EXPLORE.value,
        Notification.error.is_(None),
        Notification.sent_at >= start,
    )
    return (await session.execute(stmt)).scalar_one() > 0


async def _explore_candidate(session: AsyncSession, rules: Rules, now: datetime):
    """점수 관문 바로 아래로 떨어진 최근 24시간 항목 중 최고점. Summary 가 없어야 한다."""
    thr = rules.scoring.threshold
    last_score = (
        select(Decision.passed)
        .where(Decision.item_id == Item.id, Decision.stage == Stage.SCORE.value)
        .order_by(Decision.created_at.desc())
        .limit(1)
        .scalar_subquery()
    )
    stmt = (
        select(Item, Source)
        .join(Source, Source.id == Item.source_id)
        .outerjoin(Summary, Summary.item_id == Item.id)
        .where(
            Item.status == ItemStatus.DROPPED.value,
            Summary.item_id.is_(None),
            Item.score >= thr - EXPLORE_BAND,
            Item.score < thr,
            Item.published_at >= now - timedelta(hours=24),
            last_score.is_(False),
        )
        .order_by(Item.score.desc())
        .limit(1)
        .with_for_update(of=Item, skip_locked=True)
    )
    return (await session.execute(stmt)).first()


async def _explore(session: AsyncSession, rules: Rules, notifier: Notifier) -> bool:
    """하루 1건 경계 항목 실험. 판정 → 통과면 🧪 발송 → SENT. false 면 그날은 보내지 않는다."""
    cfg = rules.notify
    now = datetime.now(UTC)
    if in_quiet_hours(cfg, now) or await _explore_sent_today(session, cfg, now):
        return False
    row = await _explore_candidate(session, rules, now)
    if row is None:
        return False
    item, source = row[0], row[1]

    if await reserve_call("explore") is None:
        return False
    examples = format_examples(await nearest_feedback(session, item.id, k=3))
    result = await llm.judge(
        rules, source=source.name, title=item.title, body=item.summary_raw, examples=examples
    )
    session.add(
        Decision(
            item_id=item.id, stage=Stage.LLM.value, passed=result.verdict.worth_notifying,
            score=item.score,
            details={"explore": True, "importance": result.verdict.importance, "tags": result.verdict.tags},
        )
    )
    summary = Summary(
        item_id=item.id, title_ko=result.verdict.title_ko, summary_ko=result.verdict.summary_ko,
        tags=result.verdict.tags, importance=result.verdict.importance,
        worth_notifying=result.verdict.worth_notifying, model=result.model,
        tokens_in=result.tokens_in, tokens_out=result.tokens_out,
    )
    session.add(summary)
    if not result.verdict.worth_notifying:
        await session.commit()
        return False

    message_id = await notifier.send(item, summary, Level.EXPLORE, source.name)
    session.add(
        Notification(item_id=item.id, channel=notifier.channel, level=Level.EXPLORE.value, message_id=message_id)
    )
    item.status = ItemStatus.SENT.value
    await session.commit()
    log.info("notify.explore_sent", item_id=item.id)
    return True
```

`run_notify` 끝, `log.info("notify.done", …)` 직전 (세션 블록 안):

```python
        try:
            await _explore(session, get_rules(), notifier)
        except Exception as exc:
            await session.rollback()
            log.warning("notify.explore_failed", error=str(exc))
```

import: `from datetime import UTC, datetime, timedelta`, `from sqlalchemy import func, select`, `from app.config import NotifyConfig, Rules, get_rules`, `from app.db.budget import reserve_call, today_start`, `from app.db.models import Decision, Item, Notification, Source, Summary`, `from app.notify.policy import decide, in_quiet_hours`, `from app.pipeline import llm`, `from app.pipeline.feedback import format_examples, nearest_feedback`, `from app.schemas import ItemStatus, Level, Stage`.

- [ ] **Step 4: 확인·커밋**

Run: `uv run ruff check . && uv run mypy app && uv run pytest -q` → 통과
Run: `PYTHONIOENCODING=utf-8 uv run python scripts/run_job.py notify` → 낮 시간에 경계 항목이 있으면 `notify.explore_sent`

```bash
git add app/schemas.py app/notify/discord.py app/jobs/notify.py tests/test_discord.py
git commit -m "feat: 탐색 슬롯 — 하루 1건 경계 항목을 판정해 🧪 카드로 발송"
```

### Task 3.4: 3단계 마무리

- [ ] Codex 리뷰: `codex exec --sandbox read-only "커밋 범위 <3.1 첫 커밋>..HEAD 리뷰. 관점: _explore 의 커밋 경계와 run_notify 세션 상태, 후보 쿼리의 상관 서브쿼리 정확성, 사례 주입이 캐시 블록을 건드리지 않는지" </dev/null`
- [ ] 지적 반영 → 재커밋 → 재리뷰.
- [ ] 옵시디언: 기능 노트·런타임 플로우 갱신, 일일 노트 `기술파악 - 피드백 사례·탐색 슬롯.md`.

---

# 4단계 — 신뢰도 보정과 주간 리포트

끝나면: 주간 표로 임계값·신뢰도를 데이터로 조정한다.

### Task 4.1: 신뢰도 보정 함수

**Files:**
- Create: `app/pipeline/trust.py`
- Test: `tests/test_trust.py`

**Interfaces:**
- Produces: `adjust_trust(base: float, useful: int, useless: int, *, prior_weight: int = 10) -> float`

- [ ] **Step 1: 테스트**

```python
# tests/test_trust.py
# 신뢰도 보정 테스트 — 라벨 0건이면 원값, 10건이면 반반, 범위 클램프

from app.pipeline.trust import adjust_trust


def test_no_labels_keeps_base():
    assert adjust_trust(0.7, 0, 0) == 0.7


def test_ten_labels_split_evenly_with_prior():
    # (10*0.5 + 10) / (10 + 10) = 0.75
    assert adjust_trust(0.5, 10, 0) == 0.75


def test_clamped_to_range():
    assert adjust_trust(0.2, 0, 50) == 0.2
    assert adjust_trust(1.0, 50, 0) == 1.0
```

- [ ] **Step 2: 구현**

```python
# app/pipeline/trust.py
# 소스 신뢰도 보정 — 최근 피드백을 베이즈 평활로 반영한다. 사전 가중치 10 = 라벨 10건이면 반반

from __future__ import annotations


def adjust_trust(base: float, useful: int, useless: int, *, prior_weight: int = 10) -> float:
    value = (prior_weight * base + useful) / (prior_weight + useful + useless)
    return round(min(1.0, max(0.2, value)), 4)
```

- [ ] **Step 3: 확인·커밋**

Run: `uv run pytest tests/test_trust.py -v` → 3 passed

```bash
git add app/pipeline/trust.py tests/test_trust.py
git commit -m "feat: 소스 신뢰도 베이즈 보정 함수"
```

### Task 4.2: 주간 리포트 스크립트

**Files:**
- Create: `scripts/weekly_report.py`

**Interfaces:**
- Consumes: `adjust_trust`, `Source.trust_adjusted`

- [ ] **Step 1: 스크립트**

```python
# scripts/weekly_report.py
# 주간 튜닝 리포트 — 관문 깔때기·소스별 정밀도·강도·점수 구간·선별 보정·탈락 사유. --apply 면 신뢰도 보정을 쓴다

from __future__ import annotations

import argparse
import asyncio
from collections.abc import Sequence
from typing import Any

from sqlalchemy import text

from app.db.session import engine, session_scope
from app.pipeline.trust import adjust_trust

DAYS = 7
TRUST_DAYS = 30

FUNNEL = text(
    """
    SELECT d.stage, d.passed, d.details->>'reason' AS reason, count(*) AS n
    FROM decisions d
    WHERE d.created_at >= now() - make_interval(days => :days)
    GROUP BY 1, 2, 3 ORDER BY 1, 2, 4 DESC
    """
)
BY_SOURCE = text(
    """
    SELECT s.name, s.trust_score, s.trust_adjusted,
           count(DISTINCT n.item_id) AS sent,
           count(*) FILTER (WHERE f.verdict = 'useful') AS useful,
           count(*) FILTER (WHERE f.verdict = 'useless') AS useless
    FROM sources s
    LEFT JOIN items i ON i.source_id = s.id
    LEFT JOIN notifications n ON n.item_id = i.id AND n.error IS NULL
         AND n.sent_at >= now() - make_interval(days => :days)
    LEFT JOIN feedback f ON f.item_id = n.item_id
    GROUP BY 1, 2, 3 ORDER BY sent DESC
    """
)
BY_IMPORTANCE = text(
    """
    SELECT sm.importance,
           count(*) FILTER (WHERE f.verdict = 'useful') AS useful,
           count(*) FILTER (WHERE f.verdict = 'useless') AS useless
    FROM notifications n
    JOIN summaries sm ON sm.item_id = n.item_id
    LEFT JOIN feedback f ON f.item_id = n.item_id
    WHERE n.error IS NULL AND n.sent_at >= now() - make_interval(days => :days)
    GROUP BY 1 ORDER BY 1 DESC
    """
)
BY_SCORE_BAND = text(
    """
    SELECT floor(i.score / 0.05) * 0.05 AS band,
           count(*) FILTER (WHERE f.verdict = 'useful') AS useful,
           count(*) FILTER (WHERE f.verdict = 'useless') AS useless,
           count(*) AS sent
    FROM notifications n
    JOIN items i ON i.id = n.item_id
    LEFT JOIN feedback f ON f.item_id = n.item_id
    WHERE n.error IS NULL AND n.sent_at >= now() - make_interval(days => :days)
    GROUP BY 1 ORDER BY 1
    """
)
TRIAGE_VS_JUDGE = text(
    """
    SELECT floor((t.details->>'relevance')::float / 0.2) * 0.2 AS band,
           count(*) AS triaged,
           count(*) FILTER (WHERE j.passed) AS judged_true,
           count(*) FILTER (WHERE j.id IS NOT NULL AND NOT j.passed) AS judged_false
    FROM decisions t
    LEFT JOIN decisions j ON j.item_id = t.item_id AND j.stage = 'llm'
    WHERE t.stage = 'triage' AND t.passed
      AND t.created_at >= now() - make_interval(days => :days)
    GROUP BY 1 ORDER BY 1
    """
)
DROP_REASONS = text(
    """
    SELECT d.details->>'reason' AS reason, count(*) AS n
    FROM decisions d
    WHERE d.stage = 'triage' AND d.passed AND (d.details->>'relevance')::float < 0.3
      AND d.created_at >= now() - make_interval(days => :days)
    GROUP BY 1 ORDER BY 2 DESC LIMIT 10
    """
)
TRUST_LABELS = text(
    """
    SELECT s.id, s.name, s.trust_score,
           count(*) FILTER (WHERE f.verdict = 'useful') AS useful,
           count(*) FILTER (WHERE f.verdict = 'useless') AS useless
    FROM sources s
    LEFT JOIN items i ON i.source_id = s.id
    LEFT JOIN feedback f ON f.item_id = i.id
         AND f.created_at >= now() - make_interval(days => :days)
    WHERE s.enabled
    GROUP BY 1, 2, 3 ORDER BY 2
    """
)


def table(title: str, rows: Sequence[Any]) -> None:
    print(f"\n## {title}")
    if not rows:
        print("(없음)")
        return
    keys = list(rows[0]._mapping.keys())
    print(" | ".join(keys))
    for r in rows:
        print(" | ".join(str(v) for v in r._mapping.values()))


async def main(apply: bool) -> None:
    try:
        async with session_scope() as session:
            p = {"days": DAYS}
            table("관문별 깔때기 (7일)", (await session.execute(FUNNEL, p)).all())
            table("소스별 발송·👍·👎", (await session.execute(BY_SOURCE, p)).all())
            table("importance 별 👍·👎", (await session.execute(BY_IMPORTANCE, p)).all())
            table("점수 구간별 👍·👎 (탐색 포함)", (await session.execute(BY_SCORE_BAND, p)).all())
            table("선별 relevance 구간 vs 판정", (await session.execute(TRIAGE_VS_JUDGE, p)).all())
            table("저관련도 사유 상위 10", (await session.execute(DROP_REASONS, p)).all())

            labels = (await session.execute(TRUST_LABELS, {"days": TRUST_DAYS})).all()
            print("\n## 신뢰도 보정 (30일)")
            print("source | base | useful | useless | adjusted")
            for r in labels:
                adjusted = adjust_trust(r.trust_score, r.useful, r.useless)
                print(f"{r.name} | {r.trust_score} | {r.useful} | {r.useless} | {adjusted}")
                if apply:
                    await session.execute(
                        text("UPDATE sources SET trust_adjusted = :v WHERE id = :id"),
                        {"v": adjusted, "id": r.id},
                    )
            print("\n(--apply 로 반영됨)" if apply else "\n(드라이런. --apply 로 sources.trust_adjusted 에 씀)")
    finally:
        await engine.dispose()


if __name__ == "__main__":
    parser = argparse.ArgumentParser(description="주간 튜닝 리포트")
    parser.add_argument("--apply", action="store_true", help="신뢰도 보정값을 DB 에 쓴다")
    asyncio.run(main(parser.parse_args().apply))
```

- [ ] **Step 2: 실행**

Run: `PYTHONIOENCODING=utf-8 uv run python scripts/weekly_report.py`
Expected: 표 7개. 라벨이 아직 적어 대부분 0 이지만 깔때기와 선별 구간 표는 채워진다.

- [ ] **Step 3: 커밋**

```bash
git add scripts/weekly_report.py
git commit -m "feat: 주간 튜닝 리포트 — 깔때기·정밀도·구간 표, --apply 로 신뢰도 보정"
```

### Task 4.3: 4단계 마무리

- [ ] `CLAUDE.md` Commands 절에 `scripts/weekly_report.py`, `scripts/backfill_embeddings.py`, `scripts/calibrate_dedupe.py` 를 적는다.
- [ ] Codex 리뷰: `codex exec --sandbox read-only "커밋 범위 <4.1 첫 커밋>..HEAD 리뷰. 관점: 리포트 SQL 의 조인 중복 집계, --apply 의 트랜잭션, adjust_trust 경계" </dev/null`
- [ ] 지적 반영 → 재커밋 → 재리뷰.
- [ ] 옵시디언: 기능 노트 `검증 파이프라인 v2` 완료 표시, 허브 표 갱신, 일일 노트 `기술파악 - 신뢰도 보정·주간 리포트.md`.
- [ ] 첫 주간 리포트를 사용자와 함께 읽고 `rules.yaml` 의 `threshold`·`dedupe` 를 한 번 조정한다.

---

## 자체 점검 결과

- **설계서 대응** — §5 임베딩(1.1·1.3·1.4), §6 중복(1.5·1.6·1.7), §7 선별(2.5·2.6), §8 점수·stale(2.3·2.6), §9 C-1(3.1·3.2)·C-2(4.1·4.2)·C-3(4.2)·C-4(3.3), §10 스키마(1.1·1.2)·설정(2.1), §11 실패·예산(1.3·2.4·2.6), §12 전환 순서(단계 배치 동일), §13 테스트(각 태스크), §15 다중 워커 비목표(변경 없음).
- **이름 일관성** — `reserve_call(kind) -> str | None`, `find_candidates(session, item_id, cfg)`, `classify(*, item_id, own_source_id, own_title, dup, related, cfg)`, `embed_pending(session, item_ids)`, `call_triage(rules, entries)`, `parse_triage(batch, expected)`, `nearest_feedback(session, item_id, *, k, min_sim)`, `format_examples(examples)`, `llm.judge(rules, *, source, title, body, examples)`, `adjust_trust(base, useful, useless)` 를 모든 태스크가 같은 시그니처로 쓴다.
- **문서 대조가 필요한 외부 형식** — Voyage/OpenAI 요청 본문(1.3), Anthropic `system` 블록 배열과 `cache_control`(2.5), pgvector 의 `CAST(text AS vector)` 동작(1.1·3.1). 구현 직전에 공식 문서로 확인하고, 다르면 코드를 문서에 맞춘다.
