# 기술 파악 — 구현 설계서 (v0.2)

- 작성일: 2026-08-30 (v0.2: Neon 반영, 본문 보강 단계 명시)
- 기준 문서: 기획서 v0.1 (`claude/기획서.md`)
- 상태: 초안
- 전제 결정: 백엔드 **Python**, DB **Neon(서버리스 Postgres)**, 실행 환경 **클라우드 무료/저가 VM + Docker Compose(앱 이미지 1개)**, 푸시 채널 **메신저 봇 선행 → 앱 확장(C안)**. 봇은 텔레그램을 기본으로 하되 발송 계층을 어댑터로 추상화해 디스코드도 붙일 수 있게 한다.
- 보류: WaterCrawl 등 크롤링 플랫폼은 MVP에서 제외. JS 렌더링이 필요한 소스나 저장소 전체 크롤링이 필요해질 때 `sources/` 플러그인으로 검토.

---

## 1. 설계 원칙

1. **작게 시작해서 매일 쓰는 상태를 최우선으로.** 1단계(MVP)는 "수집기 3개 + 규칙 필터 + LLM 요약 + 텔레그램 푸시"가 실제로 도는 것까지다. 그 이후에 소스·점수화·앱을 얹는다.
2. **모든 단계는 플러그인.** 수집기(Source), 필터 규칙(Filter), 발송 채널(Notifier)은 각각 공통 인터페이스를 갖는 독립 모듈로 만들어, 소스 하나 추가가 파일 하나 추가로 끝나게 한다.
3. **파이프라인은 멱등(idempotent)하게.** 같은 항목이 두 번 들어와도 알림이 두 번 가지 않도록, 모든 단계는 DB의 상태를 기준으로 재실행 가능해야 한다.
4. **LLM은 마지막 관문에서만.** 규칙 필터·점수화로 걸러진 소수 항목에만 LLM을 호출해 비용을 통제한다.
5. **결정은 로그로 남긴다.** 왜 통과/탈락했는지(매칭 키워드, 점수 내역, LLM 판단)를 항목마다 저장해 튜닝의 근거로 삼는다.

---

## 2. 전체 아키텍처

```
┌──────────────────────────────── 외부 소스 ────────────────────────────────┐
│ GitHub Releases/Trending   RSS(기업 블로그·arXiv·YouTube)   HN/Reddit API   X │
└───────────┬──────────────────────────┬──────────────────────┬───────────────┘
            │ 폴링(APScheduler)          │ 폴링                 │ 폴링/웹훅(FastAPI)
            ▼                          ▼                      ▼
┌──────────────────────────── Collector Worker ────────────────────────────┐
│  sources/*.py  →  RawItem  →  normalize()  →  dedupe(url hash·제목 유사도) │
│  (피드가 주는 제목·요약·본문만 사용, 원문 페이지 요청 없음)                  │
│                                   │ 신규만 INSERT (status=NEW)             │
└───────────────────────────────────┼──────────────────────────────────────┘
                                    ▼
                         ┌─────────────────────┐
                         │  Neon (Postgres)    │  items / sources / rules /
                         │  (+ pg_trgm)        │  decisions / notifications /
                         └──────────┬──────────┘  feedback / user_prefs
                                    ▼
┌──────────────────────────── Pipeline Worker ─────────────────────────────┐
│  NEW → [규칙 필터] → [점수화] → 임계값 → [본문 보강*] → [LLM 요약·태깅·판단]  │
│                                                          → SCORED/DROPPED │
│  * body가 비어 있거나 짧을 때만 원문 URL을 받아 trafilatura로 본문 추출       │
└───────────────────────────────────┬──────────────────────────────────────┘
                                    ▼
┌──────────────────────────── Notifier Worker ─────────────────────────────┐
│  SCORED → 강도 결정(즉시/조용/피드) → 하루 상한·무음시간 → 어댑터 발송 → SENT │
│                         Telegram (MVP)  │  Discord  │  FCM (3단계)          │
└──────────────────────────────────────────────────────────────────────────┘
                                    ▲
                     텔레그램 인라인 버튼 "👍 유용 / 👎 불필요" → feedback 테이블
```

### 2.1 구성 요소

| 구성 요소 | 역할 | MVP 형태 |
|---|---|---|
| **API 서버 (FastAPI)** | 웹훅 수신(GitHub, 텔레그램 봇 콜백), 헬스체크, 이후 앱용 REST API | 컨테이너 1개 |
| **Scheduler/Worker (APScheduler)** | 소스별 폴링 주기 실행, 파이프라인·발송 잡 실행 | API와 같은 프로세스에서 시작(초기), 이후 분리 |
| **Neon (Postgres)** | 모든 상태의 단일 진실 원천. 큐 역할도 겸함(status 컬럼 기반) | 관리형, 컨테이너 없음 |
| **LLM API** | 요약·태깅·"알릴 가치" 판단 | Claude Haiku 계열(저비용) |
| **Notifier** | 채널 어댑터 | 텔레그램 봇 |

MVP에서는 **프로세스 하나(FastAPI + APScheduler) + Neon 하나**로 충분하다. Docker 이미지는 `app` 하나만 빌드하며, 이후 워커를 분리하더라도 같은 이미지를 `CMD`만 바꿔 여러 컨테이너로 띄운다. Redis·메시지 큐는 도입하지 않는다.

파이프라인은 **하나**다. RSS 등 수집기는 피드가 주는 제목·요약·본문만으로 `items`를 만들고, 원문 페이지를 따로 요청하지 않는다. 본문이 없는 소스(HN·Reddit 링크 등)는 규칙 필터를 통과한 항목에 한해 LLM 호출 직전에 원문을 받아 본문을 채운다(5.5절 본문 보강). 탈락할 항목의 페이지까지 긁지 않기 위해 보강 시점을 뒤로 미룬 것이다. 항목 수가 하루 수천 건 수준이면 Postgres의 status 컬럼 + `SELECT ... FOR UPDATE SKIP LOCKED`로 큐를 대체할 수 있다. 부하가 커지면 그때 Redis/RQ 또는 Celery로 Worker를 분리한다.

### 2.2 왜 이 구성인가 (대안 비교)

| 선택지 | 채택 여부 | 이유 |
|---|---|---|
| Postgres를 큐로 사용 vs Redis/Celery | **Postgres** | 단일 사용자·소량 트래픽. 인프라 1개 줄고, 상태 조회·디버깅이 SQL 한 줄로 끝남 |
| APScheduler vs cron vs Temporal | **APScheduler** | 소스별 다른 주기(5분~6시간)를 코드로 관리하기 쉬움. 프로세스 내 실행이라 배포 단순 |
| 자체 Postgres 컨테이너 vs Neon | **Neon** | VM에 DB 운영·백업 부담 없음, 브랜치로 개발/운영 DB 분리 쉬움, pg_trgm·pgvector 확장 지원. 주의: 무료 티어 컴퓨트는 유휴 시 자동 중지되므로 짧은 주기 폴링이 컴퓨트 시간을 계속 소모함 → 시작 전 현재 무료 한도 확인. 연결은 pooled 엔드포인트(pgbouncer) 사용, 잡 단위로 커넥션을 열고 닫기 |
| 임베딩 기반 중복제거 vs 제목 유사도 | **제목 유사도(pg_trgm) 우선** | 대부분의 중복은 같은 URL 또는 거의 같은 제목. 임베딩은 4단계에서 검토 |

---

## 3. 기술 스택

| 영역 | 선택 | 비고 |
|---|---|---|
| 언어 | Python 3.12+ | |
| 패키지 관리 | uv | 빠르고 lock 파일 관리 간단 |
| 웹/API | FastAPI + uvicorn | 웹훅·헬스체크·앱 API |
| 스케줄러 | APScheduler 3.x (AsyncIOScheduler) | 잡 상태는 Postgres jobstore |
| HTTP 클라이언트 | httpx (async) | 재시도는 tenacity |
| 피드 파싱 | feedparser | RSS/Atom 공통 |
| HTML 본문 추출 | trafilatura | 본문 보강 단계에서만 사용(5.5절). WaterCrawl·Crawl4AI는 MVP 보류 |
| DB | Neon (Postgres 16/17) + pg_trgm | pooled 연결 문자열 사용 |
| ORM/마이그레이션 | SQLAlchemy 2.x (async, asyncpg) + Alembic | Neon pooled 엔드포인트는 prepared statement 제한이 있어 asyncpg `statement_cache_size=0` 설정 |
| 데이터 검증 | Pydantic v2 | NormalizedItem 스키마, 설정 |
| LLM | Anthropic SDK (Claude Haiku 계열) | 구조화 출력(JSON)으로 요약·태그·판단 한 번에 |
| 텔레그램 | python-telegram-bot 21.x (async) | 인라인 키보드로 피드백 |
| 디스코드 (선택) | discord.py 또는 Webhook URL | Webhook만 쓰면 의존성 없이 가능 |
| GitHub | PyGithub 또는 직접 REST/GraphQL | Releases·Stars 조회, Webhook 수신 |
| Reddit | asyncpraw | 2단계 |
| Hacker News | Algolia HN API / Firebase API | 인증 불필요 |
| X | 공식 API v2(제한 큼) 또는 RSS 미러 | 2단계, 최우선순위 아님 |
| 설정 | pydantic-settings + `.env` / `config.yaml` | 소스·키워드·임계값은 YAML, 시크릿은 .env |
| 로깅 | structlog (JSON) | 결정 로그와 함께 |
| 테스트 | pytest + pytest-asyncio + respx(HTTP 모킹) | 소스별 fixture 응답 저장 |
| 코드 품질 | ruff (lint+format), mypy | pre-commit |
| 컨테이너/배포 | Docker + Docker Compose | VM에서 `docker compose up -d` |
| CI | GitHub Actions | lint·test → 이미지 빌드 → VM에 SSH 배포 |
| 모니터링 | healthchecks.io(잡 실패 감지) + 텔레그램 자체 알림 | 파이프라인이 죽으면 봇으로 알림 |
| 3단계 앱 | Flutter 또는 React Native/Expo + FCM | 별도 결정 |

---

## 4. 데이터 모델

```sql
sources          -- 수집 소스 정의
  id, type(rss|github_release|github_trending|hn|reddit|youtube|x),
  name, config(jsonb: url/repo/subreddit 등), poll_interval_sec,
  trust_score(0~1), enabled, last_polled_at, last_error

items            -- 정규화된 항목 (핵심 테이블)
  id, source_id, external_id, url, url_normalized, url_hash(unique),
  title, summary_raw(원문 요약/본문 일부), author, published_at, fetched_at,
  category(model|library|technique|tool_mcp|community|video|unknown),
  status(NEW|FILTERED_OUT|SCORED|DROPPED|QUEUED|SENT|FAILED),
  cluster_id(중복 묶음), raw(jsonb)

decisions        -- 항목별 판단 기록 (튜닝용)
  id, item_id, stage(rule|score|llm), passed(bool),
  score, details(jsonb: 매칭 키워드, 가중치 내역, LLM 응답), created_at

summaries        -- LLM 결과
  item_id(pk), title_ko(알림 제목), summary_ko(2~3줄), tags(text[]),
  importance(1~5), worth_notifying(bool), model, tokens_in, tokens_out

notifications    -- 발송 기록
  id, item_id, channel(telegram|discord|fcm), level(push|silent|feed),
  sent_at, message_id, error

feedback         -- 사용자 반응
  id, item_id, verdict(useful|useless), created_at

user_prefs       -- 단일 사용자 설정 (MVP는 YAML로 대체 가능)
  keywords_include(text[]), keywords_exclude(text[]), repos(text[]),
  threshold, daily_cap, quiet_hours(jsonb)
```

정규화 스키마(Pydantic):

```python
class NormalizedItem(BaseModel):
    source: str  # "github_release:vercel/next.js"
    external_id: str  # 소스 내 고유 id (release id, video id, HN id ...)
    url: HttpUrl
    title: str
    body: str | None  # 요약 입력용 (본문/설명 앞 2~3천자)
    author: str | None
    published_at: datetime
    category_hint: Category | None  # 소스가 자명한 경우 (YouTube→video 등)
    metrics: dict = {}  # stars, points, comments, views 등 점수화 입력
```

---

## 5. 워크플로우 (단계별 상세)

### 5.1 수집 (Collector)

```python
class Source(Protocol):
    name: str
    interval: int  # 초

    async def fetch(self, since: datetime | None) -> list[NormalizedItem]: ...
```

- 소스 하나 = `app/sources/<name>.py` 하나. 등록은 `config/sources.yaml`.
- APScheduler가 소스별 `interval`로 `run_source(source_id)` 잡을 실행. 잡은 `sources.last_polled_at` 이후 항목만 가져온다(가능한 소스는 `If-Modified-Since`/ETag 사용).
- 실패 시 지수 백오프, 연속 5회 실패면 `last_error` 기록 + 텔레그램으로 운영 알림.
- 폴링 주기 초안:

| 소스 | 방식 | 주기 |
|---|---|---|
| GitHub Releases (관심 저장소) | Webhook(release 이벤트) + 보조 폴링 | 웹훅 즉시 / 30분 |
| GitHub Trending | HTML 스크래핑 또는 검색 API(`created:>`, `stars:>`) | 6시간 |
| 기업 기술 블로그 RSS | feedparser | 15분 |
| arXiv RSS (cs.AI/CL/LG/SE) | feedparser | 1시간 (하루 1회 갱신이므로 충분) |
| YouTube 채널 RSS | `youtube.com/feeds/videos.xml?channel_id=` | 15분 |
| Hacker News | Algolia `search_by_date` + 프론트 진입(points≥N) 판정 | 10분 |
| Reddit | asyncpraw `subreddit.new()` | 15분 |
| X | API/미러 제한에 따라 | 30분~ |

### 5.2 정규화 · 중복 제거

1. `url_normalized`: 스킴/호스트 소문자화, `utm_*`·`ref` 등 추적 파라미터 제거, 트레일링 슬래시 제거, `www.` 제거 → SHA-256 → `url_hash`(unique). 같은 해시면 INSERT 스킵.
2. 제목 유사도: 최근 72시간 항목 중 `similarity(title, :title) > 0.6`(pg_trgm)인 항목이 있으면 같은 `cluster_id`로 묶고 status를 `FILTERED_OUT(dup)`으로 둔다. 단, 다른 소스에서 재등장한 사실은 클러스터의 `mention_count`로 올려 점수화에 반영한다.
3. 정규화 실패(필수 필드 누락)는 `raw`만 저장하고 로그.

### 5.3 규칙 필터 (1단계 관문)

`config/rules.yaml`:

```yaml
include_keywords: [claude, gpt, gemini, llama, langgraph, langchain, mcp, rag, vector, agent, next.js, fastapi, ...]
include_repos: [anthropics/*, langchain-ai/langgraph, vercel/next.js, modelcontextprotocol/*]
exclude_keywords: [hiring, giveaway, webinar, sponsored, "$"]
exclude_domains: [medium.com/@spam, ...]
always_pass_sources: [github_release:*, youtube:*]   # 화이트리스트 소스는 규칙 통과
```

- 제목+본문 앞부분에 대해 include 매칭(대소문자 무시, 단어 경계). 하나도 없으면 `FILTERED_OUT(no_keyword)`.
- exclude 매칭 시 즉시 탈락.
- 매칭된 키워드 목록을 `decisions.details`에 기록.

### 5.4 점수화 (2단계 관문)

```
score = w_src * trust(source)                 # 0~1, 소스 신뢰도
      + w_kw  * min(keyword_hits, 3) / 3      # 관심 키워드 밀도
      + w_hot * hotness(metrics)              # HN points·GitHub stars 증가 속도·reddit upvotes를 소스별 정규화
      + w_multi* min(cluster.mention_count-1, 2)/2   # 여러 소스 동시 등장
      + w_fresh* freshness(published_at)      # 24시간 이내 1.0, 이후 감쇠
```

- 초기 가중치 예: `w_src=0.25, w_kw=0.25, w_hot=0.2, w_multi=0.2, w_fresh=0.1`. 임계값 `0.45`부터 시작해 로그 보고 조정.
- 임계값 미만은 `DROPPED`, 이상이면 LLM 단계로. 점수 내역은 `decisions`에 저장.

### 5.5 본문 보강 → LLM 요약·태깅·판단 (3단계 관문)

**본문 보강(enrich)**: 점수 임계값을 통과한 항목 중 `body`가 비어 있거나 300자 미만이면 원문 URL을 httpx로 받아 trafilatura로 본문을 추출해 `body`를 채운다. 피드가 이미 본문을 주는 소스(기업 블로그 RSS, arXiv 초록, GitHub 릴리즈 노트, YouTube 설명)는 이 단계를 건너뛴다. 타임아웃 10초, 실패해도 제목·요약만으로 LLM 단계를 진행한다(실패 사실은 `decisions.details`에 기록). 이 단계를 수집 시점이 아니라 여기에 둔 이유는 탈락할 항목의 페이지까지 요청하지 않기 위해서다.

**LLM 호출**: 한 번의 호출로 JSON을 받는다:

```json
{
  "worth_notifying": true,
  "importance": 4,
  "category": "model",
  "title_ko": "[새 모델] Claude X 출시 — 컨텍스트 2배, 가격 동일",
  "summary_ko": "…2~3줄…",
  "tags": ["claude", "llm", "pricing"]
}
```

- 입력: 제목, 본문 앞 2,000자, 소스, 사용자 관심 스택(프롬프트에 고정 삽입), 카테고리 목록과 제목 형식 규칙(`[태그] 무엇 — 핵심 한 줄`).
- `worth_notifying=false`면 `DROPPED(llm)`, true면 `SCORED`.
- 비용 통제: 하루 LLM 호출 상한(예: 300건), 초과 시 점수 순으로만 처리. 응답은 `summaries`에 캐시해 재실행 시 재호출 안 함.
- 모델은 Haiku급으로 시작. 정확도 문제가 보이면 importance≥4 항목만 상위 모델로 재판단하는 2단 구조 검토.

### 5.6 발송 (Notifier)

```python
class Notifier(Protocol):
    async def send(self, item: Item, summary: Summary, level: Level) -> str: ...  # message_id
```

- 강도 결정: `importance ≥ 4 → push(소리)`, `3 → silent(무음 알림)`, `≤2 → feed(발송 안 함, 이력만)`.
- 하루 상한(예: push 15건)과 무음 시간(예: 23:00~08:00 KST)은 push를 silent로 낮추거나 아침에 몰아 보내는 방식으로 처리(`QUEUED` 상태로 대기).
- 텔레그램 메시지 형식:

```
🆕 [새 모델] Claude X 출시 — 컨텍스트 2배, 가격 동일
요약 2~3줄…
출처: Anthropic Blog · 12분 전
[원문 보기]  [👍 유용]  [👎 불필요]
```

- 인라인 버튼 콜백 → FastAPI 웹훅(또는 롱폴링) → `feedback` INSERT. 4단계에서 키워드 가중치·소스 신뢰도 자동 조정에 사용.
- 발송 실패는 3회 재시도 후 `FAILED`, 운영 알림.

### 5.7 상태 전이 요약

```
NEW ──dup/no_keyword──▶ FILTERED_OUT
NEW ──규칙 통과──▶ (점수) ──미달──▶ DROPPED
                        └─통과──▶ (LLM) ──false──▶ DROPPED
                                        └─true──▶ SCORED ──상한/무음──▶ QUEUED
                                                         └─발송──▶ SENT / FAILED
```

---

## 6. 프로젝트 구조

```
tech-radar/
├── app/
│   ├── main.py               # FastAPI 앱 + 스케줄러 기동
│   ├── config.py             # pydantic-settings, YAML 로더
│   ├── db/                   # SQLAlchemy 모델, 세션, 마이그레이션(alembic/)
│   ├── schemas.py            # NormalizedItem 등 Pydantic
│   ├── sources/              # 소스 플러그인
│   │   ├── base.py           # Source 프로토콜, 등록 레지스트리
│   │   ├── rss.py
│   │   ├── github_release.py
│   │   ├── github_trending.py
│   │   ├── hackernews.py
│   │   ├── youtube.py
│   │   └── reddit.py
│   ├── pipeline/
│   │   ├── normalize.py      # URL 정규화, 해시
│   │   ├── dedupe.py
│   │   ├── rules.py          # 키워드/화이트·블랙리스트
│   │   ├── scoring.py
│   │   └── llm.py            # 프롬프트, 구조화 출력, 캐시
│   ├── notify/
│   │   ├── base.py           # Notifier 프로토콜
│   │   ├── telegram.py
│   │   ├── discord.py
│   │   └── policy.py         # 강도·상한·무음시간
│   ├── jobs/                 # APScheduler 잡 정의(run_source, run_pipeline, run_notify)
│   └── api/                  # 웹훅 라우터(github, telegram), health, (3단계) items API
├── config/
│   ├── sources.yaml
│   └── rules.yaml
├── tests/
│   └── fixtures/             # 소스별 실제 응답 샘플
├── scripts/                  # 수동 실행·백필·튜닝 리포트
├── docker-compose.yml
├── Dockerfile
├── pyproject.toml
└── .github/workflows/ci.yml
```

---

## 7. 인프라 · 배포 · 운영

### 7.1 실행 환경

- VM 1대 (Oracle Cloud Free Tier ARM 4코어/24GB, 또는 월 5~6달러급 VPS). Docker + Docker Compose.
- DB는 Neon 프로젝트 1개. 브랜치 `main`(운영)과 `dev`(로컬 개발·CI 테스트)로 분리. 로컬 개발도 Neon dev 브랜치에 붙어 환경 차이를 없앤다.
- `docker-compose.yml`: `app`(FastAPI+스케줄러, 이미지 1개), `caddy`(HTTPS 리버스 프록시, 자동 인증서). DB 컨테이너 없음. 도메인이 없으면 DuckDNS 등 무료 서브도메인.
- 시크릿(.env): `ANTHROPIC_API_KEY`, `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`, `GITHUB_TOKEN`, `GITHUB_WEBHOOK_SECRET`, `DATABASE_URL`(Neon pooled 연결 문자열, `sslmode=require`).

### 7.2 CI/CD

1. push → GitHub Actions: `ruff` → `mypy` → `pytest`.
2. main 머지 → 이미지 빌드 → GHCR 푸시 → VM에 SSH로 `docker compose pull && up -d`.
3. Alembic 마이그레이션은 컨테이너 시작 시 자동 실행(`alembic upgrade head`).

### 7.3 운영·관측

- 매 잡 종료 시 healthchecks.io ping → 일정 시간 미수신이면 이메일/텔레그램으로 경고.
- 구조화 로그(JSON) → `docker logs`; 필요 시 Loki/Grafana는 나중에.
- 주간 튜닝 리포트 스크립트: 소스별 수집/통과/발송 수, 피드백 유용 비율, DROPPED 중 피드백으로 뒤집힌 항목 → 텔레그램으로 요약 전송.
- DB 백업: Neon의 시점 복구(PITR)에 맡기고, 별도 `pg_dump`는 월 1회 정도 선택 사항.
- Neon 컴퓨트 사용량 모니터링: 대시보드에서 월 컴퓨트 시간을 확인하고, 한도에 근접하면 야간 무음 시간 동안 폴링 주기를 늘려 DB를 쉬게 한다.

---

## 8. 단계별 개발 로드맵

| 단계 | 범위 | 완료 기준 | 예상 |
|---|---|---|---|
| **0. 골격** | 레포·uv·FastAPI·Neon 연결·Alembic·Compose·CI, `NormalizedItem`·`items` 테이블 | 로컬에서 `docker compose up`으로 헬스체크 응답 + Neon dev 브랜치에 마이그레이션 적용 | 1~2일 |
| **1. MVP** | 소스: RSS(기업 블로그 3~4개), GitHub Releases(관심 저장소 10개), YouTube(조코딩·코딩애플). 규칙 필터, LLM 요약, 텔레그램 발송, 피드백 버튼. VM 배포 | **매일 실제 알림을 받는 상태.** 2주 사용 후 기획서 9장 성공 기준 점검 | 1~2주 |
| **2. 소스·정확도** | HN, Reddit, GitHub Trending, (X). 점수화 도입, 클러스터 중복 제거, 하루 상한·무음 시간, `rules.yaml` 핫리로드 | 유용 비율 50% 이상, 하루 push 15건 이하 | 2~3주 |
| **3. 앱** | `GET /items`, `/feedback` 등 REST API + Flutter/Expo 앱 + FCM Notifier. 알림 이력·설정 화면 | 앱으로 푸시 수신·이력 조회 | 3~4주 |
| **4. 개인화** | 피드백 기반 가중치 자동 조정, 급상승 감지(stars/points 증가 속도), 임베딩 중복 제거 | 미탐·오탐 감소를 리포트로 확인 | 지속 |

---

## 9. 리스크 대응 (기획서 10장 → 구현 관점)

| 리스크 | 구현 대응 |
|---|---|
| X·레딧 API 제한 | Source 인터페이스 덕분에 소스 교체가 국소적. 폴링 주기는 `sources.poll_interval_sec`로 런타임 조정. 429 수신 시 주기 자동 2배 |
| 알림 피로 | `notify/policy.py` 한 곳에 상한·무음·강도 규칙 집중. 피드백 👎 3회 이상 키워드는 자동 가중치 하향 |
| LLM 비용 | 규칙→점수 2단 관문 후에만 호출, 일 상한, 결과 캐시, Haiku급 모델 |
| 오탐·미탐 | `decisions` 테이블에 모든 판단 기록 → 주간 리포트로 튜닝. DROPPED 항목도 피드에서 볼 수 있게 남김 |
| 지속성 | 0·1단계를 2주 안에 끝내는 것을 최우선. 소스 하나 = 파일 하나라 확장 부담 낮음 |

---

## 10. 남은 결정 사항

1. 텔레그램 vs 디스코드 — 기본 텔레그램, 어댑터로 둘 다 가능. **처음 2주는 하나만** 쓰는 것을 권장.
2. 1단계 관심 저장소 10개, 기업 블로그 RSS 3~4개, 초기 키워드 목록 → `config/*.yaml` 초안 작성 필요.
3. 허용 지연 시간 — 위 폴링 주기 초안은 **15~30분 내 도달**을 목표로 잡음.
4. 3단계 앱 프레임워크(Flutter vs Expo) — 3단계 진입 시 결정.
5. ~~DB 선택~~ → Neon으로 확정. 남은 확인 사항은 무료 티어의 현재 컴퓨트 한도와 폴링 부하의 궁합.
6. 패키지 구조 — 현재는 파이프라인 단계별 레이어드 구조. 도메인 로직을 DB·HTTP 없이 테스트하고 싶다면 `domain/ · application/ · adapters/` 헥사고날 경량 구조로 재편 가능(선택).
