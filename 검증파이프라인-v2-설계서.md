# 검증 파이프라인 v2 설계서

버전 v0.1 · 2026-09-06 · 대상 서비스: 기술파악(tech-radar)

> 키워드 목록 관문을 **정책 기반 관문**으로 바꾼다. 항목마다 임베딩을 한 번 저장해 중복·다중 소스 판정에 쓰고, 값싼 LLM 선별 배치가 관심 정책을 직접 읽어 관련도를 매기며, 사용자의 👍/👎 가 다음 판정과 소스 신뢰도에 되먹임된다.

---

## 1. 배경과 문제

현재 파이프라인은 `중복(pg_trgm) → 규칙(exclude + include 키워드) → 점수 → 본문 보강 → LLM 판정 → 발송` 이다. 관심 정책은 `rules.yaml` 의 관심사 서술문으로 이미 존재하지만, 그것을 읽는 관문은 맨 끝 LLM 뿐이고 앞 관문은 단어 목록으로 정책을 근사한다. 실데이터에서 세 가지 결함이 확인됐다.

| 결함 | 실제 사례 (2026-09-05 배치) |
|---|---|
| 맥락 없음 | 안전성 해석 논문이 `vector` 로, 3D 장면 논문이 `reasoning` 으로 통과. `uv` 는 자외선 논문도 잡는다 |
| 새 용어 무시 | 목록에 없는 새 모델명은 제목에 그 이름만 있으면 `no_keyword` 로 탈락. 알림기의 존재 이유와 정면충돌 |
| 한국어·영상 | YouTube 제목엔 영문 키워드가 없어 관문을 못 넘음. 화이트리스트 우회는 이 증상을 덮은 임시방편 |

유입은 하루 약 650건이고 그중 600건이 arXiv 다. 제목 평균 53자. 하루 발송은 5~15건이다.

## 2. 범위와 비목표

**범위** — 임베딩 저장, 벡터 기반 중복·관련 판정(B), LLM 선별 배치(A), 피드백 활용(C-1 사례 주입, C-2 신뢰도 보정, C-3 주간 리포트, C-4 탐색 슬롯). 구현은 4단계로 나누되 설계는 이 문서 하나다.

**비목표** — 소스별 정책(D)은 소스를 늘려 보며 별도 논의한다. 피드백 학습 분류기(C-5 증류)는 라벨 300건 이상 쌓인 뒤 별도 설계한다. 관심사 문장과의 코사인을 점수 항으로 쓰는 안은 넣지 않는다. 선별이 맥락까지 읽어 더 나은 관련도를 내고, 두 관련도가 공존하면 무엇을 믿을지 애매해진다.

## 3. 결정 요약

| 결정 | 선택 | 기각한 대안과 이유 |
|---|---|---|
| 선별과 점수의 관계 | 선별 → 점수. 관련도가 점수의 한 항 | (b) 점수 → 선별: 호출 26회 아끼려고 저신뢰 소스의 새 용어를 아무도 읽기 전에 버림. (c) 선별 독립 관문: 관련도와 신뢰도가 합쳐지지 않음 |
| 임베딩 제공자 | Voyage API 기본. OpenAI 키가 이미 있으면 `text-embedding-3-small`(1024차원) | 로컬 ONNX: 이미지 +300MB, 메모리 +400MB. large 급 모델: 50자 제목엔 과하고 저장 3배 |
| 임베딩 차원 | 1024 고정 | 제공자를 바꿔도 컬럼은 그대로. 모델을 바꾸면 전량 재계산 |
| 중복 판정 | trigram 을 벡터로 **대체**. 두 방식 병존 안 함 | 폴백 두 벌 유지는 복잡도만 늘림 |
| 중복 vs 관련 | 두 임계값. 중복(≥0.92)은 생존자만 기준, 관련(≥0.80)은 상태 무관 | 단일 임계값: 중복 오판은 항목을 잃고 관련 오판은 점수만 조금 오르므로 정밀도가 필요한 쪽만 좁게 |
| 키워드 관문 | 삭제. `include_keywords`·`include_repos` 는 프롬프트 힌트로 강등 | 유지: 결함 1·2 가 그대로 남음 |
| 화이트리스트 우회 | 삭제. YouTube·GitHub 도 같은 선별을 지남 | 유지: 선별이 있으면 우회할 이유가 없음 |
| 신선도 가드 | 화이트리스트 전용 48h → **전역** 72h, 선별 앞에 배치 | 백필 4,000건이 선별 호출 160회를 잡아먹는 걸 막음 |
| 상태 값 | 늘리지 않음. 관문 구분은 `decisions.stage` | 새 상태 추가: 전이도·쿼리 전부 손댐 |
| 실행 모델 | **단일 프로세스** 가 전제. 배치 잠금(`FOR UPDATE SKIP LOCKED`)을 외부 호출 동안 유지하는 현행 방식 유지 | 다중 워커: 비목표. 필요해지면 임대(lease) 컬럼과 원자적 예산 차감을 별도 설계 |
| 벡터 검색 | 72시간 창 안에서 **정확 검색**. HNSW 인덱스 없음 | HNSW: 창 안 행이 2,000건 수준이라 밀리초. 근사 검색은 상태·시간 필터와 결합 시 최근접을 놓칠 수 있음 |
| 피드백 저장 | `feedback.item_id` 유니크, 웹훅은 upsert | 클릭마다 행 추가 + 쿼리에서 최신 선택: "마지막 판정" 이 구조가 아니라 관례에 의존 |
| 일일 상한의 "오늘" | 세 상한 모두 `notify.timezone`(Asia/Seoul) 달력일 | 롤링 24시간(현행 판정 상한): 리포트의 일자 집계와 어긋남 |
| 피드백 사례 위치 | 사용자 블록의 항목 옆 | 시스템 블록: 캐시가 깨지고 어느 항목에 걸리는 사례인지 흐려짐 |
| 신뢰도 보정 저장 | `sources.trust_adjusted` 별도 컬럼 | `trust_score` 직접 갱신: 소스 동기화가 덮어씀 |
| 임계값·강도 조정 | 사람이 리포트를 보고 `rules.yaml` 수정 | 자동 조정: 값이 몇 주 안정된 뒤 |

## 4. 전체 구조와 상태 전이

```
수집 ─▶ 적재(url_hash, http 검사) ─▶ 임베딩(배치, 실패 시 NULL) ─▶ NEW

1국면 (항목별, 세이브포인트)
  NEW ─▶ 임베딩 보충(NULL 이면 재시도)
      ─▶ stale (published_at 이 72h 초과)          ─▶ DROPPED
      ─▶ ① 중복 (코사인 ≥ 0.92, 72h, 생존자 기준)   ─▶ FILTERED_OUT (cluster_id 상속)
      ─▶ ② 규칙 (exclude 키워드·도메인만)            ─▶ FILTERED_OUT
2국면 (25건씩 한 호출)
      ─▶ ③ 선별 (관련도 0~1 + 이유, 최근접 피드백 사례 주입)
3국면 (항목별, 세이브포인트)
      ─▶ ④ 점수 (src·rel·hot·multi·fresh ≥ 0.45)  ─▶ DROPPED
      ─▶ ⑤ 본문 보강 (300자 미만일 때만)
      ─▶ ⑥ 판정 (기존 + 사례 주입)                  ─▶ DROPPED / SCORED
SCORED ─▶ ⑦ 발송 정책 (기존 + 탐색 슬롯 1건/일)      ─▶ QUEUED / SENT / FAILED
```

상태 값은 `NEW · FILTERED_OUT · DROPPED · SCORED · QUEUED · SENT · FAILED` 그대로다. `FILTERED_OUT`(중복·exclude) 과 `DROPPED`(stale·점수·판정) 은 서로 다른 종단 분기이지 직렬 전이가 아니다. 재처리 대상은 오직 `NEW` 뿐이고, 종단 상태의 탈락 사유는 그 항목의 마지막 `decisions` 행이 말해 준다. `decisions.stage` 에 `triage` 가 추가되고, 1국면의 순서는 "싼 것부터, 탈락 가능성 높은 것부터" 다.

**실행 모델** — 단일 프로세스가 전제다. 파이프라인 잡은 배치 50건을 `FOR UPDATE SKIP LOCKED` 로 잠근 채 세 국면을 모두 지나고 끝에 커밋한다. 잠금을 외부 호출(임베딩·선별·판정) 동안 유지하는 것은 현행과 같다. 트랜잭션이 길어지는 대신 같은 항목이 두 번 호출되는 일이 구조적으로 없다. 다중 워커는 비목표다.

## 5. 임베딩

**저장** — Neon 에 `vector` 확장을 켜고 `items` 에 두 컬럼을 추가한다.

| 컬럼 | 타입 | 뜻 |
|---|---|---|
| `embedding` | `vector(1024)` NULL 허용 | NULL 이면 아직 계산 안 됨 또는 실패 |
| `embedding_model` | `text` NULL 허용 | 계산에 쓴 모델명. 모델 교체 시 재계산 대상 식별 |

벡터 인덱스는 두지 않는다. 모든 검색이 `published_at` 72시간 창 안에서만 이뤄지고 창 안 행은 2,000건 수준이라 정확 검색이 밀리초다. `published_at` 의 기존 btree 인덱스로 창을 자르고 그 안에서 코사인을 계산한다. 근사 검색(HNSW)은 상태·시간 필터와 결합하면 최근접을 놓칠 수 있어 정확성 면에서도 불리하다. 창 안 행이 수만 건이 되면 그때 인덱스를 붙인다. 제목 trigram GIN 인덱스와 `similarity()` 코드는 삭제한다.

**텍스트** — `제목 + 줄바꿈 + 본문 앞 300자`. 제목만 쓰면 같은 소식의 다른 표현을 놓치고 본문 전체를 쓰면 arXiv 초록끼리 주제만 같아도 붙는다. 300자는 출발값이고 보정 스크립트로 확정한다. **적재 시점의 텍스트로 한 번만 계산하고 ⑤ 본문 보강 뒤에도 다시 계산하지 않는다.** 보강은 점수를 통과한 소수에만 일어나므로, 보강 뒤 재계산하면 발송된(피드백이 붙는) 항목만 본문 위주 벡터가 되어 새 항목의 제목 위주 벡터와 비교가 어긋난다. 유사도에서는 풍부함보다 **모든 벡터가 같은 재료로 만들어졌다는 일관성** 이 중요하다.

**어댑터** — `app/pipeline/embedding.py` 에 `embed(texts: list[str]) -> list[list[float] | None]` 하나. 설정은 `EMBEDDING_PROVIDER`(voyage | openai), `EMBEDDING_MODEL`, 해당 API 키. 한 호출 최대 128건. 네트워크·5xx·429 만 3회 재시도하고 `retry-after` 를 존중한다. 최종 실패는 예외가 아니라 `None` 이다. **차원 계약** — 어댑터는 요청에 차원 1024 를 명시하고(Voyage `output_dimension`, OpenAI `dimensions`), 응답 벡터 길이가 1024 가 아니면 `None` 이 아니라 예외를 올린다. 이것은 장애가 아니라 설정 오류이고, 조용히 NULL 로 숨기면 중복 판정이 영구히 빠진다.

**시점** — 수집 잡이 `store_items` 로 새 id 를 받은 직후 그 배치를 임베딩한다. 실패는 경고만 남기고 NULL 로 둔다. 파이프라인 잡은 1국면 첫 단계에서 `embedding IS NULL` 항목을 다시 시도한다. 그래도 NULL 이면 중복·mention_count 를 건너뛰고 `dedupe_skipped` 를 기록한 채 진행한다. 제공자 장애 중에 같은 소식이 두 번 갈 수 있는 위험은 감수한다.

**모델 교체** — `embedding_model` 이 현재 설정과 다른 행은 백필 스크립트가 재계산한다. 교체 중에는 옛 벡터와 새 벡터가 섞여 비교되므로 백필이 끝날 때까지 파이프라인을 세운다.

## 6. 중복·관련 판정 (B)

코사인 유사도 하나로 세 가지를 판정한다.

```
쿼리 A (중복):  72시간 안 · embedding NOT NULL · 자기 제외 · 상태 ∈ {SCORED, QUEUED, SENT}
                 코사인 최댓값 1건
쿼리 B (관련):  72시간 안 · embedding NOT NULL · 자기 제외 · 상태 무관
                 sim ≥ related_threshold 인 행 전부 (id, source_id, cluster_id, title)

  A.sim ≥ dup_threshold(0.92) 이고 버전 예외 아님   → 중복. FILTERED_OUT, A 의 cluster_id 상속
  B 가 비어 있지 않음                              → 관련. B 중 최고 sim 의 cluster_id 상속, 파이프 계속
  그 외                                            → 독립. cluster_id = 자기 id
mention_count = B 의 서로 다른 source_id 수 + 1
```

두 쿼리로 나누는 이유는 상위 k 건을 먼저 자르고 상태를 거르면 생존 중복을 놓칠 수 있기 때문이다. 상위 5건이 모두 탈락 항목이고 6번째가 0.95 의 생존 항목인 경우가 실제로 생긴다. 상태 필터를 WHERE 에 두고 그 안에서 최댓값을 찾으면 이 문제가 없고, 정확 검색이라 비용도 같다.

**생존자 기준** — 탈락한 글이 뒤에 온 더 좋은 글을 죽이면 안 되므로 중복 기준은 생존자만이다. 관련·mention_count 는 상태와 무관하다. 어디서 떨어졌든 "여러 소스가 다뤘다" 는 신호는 유효하다.

**버전 예외** — 두 제목에서 `v?\d+(\.\d+)+(?:[-.][0-9A-Za-z.]+)?` 로 버전 토큰을 통째로 뽑아(`16.4.0-canary.14`, `v1.2.3`, `0.12.9`) 둘 다 있고 값이 다르면 sim 이 0.92 를 넘어도 관련로 내린다. `canary.13` 과 `canary.14`, `v1.2.3` 과 `v1.2.4` 를 같은 릴리즈로 묶지 않는다. 현행 trigram 에도 있던 구멍이다.

**분류 로직은 순수 함수** — `classify(candidates, thresholds) -> Verdict` 로 떼어 쿼리와 분리한다. 쿼리는 후보 5건과 유사도를 가져오는 일만 한다.

**보정** — 0.92·0.80 은 출발값이다. 백필 뒤 `scripts/calibrate_dedupe.py` 가 유사도 구간(0.95↑, 0.90~0.95, 0.85~0.90, 0.80~0.85, 0.75~0.80)별로 쌍을 10개씩 뽑아 보여주고, 사람이 읽고 `rules.yaml` 의 `dedupe` 임계값을 정한다. 임계값은 코드가 아니라 설정이다.

## 7. LLM 선별 배치 (A)

**입력** — 항목당 `소스명 · 제목 · 본문 앞 300자`. 25건을 번호 붙여 한 호출에 넣는다. 본문 전체는 넣지 않는다. 선별은 "읽을 후보인가" 만 가리고 정독은 ⑥ 판정이 한다.

**출력** — 구조화 출력(`messages.parse`)으로 항목마다 `idx · relevance(0~1) · reason(20단어 이내)`.

```
[1] rss:arxiv-cs-cl | Over-Refusal and Representation Subspaces… | 본문 300자
[2] youtube:jocoding | 클로드 가격 정책 논란 — 조코딩 의견 | 본문 300자
    유사 피드백: 👎 "[영상] 클로드 가격/정책 변화 논란 — 조코딩 의견 영상"
→ [{idx: 1, relevance: 0.2, reason: "안전성 해석 논문, 사용자 스택과 무관"},
   {idx: 2, relevance: 0.7, reason: "Claude 가격 변화, 관심사 명시 항목"}]
```

**relevance 눈금** (프롬프트에 명시) — 0.9 이상은 새 모델·릴리즈·기법·도구의 직접 변화. 0.6 은 관심 주제의 일반 글. 0.3 은 주변부. 0 은 무관·홍보·구인.

**프롬프트** — 시스템 블록에 정책(관심·비관심·주목 저장소·주목 스택)만 넣고 프롬프트 캐시를 건다. 사용자 블록에 25건과 항목별 피드백 사례가 들어간다. 모델은 판정과 같은 `claude-haiku-4-5`.

**기록** — 성공하면 항목마다 `decisions` 에 `stage=triage, passed=true, details={relevance, reason, batch_id}` 한 행. `batch_id` 는 호출마다 새로 만든 UUID 다. `passed` 는 "선별을 마쳤다" 는 뜻이고 통과·탈락은 ④ 점수가 정한다.

**실패** — 두 종류를 구분한다. 내용 실패(응답 개수·idx 불일치, 파싱 실패)는 1회 재시도하고, 그래도 실패하면 배치 항목을 `NEW` 로 되돌리며 항목마다 `passed=false, details.reason=triage_error` 를 남긴다. 같은 항목에 `triage_error` 2건이면 `DROPPED`. 이 실패는 모델이 그 항목을 두 번 다루지 못한 것이라 드물고 항목 탓이다. 기반 실패(네트워크·5xx·429·인증)는 SDK 재시도 뒤에도 실패하면 배치를 `NEW` 로 되돌리되 **결정 행을 남기지 않고** 경고 로그만 남긴다. 항목 탓이 아니므로 폐기 카운터에 세지 않는다. 기반 실패가 한 잡에서 연속 2배치면 운영 알림(`send_ops_alert`)을 보내고 그 잡을 끝낸다.

**예산** — `triage.daily_cap_calls`(기본 60, 1,500건). 판정 상한 300 과 별개다. 오늘 호출 수는 `decisions` 의 오늘(Asia/Seoul 달력일) `triage` 행에서 서로 다른 `batch_id` 의 수다.

**비용** — 하루 650건 × (제목 20 + 본문 150 토큰) ≈ 110k 입력 토큰, 26회 호출. 월 $3~4.

## 8. 점수 관문

식의 모양은 유지하고 항 하나만 바뀐다.

| 항 | 가중치 | 계산 | 변경 |
|---|---|---|---|
| src | 0.25 | × 소스 신뢰도 (`trust_adjusted` 가 있으면 그것, 없으면 `trust_score`) | C-2 반영 |
| **rel** | 0.25 | × 선별 relevance | `kw` 대체 |
| hot | 0.20 | × log 스케일 points/stars | 없음 |
| multi | 0.20 | × min(mention_count − 1, 2) / 2 | 언급을 임베딩 관련로 셈 |
| fresh | 0.10 | 24h 이내 1.0, 이후 24h 마다 절반 | 없음 |

임계값 0.45 유지. hot·multi 없이 24시간 이내일 때 혼자 통과하려면 `rel ≥ 1.4 − trust` 다.

| 소스 | trust | 필요한 relevance |
|---|---|---|
| anthropic, github 릴리즈 | 1.0 | 0.4 |
| openai | 0.9 | 0.5 |
| huggingface | 0.8 | 0.6 |
| vercel | 0.7 | 0.7 |
| youtube | 0.6 | 0.8 |
| arXiv | 0.5 | 0.9 |

48시간이 지나면 필요한 관련도가 0.2 씩 오른다. 오래된 소식일수록 더 관련돼야 한다는 뜻이다.

**전역 신선도 가드** — `scoring.max_age_hours`(기본 72) 초과 항목은 1국면 맨 앞에서 `DROPPED`, 사유 `stale`. 모든 소스에 적용된다.

**기록** — `details.breakdown` 에 다섯 항 값과 선별 `reason` 을 함께 남긴다. 한 행만 봐도 "왜 이 점수인가" 가 읽혀야 한다.

## 9. 피드백 활용 (C)

라벨은 하루 3건, 한 달 100건 수준으로 천천히 쌓인다. 라벨 몇 개로도 효과가 나는 것부터 넣는다. **항목당 판정은 하나** 다. `feedback.item_id` 를 유니크로 만들고 웹훅은 upsert 한다. 마음을 바꿔 다시 누르면 `verdict` 와 `created_at` 이 덮어써진다. 사용자가 한 명이라 사용자 식별자는 두지 않는다.

**C-1 최근접 사례 주입** — 선별 배치의 각 항목마다 임베딩이 가까운 피드백 항목 2건(코사인 ≥ 0.75)을 찾아 항목 줄 아래에 `👍/👎 "title_ko"` 로 붙인다. ⑥ 판정에는 3건. 피드백이 0건이면 줄이 안 붙을 뿐이다.

**C-2 소스 신뢰도 보정** — 소스별 최근 30일 👍·👎 로 베이즈 평활.

```
trust_adjusted = (10 × trust_score + 👍) / (10 + 👍 + 👎)      범위 [0.2, 1.0]
```

사전 가중치 10 은 라벨 10건이면 원래 값과 데이터가 반반이라는 뜻이다. `sources.trust_adjusted` 에 쓰고 `trust_score` 는 `sources.yaml` 값 그대로 둔다.

**C-3 주간 리포트** — `scripts/weekly_report.py`. 기본은 출력만, `--apply` 면 C-2 값을 쓴다.

| 표 | 답하는 질문 |
|---|---|
| 관문별 깔때기 | 어디서 얼마나 떨어지나 |
| 소스별 발송·👍·👎·정밀도 | 신뢰도가 맞나 |
| importance 별 👍·👎 | silent 기준을 3 에서 4 로 올릴까 |
| 점수 구간(0.05 단위) 별 👍·👎 | 임계값 0.45 가 맞나 |
| 선별 relevance 구간 vs 판정 결과 | 선별이 후하거나 짠가 |
| 탈락 사유 상위 10개 | 정책 문장을 어떻게 고칠까 |

**C-4 탐색 슬롯** — 하루 1건, 점수 관문에서 임계값 바로 아래(0.35~0.45)로 떨어진 최근 24시간 항목 중 최고점을 골라 판정을 돌리고, 통과하면 `🧪 실험 · 경계 항목` 머리말로 silent 발송한다. 이게 없으면 파이프는 자기가 버린 것에 대해 영원히 배우지 못한다.

탐색은 일반 발송 흐름을 타지 않고 발송 잡 끝의 `_explore` 한 함수가 처음부터 끝까지 처리한다. 후보는 `DROPPED` 이고 `Summary` 가 없으므로 일반 발송 쿼리(`SCORED` + `worth_notifying`)에 걸리지 않는다.

```
조건   오늘(Asia/Seoul) level=explore 인 notifications 가 없고, 무음 시간이 아님
후보   status=DROPPED · 마지막 decisions 가 stage=score, passed=false · score ∈ [threshold−0.10, threshold)
       · published_at 24시간 이내 · summaries 없음 · score 내림차순 1건, FOR UPDATE
판정   ⑥ 과 같은 judge 호출. Summary 저장 (worth_notifying 그대로), decisions 에 stage=llm 기록
       false → 상태 DROPPED 유지, 오늘 슬롯 소진으로 간주하지 않음 (다음 잡이 다음 후보를 봄)
       true  → 🧪 머리말로 발송 → notifications(level=explore) → 상태 SENT → 커밋
```

`Level` 에 `explore` 를 추가하고 push 상한 집계에서 제외한다. 단일 프로세스라 "오늘 explore 없음" 확인과 발송 사이에 경쟁은 없다. false 판정은 하루 최대 몇 번까지 시도할지 상한이 필요하다. `summaries 없음` 조건 덕분에 같은 후보를 두 번 판정하지는 않지만, 판정 호출이 하루 300 예산을 잠식하지 않도록 **탐색 판정은 하루 3회** 로 막는다. 오늘 `stage=llm` 이면서 `details.explore=true` 인 결정 행 수로 센다.

## 10. 스키마·설정 변경

**스키마** (마이그레이션 0003 한 번)

| 테이블 | 변경 |
|---|---|
| `items` | `embedding vector(1024)`, `embedding_model text` 추가. 벡터 인덱스 없음. 제목 trigram 인덱스 제거 |
| `sources` | `trust_adjusted float` NULL 허용 추가 |
| `notifications` | `level` 에 `explore` 값 (문자열 컬럼, 마이그레이션 없음) |
| `decisions` | `stage` 에 `triage` 값 (문자열 컬럼, 마이그레이션 없음) |
| `feedback` | `item_id` 유니크 제약. 마이그레이션이 기존 중복 행을 항목당 최신 1건으로 접은 뒤 제약을 건다. 웹훅 두 곳(텔레그램·디스코드)은 upsert 로 바꾼다 |

**`config/rules.yaml`** — 이름은 유지하고 구조를 바꾼다.

```yaml
policy:                 # ③ 선별·⑥ 판정 프롬프트가 읽는 정책. 문장으로 쓴다
  interests: |          # 지금 내용 유지
  not_interested: |     # 지금 interests 마지막 줄을 분리
  focus_repos: [anthropics/*, astral-sh/*, …]    # "특히 주목" 힌트. 관문 아님
  focus_stack: [fastapi, pydantic, mcp, …]       # 옛 include_keywords 를 줄여서
exclude:
  keywords: [hiring, 채용, 광고, sponsored, …]
  domains: []
dedupe:  {dup_threshold: 0.92, related_threshold: 0.80, window_hours: 72}
triage:  {batch_size: 25, daily_cap_calls: 60}
scoring: {w_src: 0.25, w_rel: 0.25, w_hot: 0.2, w_multi: 0.2, w_fresh: 0.1, threshold: 0.45, max_age_hours: 72}
notify:  {daily_push_cap: 15, quiet_start_hour: 23, quiet_end_hour: 8, timezone: Asia/Seoul}
```

삭제되는 키는 `include_keywords`·`include_repos`·`always_pass_sources`·`whitelist_max_age_hours`·`w_kw` 다. Pydantic 모델에 `extra="forbid"` 를 걸어 삭제된 키가 남아 있으면 기동이 실패하게 한다.

**`.env`** — `EMBEDDING_PROVIDER`, `EMBEDDING_MODEL`, `VOYAGE_API_KEY`(또는 `OPENAI_API_KEY`) 추가. `.env.example` 에 발급 안내를 적는다.

## 11. 오류 처리와 예산

| 호출 | 재시도 | 최종 실패 시 | 위험 |
|---|---|---|---|
| 임베딩 (기반 실패) | 네트워크·5xx·429 만 3회 | NULL 로 두고 진행. 중복·mention_count 건너뜀 | 장애 중 같은 소식 2회 발송 가능. 감수 |
| 임베딩 (차원 불일치) | 없음 | 예외. 잡 실패, 운영 알림 | 설정 오류를 숨기지 않음 |
| 선별 (내용 실패) | 1회 | 배치 `NEW` 복귀, `triage_error` 기록. 2회 누적 시 `DROPPED` | 항목 탓인 드문 경우 |
| 선별 (기반 실패) | SDK 기본 | 배치 `NEW` 복귀, 결정 행 없음. 연속 2배치면 운영 알림 후 잡 종료 | 장애가 길면 배치가 밀림. 상한이 낭비를 막음 |
| 판정 | SDK 기본 | 지금과 같음. 세이브포인트 롤백, `NEW` 유지 | 없음 |

예산은 임베딩 무제한(월 100원 미만), 선별 60회/일, 판정 300회/일(탐색 판정 3회 포함), 탐색 발송 1건/일. "오늘" 은 세 곳 모두 `notify.timezone` 의 달력일이고, 현행 판정 상한의 롤링 24시간도 이에 맞춘다. 각각 `decisions`·`summaries`·`notifications` 의 오늘 행 수로 세고 별도 카운터 테이블은 없다. 단일 프로세스라 읽고-호출하는 사이의 경쟁은 없다.

## 12. 전환 순서

```
1. 마이그레이션 0003   vector 확장, items 컬럼, trigram 인덱스 제거, sources.trust_adjusted, feedback 중복 접기 + 유니크
2. 백필 스크립트      NULL 또는 모델 불일치 행을 128건씩 임베딩. 멱등, 중단 후 재실행 가능
3. 보정 스크립트      유사도 구간별 쌍 표본 → dedupe 임계값 확정
4. 중복 판정 교체      벡터 쿼리 전환, trigram 코드·similarity() 삭제
5. 선별·정책·점수     rules.yaml 재구조화, 선별 배치, rel 항, stale 가드, 우회·키워드 관문 코드 삭제
6. 피드백 활용        C-1, C-4, C-2·C-3 리포트
```

5번이 처음 돌 때 남은 `NEW` 4,000건은 stale 가드가 선별 호출 없이 정리한다. 기존 `summaries`·`decisions` 는 유효하다.

## 13. 테스트

**순수 함수 단위 테스트** (DB 없음)

| 대상 | 함수 | 검증 |
|---|---|---|
| 중복·관련 분류 | `classify` | 0.92 중복, 0.80 관련, 버전 다르면 관련로 강등 |
| 버전 토큰 | `version_token` | `16.4.0-canary.14`·`v1.2.3`·`0.12.9` 추출, `v1.2.3` ≠ `v1.2.4`, 버전 없는 제목은 None |
| 선별 응답 | `parse_triage` | 개수·idx 불일치, relevance 범위, reason 길이, 내용 실패와 기반 실패의 구분 |
| 임베딩 어댑터 | `embed` | 응답 길이 ≠ 1024 면 예외 (None 아님) |
| 점수 | `score_item(rel=…)` | 신뢰도별 통과 relevance 표가 그대로 나오는지 |
| stale 가드 | `is_stale` | 경계값 |
| 신뢰도 보정 | `adjust_trust` | 0건이면 base, 10건이면 반반, 클램프 |
| 리포트 표 | 행 → 표 | 구간 나누기, 0 나누기 |

**HTTP 는 respx** — 임베딩 어댑터의 배치 분할·재시도·None 폴백, 선별의 재시도·`NEW` 복귀.

**pgvector 는 통합 테스트** — 중복 쿼리 A 가 상위 탈락 항목 너머의 생존 항목을 찾는지, 최근접 피드백, 탐색 후보 선택 3개. `TEST_DATABASE_URL` 이 있을 때만 돌고 없으면 건너뛴다. Neon `dev` 브랜치가 그 용도다.

**실데이터 검증** — 백필 뒤 보정 표본을 사용자가 읽고 임계값을 정하는 것, 첫 선별 배치의 `reason` 25개를 읽고 정책 문장을 손보는 것. 기존 73개 테스트 중 키워드 관문·화이트리스트 관련은 삭제된다.

## 14. 구현 단계

| 단계 | 내용 | 끝나면 보이는 것 |
|---|---|---|
| 1 | 마이그레이션·임베딩 어댑터·백필·보정·벡터 중복 | 한·영 교차 중복이 잡히고 trigram 코드가 사라짐 |
| 2 | 정책 설정·선별 배치·rel 점수·stale 가드·우회 삭제 | 키워드 없는 새 용어 항목이 통과하고 탈락 이유가 읽힘 |
| 3 | C-1 사례 주입·C-4 탐색 슬롯 | 👍/👎 가 다음 판정에 반영되고 🧪 카드가 하루 1건 옴 |
| 4 | C-2·C-3 리포트 스크립트 | 주간 표로 임계값·신뢰도를 데이터로 조정 |

각 단계가 커밋·Codex 리뷰·옵시디언 기록의 한 단위다. 상세 절차는 `검증파이프라인-v2-구현서.md` 에 둔다.

## 15. 열어 둔 것

- **D 소스별 정책** — arXiv 가 유입의 92% 다. 소스를 늘려 보며 별도 논의한다.
- **C-5 증류 분류기** — 라벨 300건 이상 쌓인 뒤. 임베딩과 trust·multi·fresh 를 특징으로, 확신 있는 부정만 LLM 호출 없이 버리는 용도.
- **👎 이유 버튼** — 중복·관심 없음·이미 앎을 나눌지는 라벨이 쌓인 뒤 판단.
- **리포트의 디스코드 발송** — 지금은 표준 출력. 필요해지면 운영 알림 경로로 보낸다.
- **다중 워커** — 비목표. 지금은 배치 잠금을 외부 호출 동안 유지해 중복 호출을 막지만, 워커를 늘리면 임대(lease) 컬럼, 원자적 예산 차감, 탐색 슬롯의 일자 유니크 제약, 429 게이트 공유가 함께 필요하다. 그때 별도 설계한다.
