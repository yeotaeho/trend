# 소스 확장 1차 설계서

버전 v0.1 · 2026-09-14 · 선행 문서: `기획서.md` 5.2·8장, `구현도.md` 5.1, `검증파이프라인-v2-설계서.md` · 후속 구현 계획: `소스확장-1차-구현서.md`(별도)

**Goal:** 수집 소스를 늘리되 파이프라인 v2 의 구조는 흔들지 않는다. (1) 다른 소스에서 같은 URL 이 다시 오면 버리지 않고 `metrics`·멘션을 병합하고 점수 탈락 항목을 되살려 `hot`·`multi` 항을 실제로 살린다. (2) 본문 보강이 사용자 제출 URL 을 열게 되므로 사설 주소를 막는다. (3) 첫 폴링의 백로그가 임베딩 예산을 쓰지 않게 한다. (4) Hacker News 프론트 페이지와 Hugging Face Daily Papers 를 새 타입으로 붙이고, RSS 4개를 YAML 로 더한다.

**범위 밖:** Reddit(OAuth 승인 대기), X(유료), Anthropic 사이트맵·MCP 레지스트리·GitHub Search(2차), YouTube 자막, 일일 적재 상한. 9장 참고.

---

## 0. 요약

```
[HN front_page] ─┐                                   ┌─ 새 URL → INSERT → 임베딩 → NEW (지금과 같음)
[HF daily_papers]├─ NormalizedItem ─▶ store_items ───┤
[RSS 4개 추가]  ─┘                                   └─ 아는 URL → raw.metrics 키별 max
                                                               raw.mentions 에 소스 추가
                                                               값이 바뀌었고 DROPPED(score 탈락)·72h 안이면 → NEW
                                                                 → 1국면 → 선별은 decisions 캐시 (LLM 0회)
                                                                 → 점수: hot(points·upvotes)·multi(mentions) 반영
                                                                 → 통과하면 판정 1회
```

새 소스가 주는 신호는 둘이다. `points`·`upvotes` 는 `hotness()` 의 기존 키에 그대로 맞고, "다른 소스도 다뤘다" 는 `mention_count` 에 더해진다. 지금은 `metrics` 를 채우는 소스가 GitHub prerelease 플래그뿐이고 소스가 겹치지 않아 두 항이 사실상 0 이다.

---

## 1. 배경

- 허브 노트의 "소스 확장 — HN·Reddit·GitHub Trending·X" 가 미착수였다. 구현도 8장은 이를 2단계로 두었다.
- v2 이후 실행 데이터에서 선별 449건 중 arXiv 가 380건이다. 소스를 늘릴수록 선별·임베딩 예산(선별 60회/일, Voyage 무료 RPM 3·TPM 약 1만)을 먼저 지켜야 한다. 그래서 **커뮤니티가 이미 고른 것만 받는다** (HN 프론트 페이지, HF 큐레이션).
- 결정 기록의 보류 항목 "본문 보강의 리다이렉트 최종 호스트 재검증 — HN·Reddit 소스가 들어오면 재검토" 가 이번에 닫힌다.

---

## 2. 후보 조사 (2026-09-14 실측)

집 회선에서 curl 로 확인했다. VM 은 데이터센터 IP 라 Reddit 은 더 엄격할 수 있다.

| 소스 | 방법 | 인증·비용 | 실측 | 판단 |
|---|---|---|---|---|
| Hacker News | Algolia `search?tags=front_page&hitsPerPage=30` | 무인증 | 200. `points`·`num_comments`·`created_at_i`·`url`. 30건 중 url 없음 1, `story_text` 있음 3 | **1차** |
| HF Daily Papers | `huggingface.co/api/daily_papers?limit=50` | 무인증 | 200. `paper.id`(arXiv id)·`paper.upvotes`·`paper.summary`·상위 `publishedAt`·`numComments` | **1차** |
| GeekNews · 요즘IT · Simon Willison · Anthropic engineering 미러 | RSS | 무인증 | 전부 200 (요즘IT 800KB) | **1차, 코드 0줄** |
| DeepMind 블로그 · Cursor changelog | RSS | 무인증 | 200 | 원하면 YAML 한 줄 |
| Mistral · Meta AI | RSS | | 404 | 제외 |
| Anthropic 공식 | `anthropic.com/sitemap.xml` lastmod 폴링 | 무인증 | 200, 531 URL 전부 lastmod. `/news/` 257·`/engineering/` 25·`/research/` 161 | 2차. RSS 없는 공식 사이트 공통 수집법 |
| MCP 공식 레지스트리 | `registry.modelcontextprotocol.io/v0/servers`, `updated_since`·`cursor` | 무인증 | 200. 같은 서버가 버전마다 별도 항목(1.0.0/1.0.1), `_meta…official.isLatest` 있음 | 2차. 버전 형제를 `isLatest` 로 접어야 함 |
| GitHub Trending | 공식 API 없음. 대안 Search `created:>7d stars:>100&sort=stars` | 토큰 있음 | 200, 한 주 116건 | 2차. HTML 스크래핑은 기각 |
| Reddit | OAuth + httpx `r/<sub>/top?t=day` | 비상업 무료 100 QPM. 2025 말부터 앱 등록이 수동 승인 티켓 | 무인증 `.rss` 는 1회 200 뒤 곧바로 429(`x-ratelimit-remaining: 0`), `.json` 403 | 티켓 먼저(8장), 코드는 승인 후 2차 |
| X | 공식 API | 2026-02 무료 티어 폐지, 읽기 건당 $0.005 pay-per-use. Nitter 대부분 사망 | | **보류**. 대안 Bluesky 공개 API(무인증 200)는 가치 미지수 |
| PyPI 릴리즈 | RSS `pypi.org/rss/project/<pkg>/releases.xml` | 무인증 | 200 | GitHub Releases 에 없는 패키지만. 겹치면 버전 형제 노이즈 |
| YouTube | 채널 RSS (기존) | | 두 채널 09-14 200. 09-11 404 는 일시적 | 자막(yt-dlp) 보강은 보류 |

수집 방법을 축으로 보면 **피드 폴링 · 공개 JSON API · 사이트맵 폴링 · 판정 직전 크롤링(현행 trafilatura) · 웹훅 · 인증 API** 여섯 가지다. 목록 페이지 크롤링은 사이트맵이 더 안정적이라 기각. JS 렌더링(Playwright)은 2GB VM 에서 하지 않는다.

---

## 3. 현 코드가 새 소스에 거는 제약

| 사실 | 위치 | 결과 |
|---|---|---|
| `store_items` 가 `ON CONFLICT DO NOTHING` | `app/pipeline/ingest.py` | 같은 URL 이 두 번째 소스에서 오면 조용히 버려진다. points 도 멘션도 남지 않는다 |
| `metrics` 는 `raw.metrics` 에 첫 적재 스냅샷 | `ingest.to_row`, `jobs/pipeline._judge` | 점수 낮을 때 들어오면 hotness 가 영원히 0. 그래서 프론트 페이지에 오른 뒤에만 수집한다 |
| `mention_count` 는 관련(≥0.88) 후보의 다른 `source_id` 수 + 1 | `pipeline/dedupe.classify` | 정확히 같은 URL 은 위 충돌로 두 행이 되지 않으므로 여기서도 안 센다 |
| 선별 결과는 `decisions` 캐시. NEW 로 돌아온 항목은 다시 호출하지 않는다 | `jobs/pipeline._existing_relevance` | 되살림이 LLM 호출 없이 점수만 다시 매길 수 있다 |
| stale 탈락도 `stage=score, passed=false, reason=stale` 로 남는다 | `jobs/pipeline._gate` | 되살림 조건에 `is_stale` 검사를 넣어야 stale 항목이 되살아나 매번 다시 죽지 않는다 |
| `enrich_body` 가 호스트를 검사하지 않고 `follow_redirects=True` | `app/pipeline/llm.py` | HN·Reddit 글의 링크가 `169.254.169.254`(Vultr 메타데이터) 등을 가리켜도 연다 |
| `run_source` 가 `since=last_polled_at`(첫 폴링은 None) | `app/jobs/collect.py` | 새 RSS 를 붙이면 피드 전체를 넣고 임베딩까지 한 뒤 72h 가드가 버린다 |
| 스케줄러가 기동 시 모든 소스를 동시에 돌린다 | `app/jobs/scheduler._interval` | 분당 1회급 제한 소스(Reddit)는 소스 수만큼 동시 요청이 나간다. 이번 범위의 HN·HF 는 소스당 1요청이라 무관 |

---

## 4. 설계

### 4.1 A1 — 적재 충돌 병합과 되살림

**파일:** `app/pipeline/ingest.py`, `app/jobs/pipeline.py`

**인터페이스**

```python
# app/pipeline/ingest.py
def merge_raw(
    existing: dict[str, object], item: NormalizedItem, *, same_source: bool
) -> tuple[dict[str, object], bool]:
    """기존 raw 에 새 관측을 합친다. (새 raw, 바뀌었는지).

    - metrics: 키별 max. 새 키는 추가.
    - mentions: 소스 이름의 정렬된 목록. same_source 면 넣지 않는다.
      같은 소스 판정은 raw.source 문자열이 아니라 items.source_id 와 호출자의 source_id 비교로 한다
      (옛 행에 raw.source 가 없어도 안전하다).
    - 그 외 키는 existing 우선. 같은 값이면 changed=False.
    """

async def store_items(session, source_id, items) -> list[int]:
    """새로 INSERT 된 id 만 돌려준다 (임베딩 대상). 병합·되살림된 항목은 포함하지 않는다."""
```

**`store_items` 흐름**

1. 스킴 검사·배치 내 중복 제거 (지금과 같음).
2. 배치의 `url_hash` 로 기존 행을 한 번에 조회한다. `SELECT id, url_hash, raw, status, published_at FROM items WHERE url_hash IN (...)`.
3. 기존 행이 있는 항목마다 `merge_raw`. `changed` 면 `raw` 를 UPDATE 한다.
4. `changed` 이고 `status == DROPPED` 이고 `not is_stale(published_at, rules.scoring.max_age_hours)` 인 행에 대해 마지막 `decisions` 행을 본다. `stage == score` 이고 `passed == false` 면 `status = NEW`. 로그 `ingest.revived`(item_id, source, mentions, metrics). `decisions` 에는 남기지 않는다. 판단이 아니라 재관측이고, 다음 점수 결정 행의 `breakdown` 에 `hot`·`multi` 가 남는다.
5. 기존 행이 없는 항목은 지금처럼 `INSERT … ON CONFLICT DO NOTHING RETURNING id` (같은 잡 안 경쟁은 없지만 웹훅 경로와의 경쟁을 그대로 막는다).

**되살림이 일어나지 않는 경우** — 같은 값의 재관측(HN 이 10분마다 같은 프론트 페이지를 줄 때), 마지막 결정이 `llm`(판정 false)·`rule`(중복·exclude)·`triage`(항목 실패 폐기), 72h 를 넘긴 항목, `SCORED`·`SENT` 등 살아 있는 항목(병합만 한다). 판정 false 항목은 사용자 결정으로 되살리지 않는다.

**점수 쪽** — `_judge` 에서 한 줄.

```python
mentions = item.raw.get("mentions", []) if isinstance(item.raw, dict) else []
mention_count = max(verdict.mention_count, 1 + len(mentions))
```

**같은 소스의 재관측** — HN 프론트 페이지는 매 폴링 같은 URL 을 준다. `merge_raw` 는 `points` 가 오른 만큼만 `changed` 를 내므로 점수 탈락 항목이 인기가 오르면 다시 살아나고, 같은 점수면 아무 일도 없다. 원하는 동작이다.

### 4.2 A2 — 본문 보강 가드

**파일:** `app/pipeline/llm.py`

```python
MAX_REDIRECTS = 5

def all_global(addresses: list[str]) -> bool:
    """전부 공인 주소여야 한다. 사설·루프백·링크로컬·예약 대역이 하나라도 있으면 False."""
    # ipaddress.ip_address(a).is_global

async def resolve(host: str) -> list[str]:
    # asyncio.get_running_loop().getaddrinfo(host, None) 로 블로킹 없이

async def enrich_body(url: str) -> str | None:
    # 홉마다: 스킴 http(s) 검사 → resolve → all_global 아니면 log llm.enrich_blocked 후 None
    # httpx follow_redirects=False. 3xx + Location 이면 urljoin 해서 다음 홉. MAX_REDIRECTS 초과면 None
    # 그 외는 지금처럼 raise_for_status → trafilatura.extract
```

`169.254.169.254` 는 링크로컬이라 `is_global` 이 False 다. 표준 라이브러리만 쓴다. DNS 리바인딩(검사 뒤 연결 사이에 응답이 바뀌는 경우)은 막지 않고 `ponytail:` 주석으로 한계와 승격 경로(연결 소켓의 peer 주소 재검사)를 남긴다. `is_web_url` 은 적재 단계 가드로 그대로 두고, 이 가드는 실제로 여는 곳 한 군데에만 둔다.

### 4.3 A3 — 첫 폴링 since

**파일:** `app/jobs/collect.py`

```python
def poll_since(last_polled_at: datetime | None, max_age_hours: int, *, now: datetime | None = None) -> datetime:
    """첫 폴링은 신선도 가드와 같은 창만 본다. 어차피 stale 로 버려질 백로그를 임베딩하지 않기 위해."""
    return last_polled_at or (now or datetime.now(UTC)) - timedelta(hours=max_age_hours)
```

`run_source` 가 `fetch(poll_since(source.last_polled_at, get_rules().scoring.max_age_hours))` 를 호출한다. `since` 를 존중하는 소스는 RSS·YouTube 다. GitHub Releases 는 설계상 무시하고(첫 페이지 재조회), HN·HF 도 무시한다(4.4·4.5). 일일 적재 상한은 이걸로 대체하고 만들지 않는다.

### 4.4 B1 — `hackernews` 소스

**파일:** `app/sources/hackernews.py`(신규), `app/sources/__init__.py`, `config/sources.yaml`

```python
API = "https://hn.algolia.com/api/v1/search?tags=front_page&hitsPerPage={n}"
HN_ITEM = "https://news.ycombinator.com/item?id={id}"
DEFAULT_HITS = 30
BODY_LIMIT = 3000

def hit_to_item(source_name: str, hit: dict[str, Any]) -> NormalizedItem | None:
    # title·objectID·created_at_i 없으면 None
    # url = hit["url"] or HN_ITEM (Ask HN 등 자체 글)
    # body = strip_html(story_text)[:BODY_LIMIT] or None  — 링크 글은 비워 보강에 맡긴다
    # author, published_at = datetime.fromtimestamp(created_at_i, UTC)
    # metrics = {"points": float(points or 0), "comments": float(num_comments or 0)}
    # raw = {"hn_id": objectID, "hn_url": HN_ITEM}
    # category_hint = None  — 링크 대상이 무엇인지는 선별이 본다

class HackerNewsSource:
    """프론트 페이지는 집합이라 since 로 자르지 않는다. 아는 URL 은 store_items 가 병합·되살린다."""
```

YAML

```yaml
  - name: hackernews:front
    type: hackernews
    config:
      hits_per_page: 30
    poll_interval_sec: 600
    trust_score: 0.6
```

`search_by_date` 로 points 임계값 수집(`numericFilters=points>N,created_at_i>since`)은 만들지 않는다. 프론트 페이지가 부족하다고 판단되면 `ponytail:` 주석의 승격 경로를 따른다.

### 4.5 B2 — `hf_papers` 소스

**파일:** `app/sources/hf_papers.py`(신규), `app/sources/__init__.py`, `config/sources.yaml`

```python
API = "https://huggingface.co/api/daily_papers?limit={n}"
ARXIV_ABS = "https://arxiv.org/abs/{id}"
DEFAULT_LIMIT = 50
MAX_AUTHORS = 3

def paper_to_item(source_name: str, entry: dict[str, Any]) -> NormalizedItem | None:
    # paper = entry["paper"]; id·title·상위 publishedAt 없으면 None
    # url = ARXIV_ABS  — arXiv RSS 의 링크와 정확히 같아 기존 arXiv 항목에 upvotes·멘션이 병합된다
    # external_id = paper.id, body = paper.summary[:3000], author = 앞 3명 이름 ", " 결합
    # published_at = entry["publishedAt"] (HF 게재 시각. 새 항목일 때 "오늘 떠오른 것" 이 사건이다)
    # metrics = {"upvotes": float(paper.upvotes or 0), "comments": float(entry.numComments or 0)}
    # raw = {"arxiv_id": paper.id}, category_hint = Category.TECHNIQUE

class HfPapersSource:
    """하루 목록은 집합이라 since 로 자르지 않는다. upvotes 증가가 병합·되살림의 신호다."""
```

YAML

```yaml
  - name: hf_papers:daily
    type: hf_papers
    config:
      limit: 50
    poll_interval_sec: 3600
    trust_score: 0.7
```

cs.AI·cs.CL 밖 논문은 새 항목으로 들어간다. 기존 arXiv 항목과 충돌한 경우 `published_at` 은 arXiv RSS 의 값이 유지된다(병합은 `raw` 만 바꾼다).

### 4.6 B3 — RSS 추가 (코드 0줄)

`config/sources.yaml` 에 추가한다. 반영은 `uv run python scripts/run_job.py sync`, 운영은 `main` 머지 시 컨테이너 기동의 `sync_sources` 가 한다.

```yaml
  - name: rss:geeknews
    type: rss
    config: { url: https://news.hada.io/rss/news }
    poll_interval_sec: 900
    trust_score: 0.6

  - name: rss:yozm
    type: rss
    config: { url: https://yozm.wishket.com/magazine/feed/ }
    poll_interval_sec: 1800
    trust_score: 0.5

  - name: rss:simonwillison
    type: rss
    config: { url: https://simonwillison.net/atom/everything/ }
    poll_interval_sec: 900
    trust_score: 0.8

  # rss:anthropic 과 같은 커뮤니티 미러. 외부 링크는 버린다.
  - name: rss:anthropic-engineering
    type: rss
    config:
      url: https://raw.githubusercontent.com/Olshansk/rss-feeds/main/feeds/feed_anthropic_engineering.xml
      allowed_hosts: [anthropic.com]
    poll_interval_sec: 900
    trust_score: 1.0
```

---

## 5. 테스트

| 테스트 | 종류 | 케이스 |
|---|---|---|
| `tests/test_ingest.py` | 순수 | `merge_raw`: 키별 max, 새 키 추가, 다른 소스면 mentions 추가, 자기 소스는 제외, 같은 값이면 `changed=False`, mentions 는 정렬·중복 없음 |
| `tests/test_collect.py`(신규) | 순수 | `poll_since`: `last_polled_at` 있으면 그대로, 없으면 `now − max_age_hours` |
| `tests/test_enrich_guard.py`(신규) | 순수 + respx | `all_global`: `10.0.0.1`·`127.0.0.1`·`169.254.169.254`·`::1` False, 공인 IPv4 True. `enrich_body`: 차단 호스트 → None 과 HTTP 요청 0회, 리다이렉트가 차단 호스트로 → None, 6홉 → None, 정상 → 추출문. resolve 는 monkeypatch |
| `tests/sources/test_hackernews.py`(신규) + `tests/fixtures/hn_front_page.json` | respx | 실제 응답을 3건으로 자른 fixture. url 없는 글은 HN 링크, `story_text` 는 body, metrics 키, `since` 무시 |
| `tests/sources/test_hf_papers.py`(신규) + `tests/fixtures/hf_daily_papers.json` | respx | url 이 `arxiv.org/abs/<id>`, upvotes·comments, `published_at` 은 상위 `publishedAt`, 저자 3명 제한 |
| `tests/integration/test_ingest_merge_db.py`(신규) | Neon dev | (1) 두 번째 소스의 같은 URL → 새 id 없음, `raw.metrics` max, `raw.mentions` 에 소스. (2) score 탈락 DROPPED·72h 안 → NEW. (3) 마지막 결정이 `llm` 인 DROPPED → 유지. (4) 같은 값 재관측 → `raw` 불변·DROPPED 유지. (5) `published_at` 이 73h 전인 score 탈락 → 유지 |

각 커밋 전 `uv run ruff check . && uv run ruff format . && uv run mypy app && uv run pytest -q`. 통합은 `TEST_DATABASE_URL=<dev> uv run pytest tests/integration`.

---

## 6. 운영 파라미터와 예산

| 값 | 초기값 | 어디 |
|---|---|---|
| HN 페이지 크기 / 주기 / 신뢰도 | 30 / 600초 / 0.6 | `sources.yaml` |
| HF Papers 크기 / 주기 / 신뢰도 | 50 / 3600초 / 0.7 | `sources.yaml` |
| 보강 리다이렉트 상한 | 5홉 | `pipeline/llm.py` `MAX_REDIRECTS` |
| 첫 폴링 창 | `scoring.max_age_hours`(72h) 재사용 | `jobs/collect.py` |
| 되살림 조건 | 값 변경 + DROPPED + 마지막 결정 score 탈락 + 72h 안 | `pipeline/ingest.py` |

예상 증가량(하루): 임베딩 +150~200건(HN 신규 100~150, HF 신규 20~30, RSS 30~50), 선별 +8회, 판정 +10~20회. Voyage 무료 등급(배치 64·간격 20초)과 선별 60회·판정 300회 상한 안이다. HF 는 대부분 기존 arXiv 항목과 충돌해 임베딩을 새로 하지 않는다.

---

## 7. 커밋 순서·검증·기록

| # | 커밋 | 파일 |
|---|---|---|
| 1 | `feat: 첫 폴링은 신선도 창(72h)만 — 새 소스의 백로그가 임베딩 예산을 쓰지 않게` | `app/jobs/collect.py`, `tests/test_collect.py` |
| 2 | `fix: 본문 보강이 사설·링크로컬 주소와 그리로 가는 리다이렉트를 열지 않게` | `app/pipeline/llm.py`, `tests/test_enrich_guard.py` |
| 3 | `feat: 적재 충돌 시 metrics·멘션 병합, 점수 탈락 항목은 값이 오르면 NEW 로 되살림` | `app/pipeline/ingest.py`, `app/jobs/pipeline.py`, `tests/test_ingest.py`, `tests/integration/test_ingest_merge_db.py` |
| 4 | `feat: Hacker News 프론트 페이지 수집기` | `app/sources/hackernews.py`, `app/sources/__init__.py`, `config/sources.yaml`, 테스트·fixture |
| 5 | `feat: Hugging Face Daily Papers 수집기 — arXiv 항목에 upvotes 병합` | `app/sources/hf_papers.py`, `app/sources/__init__.py`, `config/sources.yaml`, 테스트·fixture |
| 6 | `chore: RSS 소스 4개 추가 — GeekNews·요즘IT·Simon Willison·Anthropic engineering` | `config/sources.yaml` |

브랜치 `feat/source-expansion-1`. 6번 뒤 `run_job.py sync collect pipeline` 으로 로컬 1회 실행(`SCHEDULER_ENABLED=false` 또는 Neon dev — 운영 DB 를 VM 과 동시에 보지 않는다). Codex 범위 리뷰(`--base main --scope branch`) → 지적 반영 → PR → 옵시디언(기능 노트 `spoke/소스 확장 1차.md`, 허브 표, 일일 노트, 결정 기록 10장 행, 폴더·파일 역할 지도의 소스 파일 2개) → `graphify update .`.

---

## 8. 사용자 작업 — Reddit Data API 신청

코드와 무관하게 지금 제출해 두면 2차에 바로 쓴다.

1. `https://www.reddit.com/prefs/apps` 에서 앱 생성. 종류 **script**, 이름 `tech-radar`, redirect uri `http://localhost:8080`.
2. Reddit 지원 센터의 Data API 접근 요청 티켓 제출. 용도는 **비상업 개인 알림기**, 읽기 전용, 서브레딧당 15분 1회 `top?t=day`, 대상 r/LocalLLaMA · r/MachineLearning · r/ClaudeAI · r/mcp 정도.
3. 승인되면 `.env` 에 `REDDIT_CLIENT_ID`·`REDDIT_CLIENT_SECRET`. 2차 구현은 asyncpraw 없이 httpx 로 client_credentials 토큰 + 엔드포인트 하나(약 60줄).

---

## 9. 하지 않는 것

| 항목 | 이유 | 다시 볼 때 |
|---|---|---|
| X | 2026-02 무료 티어 폐지, 읽기 pay-per-use. Nitter 사망 | 유료를 감수할 만큼 X 전용 신호가 아쉬울 때. 대안 Bluesky 는 가치가 확인되면 |
| Reddit 코드 | OAuth 승인 전. 무인증은 1요청 뒤 429 | 승인 도착 후 2차 |
| Anthropic 사이트맵 · MCP 레지스트리 · GitHub Search | 새 타입 3개를 한 브랜치에 더 얹지 않는다 | 1차 운영 2주 뒤 2차 |
| HN `search_by_date` points 임계값 | 프론트 페이지 30건이 예산에 맞다 | 프론트가 놓치는 사례가 리포트에 보일 때 |
| 소스별 일일 적재 상한 | 첫 폴링 since 로 실제 문제(백로그)만 겨눈다 | 어떤 소스가 하루 수백 건을 넣을 때 |
| 판정 false 항목 되살림 | 사용자 결정. 같은 항목 이중 판정 경로를 만들지 않는다 | |
| DNS 리바인딩 방어 | 표준 라이브러리로 검사 뒤 연결 사이를 못 막는다 | 연결 소켓 peer 재검사가 필요해지면 |
| 되살림을 `decisions` 에 기록 | 판단이 아니라 재관측. 다음 점수 행의 `breakdown` 에 결과가 남는다 | 리포트가 되살림 횟수를 묻게 되면 로그 집계 |
| YouTube 자막 보강 | yt-dlp 의존성·VM 부하 | 영상 카드의 판정 품질이 문제로 드러나면 |
| PyPI 릴리즈 RSS | GitHub Releases 와 겹치면 버전 형제 노이즈 | GitHub 에 없는 패키지가 생기면 YAML 한 줄 |

---

## 10. 결정 기록에 추가할 행

| 결정 | 채택 | 기각한 대안 | 이유 |
|---|---|---|---|
| 같은 URL 재관측 | **`raw.metrics` max 병합 + `raw.mentions`, 점수 탈락만 되살림** | DO NOTHING 유지 / 별도 `mentions` 테이블 / 판정 false 도 되살림 | hot·multi 항이 살아나는 유일한 경로. 테이블은 지금 규모에 과함. 판정 false 는 이중 판정 |
| 되살림 조건 | **값 변경 + DROPPED + 마지막 결정 score 탈락 + 72h 안** | 상태 무관 재처리 | 같은 값 재관측이 결정 행을 쌓지 않아야 하고 stale 은 다시 죽는다 |
| HN 수집 범위 | **프론트 페이지 30건, since 무시** | `search_by_date` points>N | 프론트가 곧 커뮤니티 선별. 예산 안 |
| HF Papers URL | **`arxiv.org/abs/<id>` 로 arXiv 와 충돌시킴** | HF 페이지 URL | 새 항목이 아니라 arXiv 의 hotness 신호가 되게 |
| 새 RSS 백로그 | **첫 폴링 since = now − 72h** | 소스별 일일 상한 | 실제 문제는 첫 폴링뿐. 3줄 |
| 보강 SSRF | **getaddrinfo + `is_global`, 리다이렉트 수동 5홉** | 허용 호스트 목록 / 프록시 | 사용자 제출 URL 은 목록으로 못 막는다. 표준 라이브러리로 충분 |
| Reddit 클라이언트 | **httpx + OAuth client_credentials (2차)** | asyncpraw | 쓰는 엔드포인트 하나. 승인 티켓은 어느 쪽이든 필요 |
| X | **보류** | 공식 API / Nitter / 스크래핑 | 무료 티어 폐지, 미러 사망, 스크래핑은 약관·유지 비용 |
| GitHub Trending | **Search API 로 2차** | HTML 스크래핑 | 공식·안정. stars 가 hotness 키 |
| Anthropic 공식 | **사이트맵 폴링 2차** | 미러 유지만 | lastmod 531건 전부. 미러 의존 제거 + engineering 확보 |
