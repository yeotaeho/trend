# v2 → 다중 사용자 전환 1차 구현서

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

버전 v0.1 · 2026-09-08 · 설계 원문: `claude/v2-다중사용자-전환-메모.md` · 선행 문서: `검증파이프라인v2설계서.md`, `검증파이프라인v2구현서.md`

**Goal:** v2 파이프라인 구조를 흔들지 않으면서 (1) 사용자 식별자를 스키마에 넣고, (2) 선별 출력에 `kind`·`topics` 구조화 속성을 더해 점수 관문이 "변화의 종류"를 읽게 하고, (3) 같은 클러스터의 항목이 하루에 두 번 발송되지 않게 하고, (4) 피드백 사례 과일반화를 완화한다. 프로필 벡터·전역 선별 전환(B2C 본 설계)은 **이 문서 범위 밖**이다.

**Why:** 2026-09-08 실행 결과의 세 실패가 각각 v2 결정 하나에 대응된다.

| 결과 | 원인 | 이 문서의 대응 |
|---|---|---|
| MCP Python SDK v1.30.0 · v2.2.0 이 둘 다 옴 | §6 버전 예외가 백포트 릴리즈를 관련로 강등 | Task 3 클러스터당 하루 1건 발송 |
| BeaconKV(만족한 유일한 항목)가 🧪 탐색 슬롯으로만 옴 | arXiv trust 0.5 → 혼자 통과하려면 relevance ≥ 0.9 | Task 2 `kind` 가중치 + Task 5 리포트로 문턱 조정 |
| SDLC 서베이 논문 통과 | relevance 한 숫자에 주제·변화종류가 섞임 | Task 2 `kind: survey` 감점 |

**Architecture:** 선별 배치는 그대로 25건/1호출이고 출력 필드만 는다. 점수식은 항 하나(`kind`)가 더해진다. 발송 억제는 `notify/policy.py` 한 곳. 스키마는 마이그레이션 0004 하나로 `users`·`feedback.user_id`·`notifications.user_id` 를 넣고, 코드는 `DEFAULT_USER_ID = 1` 상수로 단일 사용자를 유지한다.

**Tech Stack:** 기존과 동일 — Python 3.12 · SQLAlchemy 2 async + asyncpg · Alembic · Neon(pgvector) · Anthropic SDK `messages.parse` · pytest + respx

## Global Constraints

- 이 문서의 파일 경로·함수명은 `검증파이프라인v2구현서.md` 의 인터페이스를 기준으로 썼다. **각 Task 시작 전에 `grep` 으로 실제 위치와 시그니처를 확인**하고 어긋나면 이 문서가 아니라 코드를 따른다. 줄 번호는 적지 않았다.
- 상태 값 추가 없음(`NEW · FILTERED_OUT · DROPPED · SCORED · QUEUED · SENT · FAILED`). `notifications.level` 과 `decisions.stage` 는 문자열 컬럼이라 값만 는다.
- 단일 사용자 코드 경로는 `DEFAULT_USER_ID = 1` 로 돈다. 사용자 조회·인증·온보딩은 만들지 않는다.
- 선별 호출 수·판정 호출 수·`llm_calls` 예약 방식은 바꾸지 않는다. `kind`·`topics` 는 같은 호출의 출력 필드다.
- `rules.yaml` 은 `extra="forbid"` 유지. 새 키는 전부 기본값이 있어야 기존 YAML 로도 기동된다.
- 일일 상한·"오늘" 은 기존 `today_start(tz, now)` 를 재사용한다. 새로 날짜 계산을 만들지 않는다.
- 한국어 문장 종결은 `.` `?` `!`. 새 소스 파일 첫 줄은 한 줄 한국어 역할 주석.
- 각 Task 끝: `uv run ruff check . && uv run ruff format . && uv run mypy app && uv run pytest -q` 통과 → 커밋 → (단계 끝) Codex 리뷰 → 옵시디언 기록.

---

## 파일 지도

| 파일 | Task | 책임 |
|---|---|---|
| `app/db/alembic/versions/20260908_0004_users.py` (신규) | 1 | `users`, `feedback.user_id`, `notifications.user_id`, 유니크 교체 |
| `app/db/models.py` | 1 | `User`, `Feedback.user_id`, `Notification.user_id` |
| `app/db/feedback.py`, `app/api/telegram.py`, `app/api/discord.py` | 1 | upsert 에 `user_id` |
| `app/config.py`, `config/rules.yaml` | 2, 3 | `taxonomy`, `ScoringConfig.kind_weights`, `NotifyConfig.cluster_daily_cap` |
| `app/pipeline/triage.py` | 2, 4 | `TriageItem.kind/topics`, 프롬프트, `parse_triage` 검증 |
| `app/pipeline/scoring.py` | 2 | `score_item(kind=...)` |
| `app/jobs/pipeline.py` | 2 | 선별 결과 → `decisions.details` 에 `kind·topics·prompt_version`, 점수 호출에 `kind` 전달 |
| `app/notify/policy.py`, `app/jobs/notify.py` | 3 | 클러스터당 하루 1건, 제목 버전 병기 |
| `app/pipeline/llm.py` | 4 | 판정 프롬프트 지시 완화 |
| `scripts/weekly_report.py` | 5 | (있으면) 실행만. 없으면 최소 표 하나 |

---

# Task 0: 전제 확인

- [ ] `uv run alembic current` → `0003 (head)` 인지 확인. 0003 이 아직 미적용이면 이 문서의 Task 1 을 0003 에 합치지 말고 **그대로 0004 로 진행**한다 (0003 은 이미 커밋된 계약이다).
- [ ] `git grep -n "explore" app/jobs/notify.py` 로 탐색 슬롯이 있는지 확인. 있으면 v2 3단계까지 구현된 상태다.
- [ ] `git grep -n "def score_item\|def parse_triage\|class TriageItem\|def feedback_upsert_stmt\|def today_start" app/` 로 인터페이스 위치를 적어 둔다.
- [ ] `ls scripts/weekly_report.py` — 있으면 Task 5 는 실행만, 없으면 최소 구현.

---

# Task 1: 사용자 식별자 — 마이그레이션 0004

끝나면: `users(id=1)` 이 있고, `feedback`·`notifications` 에 `user_id` 가 있으며, 피드백 유니크가 `(user_id, item_id)` 다. 동작 변화 없음.

**Files:**
- Create: `app/db/alembic/versions/20260908_0004_users.py`
- Modify: `app/db/models.py`, `app/db/feedback.py`, `app/api/telegram.py`, `app/api/discord.py`, `app/jobs/notify.py`
- Test: `tests/test_feedback_upsert.py`

**Interfaces:**
- Produces: `User(id, name, created_at)`, `Feedback.user_id`, `Notification.user_id`, `DEFAULT_USER_ID = 1` (`app/db/users.py` 신규), `feedback_upsert_stmt(user_id, item_id, verdict)`, `upsert_feedback(session, user_id, item_id, verdict)`

- [ ] **Step 1: 실패하는 테스트**

`tests/test_feedback_upsert.py` 를 아래로 바꾼다.

```python
# 피드백 upsert 문장 테스트 — (user_id, item_id) 충돌 시 verdict·created_at 덮어쓰기

from sqlalchemy.dialects import postgresql

from app.db.feedback import feedback_upsert_stmt


def test_upsert_conflict_on_user_and_item():
    sql = str(feedback_upsert_stmt(1, 7, "useless").compile(dialect=postgresql.dialect()))
    assert "ON CONFLICT (user_id, item_id) DO UPDATE" in sql
    assert "verdict = excluded.verdict" in sql
    assert "created_at = now()" in sql
```

Run: `uv run pytest tests/test_feedback_upsert.py -v` → FAIL (인자 개수).

- [ ] **Step 2: 모델**

```python
# app/db/users.py
# 사용자 상수 — 다중 사용자 전환 전까지 모든 경로가 이 id 로 돈다

DEFAULT_USER_ID = 1
```

`app/db/models.py`:

```python
class User(Base):
    __tablename__ = "users"

    id: Mapped[int] = mapped_column(primary_key=True)
    name: Mapped[str] = mapped_column(String(50))
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())


class Feedback(Base):
    __tablename__ = "feedback"
    # 판정은 (사용자, 항목) 당 하나. 다시 누르면 덮어쓴다.
    __table_args__ = (UniqueConstraint("user_id", "item_id", name="uq_feedback_user_item"),)

    id: Mapped[int] = mapped_column(primary_key=True)
    user_id: Mapped[int] = mapped_column(ForeignKey("users.id", ondelete="CASCADE"), index=True)
    item_id: Mapped[int] = mapped_column(ForeignKey("items.id", ondelete="CASCADE"), index=True)
    verdict: Mapped[str] = mapped_column(String(10))
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())
```

`Notification` 에 `user_id: Mapped[int] = mapped_column(ForeignKey("users.id", ondelete="CASCADE"), index=True)` 추가.

- [ ] **Step 3: 마이그레이션**

```python
# app/db/alembic/versions/20260908_0004_users.py
"""users 테이블, feedback·notifications.user_id, 피드백 유니크 (user_id, item_id) 로 교체

Revision ID: 0004
Revises: 0003
"""

from __future__ import annotations

import sqlalchemy as sa
from alembic import op

revision = "0004"
down_revision = "0003"
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.create_table(
        "users",
        sa.Column("id", sa.Integer(), primary_key=True),
        sa.Column("name", sa.String(50), nullable=False),
        sa.Column("created_at", sa.DateTime(timezone=True), server_default=sa.func.now(), nullable=False),
    )
    op.execute("INSERT INTO users (id, name) VALUES (1, 'owner')")
    op.execute("SELECT setval('users_id_seq', 1)")

    for table in ("feedback", "notifications"):
        op.add_column(table, sa.Column("user_id", sa.Integer(), nullable=False, server_default="1"))
        op.create_foreign_key(f"fk_{table}_user_id", table, "users", ["user_id"], ["id"], ondelete="CASCADE")
        op.create_index(f"ix_{table}_user_id", table, ["user_id"])
        op.alter_column(table, "user_id", server_default=None)

    op.drop_constraint("uq_feedback_item_id", "feedback", type_="unique")
    op.create_unique_constraint("uq_feedback_user_item", "feedback", ["user_id", "item_id"])


def downgrade() -> None:
    op.drop_constraint("uq_feedback_user_item", "feedback", type_="unique")
    op.create_unique_constraint("uq_feedback_item_id", "feedback", ["item_id"])
    for table in ("notifications", "feedback"):
        op.drop_index(f"ix_{table}_user_id", table_name=table)
        op.drop_constraint(f"fk_{table}_user_id", table, type_="foreignkey")
        op.drop_column(table, "user_id")
    op.drop_table("users")
```

`server_default="1"` 로 기존 행을 채운 뒤 기본값을 떼는 이유: 이후 코드가 `user_id` 를 빠뜨리면 NOT NULL 위반으로 즉시 드러나야 한다.

- [ ] **Step 4: upsert 와 호출부**

```python
# app/db/feedback.py
def feedback_upsert_stmt(user_id: int, item_id: int, verdict: str) -> Insert:
    stmt = insert(Feedback).values(user_id=user_id, item_id=item_id, verdict=verdict)
    return stmt.on_conflict_do_update(
        constraint="uq_feedback_user_item",
        set_={"verdict": stmt.excluded.verdict, "created_at": func.now()},
    )


async def upsert_feedback(session: AsyncSession, user_id: int, item_id: int, verdict: str) -> None:
    await session.execute(feedback_upsert_stmt(user_id, item_id, verdict))
```

`app/api/telegram.py`, `app/api/discord.py`: `await upsert_feedback(session, DEFAULT_USER_ID, item_id, verdict)`.
`app/jobs/notify.py`: `Notification(...)` 생성부 전부에 `user_id=DEFAULT_USER_ID` (탐색 슬롯 `_explore` 포함). `git grep -n "Notification(" app/` 로 빠짐없이.

`nearest_feedback` (`app/pipeline/feedback.py`) SQL 에 `AND f.user_id = :user_id` 를 추가하고 인자로 `user_id: int = DEFAULT_USER_ID` 를 받는다. 프로필 벡터로 옮기기 전까지는 사용자 1 의 피드백만 본다.

- [ ] **Step 5: 적용·검증·커밋**

Run: `uv run alembic upgrade head` → `0003 -> 0004`.
Run: `uv run ruff check . && uv run mypy app && uv run pytest -q` → 통과. 통합 테스트(`TEST_DATABASE_URL`)의 `Feedback(...)` 생성부에 `user_id=1` 추가.

```bash
git add app/db app/api app/jobs/notify.py app/pipeline/feedback.py tests
git commit -m "feat: users 테이블과 feedback·notifications.user_id — 마이그레이션 0004, 단일 사용자는 DEFAULT_USER_ID"
```

---

# Task 2: 선별 출력에 `kind`·`topics`, 점수 관문에 `kind` 가중치

끝나면: `decisions` 의 triage 행에 `kind`·`topics`·`prompt_version` 이 남고, 서베이·패치 릴리즈는 점수에서 감점되고 기법은 가점된다.

**Files:**
- Modify: `app/config.py`, `config/rules.yaml`, `app/pipeline/triage.py`, `app/pipeline/scoring.py`, `app/jobs/pipeline.py`
- Test: `tests/test_triage.py`, `tests/test_scoring.py`

**Interfaces:**
- Produces:
  - `Kind = Literal["release_major","release_patch","technique","survey","news","tutorial","promo","other"]` (`app/schemas.py`)
  - `TriageItem(idx, relevance, reason, kind: Kind, topics: list[str])`
  - `PolicyConfig.taxonomy: list[str]` (기본값은 아래 YAML)
  - `ScoringConfig.kind_weights: dict[str, float]`
  - `score_item(cfg, *, trust, relevance, kind, metrics, mention_count, published_at, now=None)` — breakdown 에 `kind` 항 추가
  - `TRIAGE_PROMPT_VERSION = "2026-09-08.1"` (`triage.py`)

- [ ] **Step 1: 설정**

`config/rules.yaml` 의 `policy:` 아래에 추가 (사용자 카테고리 선택 UI 와 같은 어휘가 될 고정 분류표):

```yaml
policy:
  interests: |          # 유지
  not_interested: |     # 유지
  focus_repos: [...]
  focus_stack: [...]
  # 선별이 항목마다 1~3개 고르는 고정 분류. 나중에 사용자 카테고리 선택과 같은 표를 쓴다.
  taxonomy:
    - llm-model          # 새 모델, 벤치마크
    - agent              # 에이전트 패턴, 오케스트레이션
    - mcp-tooling        # MCP 서버, IDE 플러그인, CLI
    - inference-opt      # 추론 최적화, KV 캐시, 양자화
    - rag-retrieval
    - training-finetune
    - python-backend     # FastAPI, Pydantic, uv, DB
    - web-frontend       # Next.js, React
    - devops-infra
    - ai-safety-eval
    - dev-community      # 논쟁, 가격·정책 변경
    - video
scoring:
  # ... 기존 유지 ...
  # kind 별 가점/감점. 가중치 곱이 아니라 그대로 더한다. 0 이면 영향 없음.
  kind_weights:
    release_major: 0.05
    release_patch: -0.15
    technique: 0.10
    survey: -0.15
    news: 0.0
    tutorial: -0.05
    promo: -0.30
    other: 0.0
```

`app/config.py`: `PolicyConfig.taxonomy: list[str] = Field(default_factory=lambda: [...위 12개...])`, `ScoringConfig.kind_weights: dict[str, float] = Field(default_factory=lambda: {...위 값...})`. 둘 다 기본값이 있어 기존 YAML 로도 기동된다. `kind_weights` 의 키가 `Kind` 밖이면 검증 오류를 내는 `field_validator` 를 둔다.

- [ ] **Step 2: 실패하는 테스트 — triage**

`tests/test_triage.py` 에 추가:

```python
def test_item_carries_kind_and_topics():
    it = TriageItem(idx=1, relevance=0.8, reason="r", kind="technique", topics=["inference-opt"])
    assert it.kind == "technique"


def test_unknown_topic_is_dropped_not_failed():
    batch = TriageBatch(items=[TriageItem(idx=1, relevance=0.5, reason="r", kind="news", topics=["inference-opt", "nope"])])
    ok, failed = parse_triage(batch, [1], taxonomy=["inference-opt"])
    assert failed == [] and ok[1].topics == ["inference-opt"]


def test_more_than_three_topics_truncated():
    batch = TriageBatch(items=[TriageItem(idx=1, relevance=0.5, reason="r", kind="news", topics=["a", "b", "c", "d"])])
    ok, _ = parse_triage(batch, [1], taxonomy=["a", "b", "c", "d"])
    assert ok[1].topics == ["a", "b", "c"]


def test_user_content_unchanged_by_new_fields():
    # 입력 형식은 그대로다. 출력 스키마만 는다.
    assert build_user_content(entries()).startswith("[1] ")
```

기존 `TriageItem(...)` 생성부에 `kind="news", topics=[]` 를 채운다. Run → FAIL.

- [ ] **Step 3: triage 구현**

`app/schemas.py` 에 `Kind` 리터럴 추가. `app/pipeline/triage.py`:

```python
TRIAGE_PROMPT_VERSION = "2026-09-08.1"  # 출력 스키마나 눈금이 바뀔 때마다 올린다. decisions 에 남는다.
MAX_TOPICS = 3

SYSTEM_PROMPT = """\
너는 개인용 개발 트렌드 알림기의 선별 단계다. 항목마다 세 가지를 매긴다.
정독·요약은 다음 단계가 하니 여기서는 빠르게 판단한다.

사용자 정책:
{policy}

1) relevance (0~1) — 이 사용자가 읽을 후보인가.
- 0.9 이상: 새 모델·릴리즈·기법·도구의 직접적인 변화. 사용자가 알아야 할 것.
- 0.6: 관심 주제의 일반적인 글. 읽으면 도움이 되지만 변화는 아님.
- 0.3: 주변부. 관심 주제와 스치는 정도.
- 0: 무관, 홍보, 구인, 스폰서, 잡담.
목록에 없는 새 이름·용어라도 문맥이 정책과 맞으면 높게 준다. 키워드가 아니라 뜻으로 판단한다.

2) kind — 변화의 종류. 반드시 하나.
- release_major: 메이저 버전, 새 제품·모델 출시, 큰 기능 추가
- release_patch: 패치·유지보수·보안 백포트·마이너 정비
- technique: 구체적인 방법과 결과 수치가 있는 기법·논문
- survey: 서베이·종합·전망·프레임워크 제안 (수치 없는 논의)
- news: 정책·가격·장애·인수 등 사건
- tutorial: 사용법·입문·따라하기
- promo: 홍보·구인·스폰서
- other

3) topics — 아래 분류표에서 1~3개. 표에 없는 값은 쓰지 않는다.
{taxonomy}

"유사 피드백" 이 붙은 항목은 사용자가 비슷한 글에 남긴 👍/👎 이다. 참고하되, 항목 자체의 변화 크기를 우선한다.
reason 은 20단어 이내 한국어 한 문장.
모든 항목에 대해 idx 를 그대로 돌려준다.
"""


class TriageItem(BaseModel):
    idx: int
    relevance: float
    reason: str
    kind: Kind
    topics: list[str]
```

`parse_triage(batch, expected, *, taxonomy: list[str])`: `kind` 는 pydantic 리터럴이 막는다(범위 밖이면 SDK 파싱 실패 → 배치 실패로 취급, 기존 층위 유지). `topics` 는 분류표 밖 값을 **버리고** 앞 3개로 자른다 — 항목 실패로 세지 않는다. `call_triage` 의 시스템 프롬프트에 `taxonomy="\n".join(f"- {t}" for t in rules.policy.taxonomy)` 를 채운다. 시스템 블록은 여전히 정책만 담으므로 캐시는 유지된다.

- [ ] **Step 4: 실패하는 테스트 — scoring**

`tests/test_scoring.py` 의 `score_item(...)` 호출 전부에 `kind="news"` 추가. 그리고:

```python
def test_kind_weight_is_added_not_multiplied():
    base = score_item(CFG, trust=0.5, relevance=0.8, kind="news", metrics={}, mention_count=1, published_at=NOW, now=NOW)
    tech = score_item(CFG, trust=0.5, relevance=0.8, kind="technique", metrics={}, mention_count=1, published_at=NOW, now=NOW)
    assert tech.score == pytest.approx(base.score + CFG.kind_weights["technique"])
    assert tech.breakdown["kind"] == CFG.kind_weights["technique"]


def test_arxiv_technique_passes_at_0_8_relevance():
    # 2026-09-08 BeaconKV 사례. trust 0.5, rel 0.8, 24h 이내면 technique 은 통과해야 한다.
    r = score_item(CFG, trust=0.5, relevance=0.8, kind="technique", metrics={}, mention_count=1, published_at=NOW, now=NOW)
    assert r.passed


def test_arxiv_survey_fails_at_same_relevance():
    r = score_item(CFG, trust=0.5, relevance=0.8, kind="survey", metrics={}, mention_count=1, published_at=NOW, now=NOW)
    assert not r.passed
```

두 arXiv 테스트가 기본 가중치로 통과하지 않으면 **테스트를 고치지 말고** `kind_weights` 기본값을 조정한다. 이 두 케이스가 이 Task 의 수용 기준이다. (기본값 기준: 0.25·0.5 + 0.25·0.8 + 0.1·1.0 = 0.425. technique +0.10 → 0.525 통과, survey −0.15 → 0.275 탈락.)

- [ ] **Step 5: scoring 구현**

```python
def score_item(cfg, *, trust, relevance, kind: Kind, metrics, mention_count, published_at, now=None) -> ScoreResult:
    ...
    breakdown = {
        "src": cfg.w_src * trust,
        "rel": cfg.w_rel * relevance,
        "hot": ...,
        "multi": ...,
        "fresh": ...,
        "kind": cfg.kind_weights.get(kind, 0.0),   # 곱이 아니라 덧셈. 0 이면 영향 없음
    }
```

- [ ] **Step 6: 파이프라인 연결**

`app/jobs/pipeline.py`:
- `_triage` 가 `decisions.details` 에 `{"relevance", "reason", "kind", "topics", "batch_id", "prompt_version": TRIAGE_PROMPT_VERSION}` 을 남긴다.
- `_existing_relevance` 가 `details` 에서 `kind`·`topics` 도 복원한다. 옛 행(필드 없음)은 `kind="other", topics=[]` 로 채운다 — 재처리 시 다시 호출하지 않는다.
- `_judge` 의 `score_item(...)` 호출에 `kind=tri.kind` 전달.
- `parse_triage(..., taxonomy=rules.policy.taxonomy)`.

- [ ] **Step 7: 실행 확인**

Run: `PYTHONIOENCODING=utf-8 uv run python scripts/run_job.py pipeline`
Expected: `decisions` 최신 triage 행 25개의 `details->>'kind'` 분포를 SQL 로 보고, arXiv 항목에 `survey`/`technique` 가 갈려 있는지 확인. 전부 `other` 면 프롬프트 문제다.

```sql
SELECT details->>'kind' AS kind, count(*)
FROM decisions WHERE stage='triage' AND details ? 'prompt_version'
GROUP BY 1 ORDER BY 2 DESC;
```

- [ ] **Step 8: 검증·커밋**

```bash
git add app/schemas.py app/config.py config/rules.yaml app/pipeline/triage.py app/pipeline/scoring.py app/jobs/pipeline.py tests
git commit -m "feat: 선별 출력에 kind·topics, 점수 관문에 kind 가중치 — 서베이·패치 감점, 기법 가점"
```

---

# Task 3: 클러스터당 하루 1건 발송

끝나면: 같은 클러스터의 두 번째 항목은 발송되지 않고 `notifications(level=cluster_dup)` 로만 남으며, 첫 발송 제목에 형제 항목의 버전이 병기된다.

**Files:**
- Modify: `app/config.py`, `config/rules.yaml`, `app/notify/policy.py`, `app/jobs/notify.py`, `app/schemas.py`
- Test: `tests/test_notify_policy.py`, `tests/integration/test_cluster_cap_db.py`

**Interfaces:**
- Produces:
  - `Level.CLUSTER_DUP = "cluster_dup"` — push 상한 집계에서 제외, 실제 발송 없음
  - `NotifyConfig.cluster_daily_cap: int = 1`
  - `async cluster_sent_today(session, cluster_id, user_id, now) -> int` (`app/notify/policy.py`)
  - `sibling_versions(own_title, sibling_titles) -> list[str]` (순수 함수; `dedupe.version_tokens` 재사용)
  - `decorate_title(title_ko, versions) -> str`

- [ ] **Step 1: 실패하는 테스트 (순수)**

```python
# tests/test_notify_policy.py 에 추가
from app.notify.policy import decorate_title, sibling_versions


def test_sibling_versions_collects_distinct_tokens_in_order():
    vs = sibling_versions("MCP Python SDK v2.2.0 released", ["MCP Python SDK v1.30.0 released", "v2.2.0 notes"])
    assert vs == ["v2.2.0", "v1.30.0"]


def test_decorate_title_appends_versions_once():
    t = decorate_title("[릴리즈] MCP Python SDK v2.2.0 — HTTP 리다이렉트·OAuth 검증 변경", ["v2.2.0", "v1.30.0"])
    assert t.endswith("(v2.2.0 · v1.30.0)")


def test_decorate_title_noop_for_single_version():
    assert decorate_title("x", ["v1"]) == "x"
```

- [ ] **Step 2: 구현 (policy.py)**

```python
_CLUSTER_SENT_SQL = text(
    """
    SELECT count(*) FROM notifications n
    JOIN items i ON i.id = n.item_id
    WHERE i.cluster_id = :cluster_id
      AND n.user_id = :user_id
      AND n.sent_at >= :today_start
      AND n.level IN ('push', 'silent', 'explore')
    """
)


async def cluster_sent_today(session, cluster_id: int, user_id: int, now: datetime) -> int:
    start = today_start(get_rules().notify.timezone, now)
    return int(await session.scalar(_CLUSTER_SENT_SQL, {"cluster_id": cluster_id, "user_id": user_id, "today_start": start}) or 0)


def sibling_versions(own_title: str, sibling_titles: list[str]) -> list[str]:
    seen: list[str] = []
    for title in [own_title, *sibling_titles]:
        for tok in sorted(version_tokens(title)):
            if tok not in seen:
                seen.append(tok)
    return seen


def decorate_title(title_ko: str, versions: list[str]) -> str:
    if len(versions) < 2:
        return title_ko
    return f"{title_ko} ({' · '.join(versions)})"
```

- [ ] **Step 3: 발송 잡 연결**

`app/jobs/notify.py` 의 일반 발송 루프에서, 강도 결정 뒤 **push·silent 인 경우에만**:

1. `cluster_sent_today(...) >= cfg.cluster_daily_cap` 이면 발송하지 않고 `Notification(level=Level.CLUSTER_DUP, message_id=None, user_id=...)` 를 넣고 항목을 `SENT` 로 둔다. `decisions` 에는 남기지 않는다(발송 정책이지 판정이 아님).
2. 아니면 같은 클러스터의 다른 항목(상태 `SCORED`·`QUEUED`·`SENT`, 72시간 창) 제목을 모아 `decorate_title(summary.title_ko, sibling_versions(item.title, titles))` 로 제목을 바꿔 보낸다. 형제가 없으면 제목 그대로다.

`feed` 레벨은 원래 발송이 없으므로 건드리지 않는다. 탐색 슬롯 `_explore` 는 별도 흐름이지만 후보를 고를 때 `cluster_sent_today(...) == 0` 조건을 후보 쿼리에 더한다 — 이미 알린 클러스터를 실험으로 또 보내면 안 된다.

- [ ] **Step 4: 통합 테스트**

`tests/integration/test_cluster_cap_db.py`: 같은 `cluster_id` 의 항목 A(SENT, 오늘 push) 와 B(SCORED, worth_notifying) 를 만들고 발송 잡을 한 번 돌리면 B 의 `notifications.level == "cluster_dup"` 이고 발송 어댑터가 호출되지 않아야 한다(어댑터는 monkeypatch). 반대로 A 가 어제면 B 는 발송된다.

- [ ] **Step 5: 설정·검증·커밋**

`rules.yaml` `notify:` 에 `cluster_daily_cap: 1`. `NotifyConfig` 기본값 1.

```bash
git add app/config.py config/rules.yaml app/notify/policy.py app/jobs/notify.py app/schemas.py tests
git commit -m "feat: 클러스터당 하루 1건 발송 — 백포트·형제 릴리즈 억제, 제목에 버전 병기"
```

---

# Task 4: 피드백 사례 지시 완화

끝나면: 선별·판정 프롬프트 어디에도 "강하게" 가 없다.

**Files:**
- Modify: `app/pipeline/triage.py` (Task 2 에서 이미 반영됨 — 확인만), `app/pipeline/llm.py`

- [ ] **Step 1**: `app/pipeline/llm.py` `SYSTEM_PROMPT` 의 `- "유사 피드백" 은 사용자가 비슷한 글에 남긴 👍/👎 다. importance 와 worth_notifying 에 강하게 반영한다.` 를 아래로 바꾼다.

```
- "유사 피드백" 은 사용자가 비슷한 글에 남긴 👍/👎 다. 참고하되, 이 항목 자체의 변화 크기와 새로움을 우선한다. 같은 출처·같은 화자라는 이유만으로 같은 판정을 내리지 않는다.
```

- [ ] **Step 2**: `git grep -n "강하게" app/pipeline/` 결과가 비어야 한다.
- [ ] **Step 3**: 커밋 `fix: 피드백 사례 과일반화 완화 — 출처 동일만으로 같은 판정 금지`.

---

# Task 5: arXiv 문턱을 데이터로 — 리포트 실행

끝나면: relevance 구간 × kind 별 통과/탈락/👍/👎 표를 보고 `kind_weights` 와 `scoring.threshold` 를 정할 근거가 있다.

- [ ] **Step 1**: `scripts/weekly_report.py` 가 있으면 실행하고 "선별 relevance 구간 vs 판정 결과" 표를 본다. 없으면 아래 SQL 하나를 `scripts/report_relevance_kind.py` 로 만든다 (표 출력만).

```sql
WITH tri AS (
  SELECT DISTINCT ON (item_id) item_id,
         (details->>'relevance')::float AS rel,
         coalesce(details->>'kind', 'other') AS kind
  FROM decisions WHERE stage = 'triage' ORDER BY item_id, created_at DESC
), last AS (
  SELECT DISTINCT ON (item_id) item_id, stage, passed
  FROM decisions ORDER BY item_id, created_at DESC
)
SELECT s.name AS source, floor(tri.rel * 10) / 10 AS rel_band, tri.kind,
       count(*) AS n,
       sum((i.status IN ('SCORED','QUEUED','SENT'))::int) AS survived,
       sum((f.verdict = 'useful')::int) AS up,
       sum((f.verdict = 'useless')::int) AS down
FROM tri JOIN items i ON i.id = tri.item_id
JOIN sources s ON s.id = i.source_id
LEFT JOIN feedback f ON f.item_id = i.id AND f.user_id = 1
WHERE i.published_at >= now() - interval '14 days'
GROUP BY 1, 2, 3 ORDER BY 1, 2 DESC, 3;
```

- [ ] **Step 2**: 표를 옵시디언 일일 노트에 붙이고, arXiv `technique` 이 몰린 구간이 `threshold` 를 넘는지 확인한다. 안 넘으면 `kind_weights.technique` 를 올리지 말고 **`w_src` 를 0.25 → 0.20, `w_rel` 을 0.25 → 0.30** 으로 먼저 옮겨 본다 (소스 신뢰도가 신호 밀도를 대신 벌하고 있다는 게 진단이므로). 바꾼 값은 `rules.yaml` 로만 간다.
- [ ] **Step 3**: 커밋 `chore: relevance×kind 리포트, 점수 가중치 조정 (근거: 09-08~ 리포트)`.

---

# 마무리

- [ ] 전체 검사: `uv run ruff check . && uv run ruff format . && uv run mypy app && uv run pytest -q`
- [ ] `git grep -n "uq_feedback_item_id\|강하게" app/` 비어 있음.
- [ ] Codex 리뷰: `codex exec --sandbox read-only "커밋 범위 <0004 첫 커밋>..HEAD 를 리뷰. 관점: user_id 가 빠진 Notification/Feedback 생성부, kind 리터럴 밖 값의 처리 층위, 클러스터 억제와 push 상한의 상호작용, 탐색 슬롯이 억제된 클러스터를 다시 뽑는지" </dev/null`
- [ ] 옵시디언: `spoke/다중 사용자 전환 1차.md` 기능 노트, 허브 표 갱신.
- [ ] 2주 운용 뒤 Task 5 리포트를 다시 뽑아 `claude/v2-다중사용자-전환-메모.md` §3-B(프로필 벡터·전역 선별) 설계로 넘어간다.

## 이 문서가 하지 않는 것

- 프로필 벡터 `p_u`, 전역 선별 전환, 온보딩, 사용자별 카테고리 필터, 사용자별 알림 정책, 발송 팬아웃. 전부 메모 §3-B·§6-5 의 별도 설계다.
- `feedback.user_id` 를 실제로 1 이외 값으로 쓰는 경로. 텔레그램 딥링크 등록은 그 설계에 포함된다.
